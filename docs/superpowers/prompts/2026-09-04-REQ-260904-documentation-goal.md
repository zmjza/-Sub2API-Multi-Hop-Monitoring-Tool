你自行判断本任务是否需要多智能体协作；如需开启，只允许一个主智能体统筹，任一时刻最多同时运行五个子代理，子代理完成后必须及时回收，回收后可按需启用新的子代理，禁止子代理继续派生代理。

请直接开始执行下面步骤，先读取项目规则、正式 PRD、设计文档、相关模块代码和现有测试，再完成深度流程第 1/2 阶段的文档与任务拆分，不要先解释；本阶段不编辑业务实现文件。

## 任务性质

这是老项目的跨模块功能优化，当前处于深度流程、非 UI 壳路线。基础目标数为 2，目标总数为 2，当前为第 1/2 个目标，目标阶段为 documentation。当前目标只负责真实文档、任务模块、微观任务、开发追踪、测试准备和真机验收准备；禁止进入 UI 壳施工、业务源码修改、真实验收、发布和归档。最终验收责任已经确认由 Codex 承担，后续开发与验收目标需要执行 macOS 真机和适用的页面行为/样式检查；Windows 只记录交叉构建证据。

## 当前场景

这是一个已有 Electron + React 桌面项目的跨模块优化任务。PRD 和设计已由用户确认，当前需要把确认内容翻译为可执行的文档、任务和验收准备，避免开发阶段重新猜测数据口径、删除边界、切换行为或动效范围。

## 目标和完成结果

本目标完成后，D1–D6 每一项都必须有带编号、带依赖、带文件边界、带测试和带完成条件的微观任务；受影响长期文档、开发追踪、测试用例、真机清单和当前 UI 接线事实均已真实更新；业务实现文件保持无改动。只有这些证据同时成立，才允许解锁第 2/2 个 development-and-acceptance 目标。

## 读取要求

先读取项目根 `AGENTS.md`、`docs/pitfalls/README.md`、Electron 构建、工具链、倍率比较相关 pitfalls，以及正式 PRD、设计文档、`liran_docs/03-索引.md`、`04-开发追踪.md`、`08-测试用例.md`、`09-真机实测.md`、`10-UI壳接入清单.md`。再读取 M08/M09/M10/M11/M01/M02/M16 模块文档、OpenCodex 归一化/页面、总览推荐、Sub2API/Radar/常用网站状态和测试的真实实现与调用方。记录 Git 状态和只读命令结果，不把未读取内容当事实。

## 修改范围

本目标允许修改的文件仅限正式 PRD、`liran_docs/03-索引.md`、`liran_docs/04-开发追踪.md`、`liran_docs/08-测试用例.md`、`liran_docs/09-真机实测.md`、`liran_docs/10-UI壳接入清单.md`、受影响 M01/M02/M08/M09/M10/M11/M16 模块文档、必要的 API 文档/数据字典长期事实，以及本任务的文档/任务拆分文件。不得修改 `src/`、`electron/`、`scripts/` 业务实现文件，不得新增依赖、数据库迁移或 UI 壳文件。

## 流程合同

流程档位：深度流程
修改前评分：78/100
基础目标数量：2
目标提示词数量：2
当前目标：第 1/2 次
目标阶段：documentation
UI 目标：跳过
UI 路线：not-applicable
等待外部输入：否
验收责任：Codex 真机测试
页面样式检查：不单独创建 UI 壳或 Design QA；开发与验收阶段检查现有页面在桌面、窄窗口和 380×260 悬浮窗中的动画可用性、焦点、无重叠和无溢出。
当前阶段：文档目标
编码解锁条件：受影响文档、全部微观任务、开发追踪、测试清单和真机清单均真实存在并通过产物审计，且业务实现文件无 diff
允许升级条件：只有真实 diff、重新搜索到的调用方、公共行为变化或验证复杂度使范围超过深度流程时，才提供修改后评分和证据升级
上下文压缩后必须恢复本流程合同，不得重新猜测档位。
没有修改后评分证据不得改变档位。
当前状态：文档准备中
已完成门禁：需求与设计已确认；用户已选择 Codex 真机测试；正式 PRD 和设计文档已经提交
待完成门禁：受影响长期文档已更新；全部已确认需求已拆为带编号的任务和微观任务；真实 diff、文件存在性和非占位内容已检查；所有业务实现文件仍无改动
允许下一动作：只读调查、更新文档、拆分任务、建立测试用例、建立真机清单、运行文档校验器并输出下一目标解锁条件
禁止动作：修改 `src`、`electron`、`scripts` 实现代码；删除或迁移数据库数据；新增依赖；改 OpenCodex 服务端；改 IPC 协议；做 UI 壳施工；启动真实站点写操作；真机验收留待最终目标；发布版本；创建归档
需求总结已输出：是，正式 PRD 和设计文档已提供
需求总结已确认：是，用户已逐部分确认
验收责任已确认：是，Codex 真机测试
PRD 草稿已转换：是
PRD 索引已更新：是，关联文档索引已登记
正式 PRD 已输出：是
PRD 确认状态：已确认
提示词状态：待校验
真实产物审计：已要求执行者在本目标结束前完成
进入证据：正式 PRD 已存在；设计文档已提交；用户已确认数据口径、推荐删除范围、快速切换方案和动效方案；用户已选择 Codex 真机测试
阻塞原因：不适用
状态转换规则：文档准备中 → 任务拆分中 → 开发追踪已更新 → 验收准备中 → 编码已解锁；本目标只能完成文档阶段，不得跳过状态
需求整理好了只能进入待详细总结确认。
本阶段职责：把已确认需求翻译成可执行的文档和微观任务，确保开发目标无需重新猜测范围。
禁止进入的后续阶段：在本目标结束前不得进入 ui、development-and-acceptance、acceptance 或归档。
下一目标解锁条件：所有受影响文档和任务真实存在且有 diff，04/08/09/10 门禁通过，且业务实现文件未被修改；满足后只解锁深度流程第 2/2 个 development-and-acceptance 目标。

- 流程档位：深度流程（非 UI 壳）。
- 修改前评分：78/100。评分依据是现有 OpenCodex 数据归一化、总览推荐展示、三个嵌入视图和九个页面动效同时受影响，涉及 Renderer、主进程状态、测试、文档和 macOS 真机验证，但不改数据库 schema、不改外部协议、不新增 UI 壳。
- 基础目标数：2。
- 目标总数：2。
- 当前目标：documentation 1/2。
- UI 决策：本轮不创建或改造 UI 壳；已有页面只在开发目标中做局部交互和动效优化。
- UI 路线：不适用：用户明确说明这是现有页面更新优化，不需要新 UI 壳、Stitch 或外部视觉路线。
- 等待外部输入：无。OpenCodex 本机服务已由主智能体只读读取，需求、设计和验收责任已确认。
- 验收责任：Codex 真机测试；最后目标负责 macOS 真机、页面行为/样式检查、问题回写和归档。
- 页面样式检查：不单独创建 UI 壳或 Design QA 流程；开发与验收阶段必须检查已有页面在桌面、窄窗口和 380×260 悬浮窗中的动画可用性、焦点、无重叠和无溢出。
- 当前状态：文档目标待执行。
- 进入证据：正式 PRD 已存在；设计文档已提交；用户已确认数据口径、推荐删除范围、快速切换方案和动效方案；用户已选择 Codex 真机测试。
- 已完成门禁：需求与设计已确认，但任务拆分、活动文档、测试清单和真机清单尚未按本目标审计完成。
- 待完成门禁：受影响长期文档已更新；全部已确认需求已拆为带编号的任务和微观任务；04、08、09、10 活动文档已写入本任务；真实 diff、文件存在性和非占位内容已检查；所有业务实现文件仍无改动。
- 允许下一动作：只读调查、更新文档、拆分任务、建立测试用例、建立真机清单、运行文档校验器并输出下一目标解锁条件。
- 禁止动作：修改 `src`、`electron`、`scripts` 实现代码；删除或迁移数据库数据；新增依赖；改 OpenCodex 服务端；改 IPC 协议；做 UI 壳施工；启动真实站点写操作；真机验收留待最终目标；发布版本；创建归档。
- 本阶段职责：把已确认需求翻译成可执行的文档和微观任务，确保开发目标无需重新猜测范围。
- 禁止进入的后续阶段：在本目标结束前不得进入 ui、development-and-acceptance、acceptance 或归档。
- 下一目标解锁条件：所有受影响文档和任务真实存在且有 diff，04/08/09/10 门禁通过，且业务实现文件未被修改；满足后只解锁深度流程第 2/2 个 development-and-acceptance 目标。

## 当前可信事实

项目路径为 `/Users/liran/Documents/中转站悬浮窗`，版本 2.6.0，分支为 `codex/geetest-notifications-channel-refresh`。正式 PRD 位于 `liran_docs/requirements/REQ-260904-usage-navigation-motion-PRD.md`，设计文档位于 `docs/superpowers/specs/2026-09-04-usage-navigation-motion-design.md`。本机 `http://localhost:10100/api/logs` 使用主进程管理员令牌访问后返回 `total: 2000`；真实记录包含 `usage.inputTokens`、`cachedInputTokens`、`cacheReadInputTokens`、`cacheCreationInputTokens`、`outputTokens`、`totalTokens`、`durationMs`、`firstOutputMs` 和 `displayMetrics.tokPerSecond.value`。

当前 OpenCodex 代码位于 `src/renderer/shells/usage/opencodex-data.ts` 和 `src/renderer/shells/usage/OpenCodexUsagePage.tsx`，仍固定请求 2000 条；归一化把原始输入直接当作真实输入，缓存写入固定为占位符，日期“今天”按最近 24 小时计算。中转站模式已存在 `src/renderer/shells/usage/cache-rate.ts`，应作为缓存率唯一复用基线。总览推荐展示在 `src/renderer/shells/overview/OverviewPage.tsx` 的 `rate-comparison-band`，内部倍率、充值比例、倍率 Popover、渠道状态和手动关联不能误删。Sub2API、Radar、常用网站分别已有主进程 WebContentsView 和 Renderer 状态，但顶部快速切换和切换响应需要在开发目标中优化。当前自动化基线为 Vitest 56 文件/418 项通过，typecheck、lint、build 已在本会话实际通过。

## 已确认需求，必须一次性拆完

### D1 删除总览渠道推荐

只删除全部站点总览中面向用户的推荐渠道卡片、推荐文案、推荐状态和推荐区域占位。必须保留每站倍率查看 Popover、充值比例、倍率数据加载/缓存、渠道状态页、手动渠道关联、悬浮窗渠道摘要和内部倍率候选/评分。任务中要列出推荐展示的真实组件、调用方、测试和无残留验证方式；如果某个请求只服务推荐展示，标为“候选删除”并要求开发目标调查后决定，不能在文档阶段假定删除。

### D2 修正 OpenCodex Token 与性能字段

真实输入为 `max(inputTokens - cacheReadInputTokens, 0)`，兼容 `cachedInputTokens`；缓存读取使用 `cacheReadInputTokens` 或兼容字段；缓存写入使用 `cacheCreationInputTokens`，缺失显示占位符，真实 0 保留为 0；输出使用 `outputTokens`。总 Token 优先使用日志 `totalTokens`，缺失才安全求和，不能将缓存读取重复计入真实输入。耗时使用 `durationMs`，首字使用 `firstOutputMs`，t/s 优先使用 `displayMetrics.tokPerSecond.value`；异常值不产生 NaN、Infinity 或负数。用户样例必须在单测中固定验证：输入 3003、输出 223、缓存读取 266112、缓存写入 0、总 Token 269338、耗时 11.37s、首字 8.14s、19.61 t/s。

### D3 新增缓存率列

在 OpenCodex 使用记录 Token 右侧新增“缓存率”，复用 `src/renderer/shells/usage/cache-rate.ts` 的计算、格式化和颜色边界：缓存读取 /（真实输入 + 缓存读取 + 缓存写入）×100。覆盖分母为 0、0 值、30/31、60/61、85/86 边界，列设置和表格状态同步。本轮不新增 OpenCodex CSV 导出。

### D4 加载量和日期边界

默认从 `/api/logs` 请求 4000 条，文案同步真实数量；少于 4000 时不伪造。今天为本地当天 00:00:00 到当前时间；近 7 天为含今天的 7 个本地自然日，从当天往前 6 天 00:00 开始；近 30 天同理从当天往前 29 天 00:00 开始；自定义开始日含 00:00，结束日含 23:59:59.999。拆出可注入时钟或等价测试任务，覆盖跨日、跨月、时区、空值和反向范围。

### D5 三个模块的即时快速切换

Sub2API 服务器、Radar、常用网站各自提供顶部横向胶囊切换栏，只切换本模块条目。点击先反馈 active/loading，再由主进程打开或复用目标 WebContentsView；只允许单视图挂载；旧响应不能覆盖新选择；失败优先恢复原页面并可重试；长名称提供 tooltip/ARIA。保留 partition、同源白名单、Cookie 隔离和所有 Electron 安全设置。任务需分别列出 Renderer、主进程服务、状态消息、E2E 和窄窗口验证范围。

### D6 全站高级感动效

覆盖全部站点、API 密钥、使用记录、渠道状态、站点管理、Sub2API 服务器、Radar、常用网站、通知、设置。页面/卡片 160–240ms 淡入和轻微位移；按钮/导航 120–180ms 颜色、阴影和轻微位移/缩放；弹层、通知、刷新、加载和成功/失败状态有一致反馈；`prefers-reduced-motion` 移除位移、缩放和循环动画；不改变布局、窗口尺寸、数据、IPC、安全策略。任务需按页面列出动效对象、状态、样式作用域、可访问性和验证条件，禁止创建新的动画依赖。

## 执行步骤

1. 读取 `AGENTS.md`、`docs/pitfalls/README.md`、相关 Electron/工具链/倍率 pitfalls、正式 PRD、设计文档、`liran_docs/03-索引.md`、`04-开发追踪.md`、`08-测试用例.md`、`09-真机实测.md`、`10-UI壳接入清单.md`、M08/M09/M10/M11/M16/M01/M02 模块文档，以及所有直接调用方和现有测试。只读调查，不改实现代码。
2. 更新正式 PRD 的关联执行文档索引和本任务状态，确认不再存在未决产品选择；保留任务 ID `REQ-260904-usage-navigation-motion`，不要创建第二个 PRD。
3. 按依赖顺序一次性拆出带编号的任务模块：OpenCodex 归一化与日期、缓存率列、渠道推荐展示删除、三个嵌入模块快速切换、全站动效、测试与真机验收、文档/版本收口。每个任务必须写明目标、前置依赖、允许文件、禁止文件、调用方、正常/失败/回归测试、完成条件和建议状态。
4. 把所有微观任务写入本任务对应的施工文档或现有模块文档，不能把任务拆分留到开发目标；任务中明确哪些只改 Renderer、哪些可能涉及主进程，哪些必须复用现有函数。
5. 更新 `liran_docs/04-开发追踪.md`，新增本任务完整依赖顺序、任务编号、当前状态 `待开发`、文档目标完成条件和开发目标解锁条件；不要把本阶段写成已完成或真机通过。
6. 更新 `liran_docs/08-测试用例.md`，加入 OpenCodex 样例字段、缓存率、4000 条、自然日、推荐无残留、快速切换、失败恢复、请求序列保护、全站动效、reduced-motion、桌面/窄窗/悬浮窗的活动用例；只写待执行用例，不伪造通过结果。
7. 更新 `liran_docs/09-真机实测.md`，建立 Codex 后续真机清单，写清 macOS 真实入口、最短步骤、期望结果、证据路径约定、失败回写栏和 Windows 交叉构建边界；当前状态必须是 `待实测`，不能写成通过。
8. 仅当当前 UI 壳清单已有对应有效接线事实时，定向更新 `liran_docs/10-UI壳接入清单.md` 的当前有效接线；不要创建新的 UI 壳清单，不要把全站动效误写成新 UI 壳路线。
9. 更新受影响的长期模块文档和 API/数据字典事实：M08 使用记录、M09 渠道状态、M10 总览、M11 调度缓存、M01/M02 窗口与生命周期；只补充本轮长期有效口径，不复制施工流水。
10. 运行 PRD/文档结构校验。优先使用 `python3 /Users/liran/Documents/codex 相关项目/plan-docs/scripts/check_adaptive_flow_docs.py --prd-file liran_docs/requirements/REQ-260904-usage-navigation-motion-PRD.md --prd-status formal`；如该仓库没有项目内脚本，明确记录使用外部技能仓库脚本，不能伪造“项目脚本通过”。检查所有目标文档真实存在、正文非占位、任务 ID 一致、diff 只涉及文档。
11. 最终检查 `git diff --name-only`，确认没有 `src/`、`electron/`、`scripts/` 业务实现文件变更，没有新依赖，没有删除；检查任务清单覆盖 D1–D6 每项。

## 业务与安全边界

只允许更新文档、测试计划、真机清单和任务追踪。不得访问或输出管理员令牌内容，不得把真实日志中的账号、Cookie、Token 或完整敏感值写入文档。不得通过真实站点执行写操作。不得为了补齐任务而虚构接口、字段、页面、截图、真机结果或发布资产。不得把当前 2000 条服务返回写成已经有 4000 条；4000 是下一阶段请求上限。不得把“用户选择 Codex 真机测试”写成已经完成真机验收。

## UI 专项要求

本目标不创建新的 UI 壳、Stitch 项目、外部视觉提示词或独立 Design QA。文档必须把现有页面优化与新 UI 壳明确区分：开发目标只在已有页面/组件和局部 CSS 上实现交互动画与快速切换，不改变信息架构、窗口尺寸、公共组件规格或嵌入网页安全边界。任务拆分仍需列出 hover、press、focus-visible、loading、error、empty、modal、selected 和 reduced-motion 状态，供开发与验收阶段使用。

## 验证要求

本目标只验证文档和产物，不运行业务实现测试作为通过依据。必须运行正式 PRD 校验、文档结构检查、`git diff --check`、`git diff --name-only` 和任务覆盖审计；若需要查看代码，只能使用只读命令。开发目标再运行 Vitest、typecheck、lint、format、build、Electron E2E 和适用的 macOS 真机/页面检查。本阶段不得把计划中的验证写成已通过。

## 完成后输出

完成后只输出：本阶段实际读取的关键文件；逐文件新增/修改内容；任务模块和微观任务编号及依赖顺序；04/08/09/10 和长期模块文档状态；校验命令与真实输出；业务实现文件未改动证据；当前状态是否为 `文档阶段完成，已解锁 development-and-acceptance 2/2`。不要输出下一条开发命令，除非上述门禁全部通过；如果任一门禁失败，继续当前文档目标并明确失败原因。

## 档位执行门禁

文档阶段必须依次完成：受影响文档真实 diff；全部微观任务写回；开发追踪已更新；验收准备已完成；下一目标解锁。编码解锁前不得编辑业务实现文件；如果任何阶段证据缺失，保持 documentation 1/2，不得进入开发。

## 收口条件

文档阶段只有在以下条件同时满足时才算完成：正式 PRD 和设计文档存在；D1–D6 全部拆为可执行微观任务；04、08、09、10 已真实更新且任务 ID 一致；受影响长期文档已补充当前事实；PRD/文档校验通过或已如实记录工具缺失；`git diff` 不包含业务实现文件；没有新增依赖、敏感数据或虚构证据。任何条件不满足都保持 documentation 1/2，不得解锁开发目标，不得称整体任务完成，不得归档。
