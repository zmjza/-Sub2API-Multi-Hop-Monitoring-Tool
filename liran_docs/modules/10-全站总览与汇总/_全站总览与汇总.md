# 全站总览与汇总

## 2026-07-23 规划增量

新增叶子文档：[全部站点当前 Key 口径汇总](当前Key口径汇总.md)，覆盖 RQ-24。新口径取代“所有有效站点账号整体快照直接求和”作为顶部业务指标的旧需求：顶部和每卡必须先解析每站当前 Key，再按该 Key 查询和聚合。旧版本证据仅为历史完成，不证明本轮已实现。

> 2026-07-14 当前证据：有效快照、总余额、今日 Token/实际消费、覆盖数、当前站、状态和更新时间已接入；领域测试覆盖过期缓存不计汇总和部分失败组合，获授权站点只读聚合、macOS 打包应用页面与几何检查已通过。

上级：[[03-索引]]
下级：跨站汇总、覆盖率、排序与导航
依赖：[[11-调度缓存与退避]]、[[07-API-Key与倍率]]、[[08-今日统计与使用记录]]、[[09-渠道状态]]

## 2026-07-21 1.3.9 倍率标题移除

- 倍率区域不再渲染主标题或副标题，刷新周期与刷新按钮保持右侧布局。
- 四平台卡片、官网 SVG、排序、横滑和倍率业务逻辑不变。

## 2026-07-21 1.3.8 倍率标题与品牌资源

- 倍率区域移除说明副标题，标题行只保留“倍率对比”、刷新周期和刷新按钮。
- 四平台图标改为官网 SVG 并随 Renderer 离线打包，继续占用固定 40×40 槽位；Antigravity 仍复用 Gemini 图标。
- 卡片尺寸、单行横滑、平台顺序、推荐内容和所有业务数据流保持不变。

## 2026-07-21 1.3.7 倍率对比实例级还原

- 倍率区按 Stitch HTML 还原为 OpenAI、Claude、Gemini、Grok 四张 32px 圆角单行卡片，使用 40×40 本地品牌图标、平台主题色和统一推荐/空态结构。
- 横向容器保留 `overflow-x: auto`、焦点语义和原生横滑，只在组件局部隐藏 WebKit/Firefox 滚动条；1600px 全显，720px 可滑且不换行。
- Antigravity 通过直接字段、文本和结构化关系三条证据链归入 Gemini，与 Gemini 共用候选池；相似子字符串不识别。
- 真实倍率、充值比例、渠道状态、五分钟稳定门槛、缓存与评分逻辑全部复用，不改变其他页面或公共协议。

## 2026-07-21 1.3.6 卡片底部基线

- 站点 footer 固定为充值比例、查看倍率、查看渠道状态三列单行网格；最窄主窗口和长内容下仍不换行、不溢出。
- 当前渠道摘要改为固定 102px 槽位并承担正文后的自动剩余空间，成功、加载、无匹配和错误状态外框等高且不裁切说明文字。
- E2E 以五张临时站点卡片验证 1600px 四列首行、720px 单列、同一行摘要/footer 顶边误差 ≤1px 和跨行底部偏移一致。
- 本次只调整 `overview.css` 布局，不改变倍率推荐、渠道关系或 Key 数据流。

## 2026-07-20 1.3.5 五分钟硬门槛与单推荐

- 只有当前渠道状态为 normal，且最近三分钟至少一条记录、窗口内全部记录 normal、无未来或非法时间戳的唯一匹配候选才能参与倍率推荐；无渠道、歧义、异常、过期和请求失败全部排除，不再给中性分。
- 合格候选按同平台 min-max 计算价格分：`10 × (最高有效倍率 - 当前有效倍率) ÷ (最高有效倍率 - 最低有效倍率)`；同价全部 10，稳定分固定 10，综合分为价格 60% + 稳定 40%。
- 每个平台只显示第一名，固定 OpenAI、Claude、Gemini、Grok 四列；实际平台按分组名称、渠道模型、说明、结构化关系、provider/platform 依次识别，错标分组不会仅按 `group.platform` 分类。
- 四列使用浅绿、浅橙、浅蓝、黑灰主题并保持单行；第五个平台起横向滚动。自动刷新支持 1/3/5/10 分钟，默认 5 分钟，手动和自动刷新共用 single-flight。
- 站点卡片等高，倍率徽标使用 `BadgePercent`，当前渠道摘要在统一 footer 上方，充值比例、查看倍率和查看渠道状态固定在底部。

## 2026-07-20 1.3.4 倍率卡片优化

历史 1.3.4 曾在倍率候选内显示渠道状态并允许无渠道候选参与；该行为已由 1.3.5 覆盖。当前渠道摘要只显示在站点卡片，倍率区只显示每平台一个合格推荐。总览内容变高后页面切换必须操作 `.content-scroll` 真实滚动容器，不能操作外层 `.app-content`。

## 职责

以有效快照构建全站汇总和站点比较。覆盖 RQ-09、RQ-11。

## 汇总规则

- 总余额、今日 Token、今日实际消费仅累加有效快照。
- 展示 `已统计 X/Y`，X 为纳入汇总的站点数，Y 为正式站点数。
- 查询中但有有效缓存的站点可以计入并标记；过期缓存不计入。
- 站点健康按认证、核心查询、渠道状态和缓存新鲜度组合，不用单一颜色隐藏原因。

## 交互

- 支持异常、低余额、Token、消费排序；排序稳定。
- 点击站点进入详情并同步设置当前站。
- 零站点展示添加入口；部分失败展示覆盖率和安全错误摘要。
- 页面不包含悬浮窗预览，也不显示固定“正在获取余额”等假状态；刷新必须由真实请求状态驱动，成功后更新状态和时间。
- 今日 Token 使用统一 K/M 格式，缺少可信倍率时显示“倍率不可用”。
- 总览采用高密度工作台结构：汇总指标为紧凑横向指标带，站点信息使用高不透明响应式卡片网格。
- 玻璃材质仅用于应用外壳、侧边导航和顶部工具栏；汇总数值、站点表格、错误详情与排序控件使用高不透明浅色表面。

## 验收

- 有效/过期/部分/认证失败组合、货币精度、排序、零站点和点击导航均有测试。

## Stitch 产物补充

- 当前视觉事实来源为 `liran_docs/stitch-artifacts/10340103531009759971/01-overview/screen.html`。
- 产物是总余额、今日消耗 Token、今日消耗、站点状态计数和站点表格的第一视觉事实来源；原 HTML 中的悬浮窗规格实例只作为独立悬浮窗参考，不进入当前总览页面。表格列包括站点名称、默认 Key、倍率、余额/消耗、状态和最后更新时间。

## 任务范围

TASK-05-06、TASK-07-07、TASK-10-07。

## 2026-07-18 外发版合并增量（内部测试通过；真机待实测）

本轮 Radar 与总览在 macOS 打包 E2E 中保持独立；主导航、刷新、公开数据错误边界和总览布局均有当前轮截图/断言证据。

- 总览与 Radar 必须是相互隔离的主导航页面；Radar 的网络失败、空数据或公开数据延迟不能改变总览快照、站点刷新和缓存汇总。
- 渠道关系排序可以影响渠道状态展示顺序，但不得改变总览有效缓存、覆盖率和站点健康计算。
- 主导航新增 Radar 后，现有总览入口、站点切换、刷新状态、窗口和悬浮窗路由必须保持可达并有回归测试。

## 2026-07-18 1.2.0 优化（macOS 真机通过）

- 站点状态由表格改为响应式卡片网格，保留站点状态、Key、倍率、余额/额度、今日统计、更新时间和刷新行为。
- 站点卡片支持双击编辑备注；备注使用 `site:<siteId>:note` 本地设置存储，限制 500 字，不进入凭据、日志或 CSV。
- 手动 Key 有有效 quota 时显示 `max(0, quota - quota_used)`；quota 无效时回退用户总余额；自动选择始终显示用户余额。

## 2026-07-19 1.2.1 全站刷新与每站点 Key 状态

- 总览刷新调用主进程全站调度器，当前站点优先、按站点数量限制并发、同批 Promise 去重并隔离单站失败。
- 每站点 Key context、手动偏好和筛选派生状态按 site ID 隔离；切换当前站点只改变描边，不改变其他卡片模式。
- Key 摘要先从安全缓存展示并在 `/keys` 返回后立即发布，慢分组/倍率/统计随后补齐；完整 Key 永不进入缓存或 Renderer。

> 1.4.3 修订：上述“完整 Key 永不进入 Renderer”仍适用于总览、Key 上下文和悬浮窗；仅 API 密钥管理页为用户明确要求，允许完整 Key 在当前页面会话内存中短暂展示，且不进入缓存、日志或总览统计链路。

- 旧版本手动偏好在首轮 Key 缓存尚未建立时保留，只有取得非空清单并确认 Key 失效才回退自动。

## 2026-07-19 1.3.0 跨站倍率比较

- 指标区和站点状态之间新增紧凑对比带，按动态平台显示折算后最低站点与分组，平局全部保留。
- 每张站点卡片底部固定左侧充值比例、右侧“查看倍率”，两者点击均不触发选站或备注编辑。

## 2026-07-19 1.3.1 综合排行与快捷渠道状态

- 倍率带以 10 分制展示综合分、价格分和稳定分；前四平台固定为 OpenAI、Claude、Gemini、Grok，第五平台起继续向右排列，容器不换行。
- 四个平台卡片分别使用浅绿、浅橙、浅蓝和黑灰局部主题，22px 圆角、柔和边框和轻阴影；候选列表内部独立滚动，长站点/分组信息截断。
- 站点操作区新增“查看渠道状态”，与“查看倍率”和充值比例并列且隔离点击事件；弹窗按需展示整站全部渠道。
- 本条为历史 1.3.1 行为，已被 1.3.5 覆盖：当前 Key 摘要自动加载；无渠道状态不参与倍率评分。
- 五平台几何、颜色、横向滚动和整站渠道弹窗均由打包应用 E2E 断言并完成视觉检查。
- 倍率刷新拥有独立 loading/error/cache 状态，不阻塞 Key 下拉、余额或全站刷新。
- 新增/删除站点时按稳定 site ID 集合重新加载倍率上下文；余额和更新时间变化不重复触发倍率全刷。
