# 窗口、悬浮窗与托盘

> 2026-07-14 当前证据：主窗口无框、默认工作区 60%×90%、Renderer 响应式铺满、固定侧栏、悬浮窗 380×260、四角停靠持久化、跨窗口站点同步、非置顶桌面常驻和主窗/悬浮窗切换已通过开发与 macOS 打包应用验证。物理多显示器拔插因当前环境只有单显示器而不适用。

上级：[[03-索引]]
下级：主面板窗口、悬浮窗定位、托盘菜单、动态标题
依赖：[[01-应用外壳与生命周期]]、[[11-调度缓存与退避]]

## 职责

实现主面板与悬浮窗的稳定窗口行为、站点切换、动态标题、位置恢复和托盘操作。覆盖 RQ-03、RQ-11、RQ-12。

## 叶子能力

窗口行为按定位、站点交互、动态标题和托盘四个叶子职责拆分。

### 悬浮窗定位

- 默认主显示器右上角，可选择左上、右上、左下、右下四角并持久化。
- 按当前主显示器 workArea 和 12px 边距重新计算位置；显示器或工作区变化时回退主显示器可见区域。
- 固定响应式约束，查询动画、长站点名和错误状态不得改变外框尺寸。
- Stitch HTML 已确认悬浮窗逻辑规格为 `380 x 260`，只实现固定浅色外观；`1440 x 1024` 仅为主界面视觉参考，Renderer 必须响应式铺满 BrowserWindow。主窗口初始占工作区宽度 60%、高度 90%，居中且可从边缘和四角缩放。
- 悬浮窗可使用克制玻璃材质，但数值和状态必须位于可读表面；减少透明效果时降级为高不透明实色面板，高对比度时强化边框和焦点。
- 悬浮窗保持已显示但 `alwaysOnTop=false`；切换浏览器、其他应用或台前调度组时不得自动隐藏，前台应用必须能覆盖它。macOS 跨 Space 可见但 `visibleOnFullScreen=false`；Windows 保持普通窗口层。

### 站点切换与点击

- 左右按钮循环切站；无站点或单站时相应禁用。
- 切站立即展示缓存并提交高优先级刷新。
- 点击非控制区打开当前站详情；图标按钮使用工具提示。

### 动态标题

- 余额 >=2、<2、未知三态使用已确认文案。
- 余额来源必须对应当前选中站点，不能使用跨站总余额。

### 托盘

- 打开主面板、显示/隐藏悬浮窗、开机启动开关、退出。
- 菜单状态与真实窗口/设置同步。
- 主窗口最小化在悬浮窗启用时显示悬浮窗，禁用时仅隐藏到托盘；关闭按钮直接退出。

## 验收

- 四角停靠、持久化、长文本、切站、失焦保持、前台应用覆盖、台前调度跨 Space、固定浅色和关闭退出需有自动化与 macOS 打包应用证据；减少透明、高对比度和物理多显示器在具备对应系统/硬件条件时补充，不冒充已通过。

## Stitch 产物补充

- 当前视觉事实来源为 `liran_docs/stitch-artifacts/10340103531009759971/05-floating/screen.html`。
- 产物包含正常、延迟较高、连接中等悬浮窗状态实例，以及左右切站、滚动标题、余额、今日 Token、今日消费和更新时间。
- 接收前必须盘点每个实例的固定尺寸、标题滚动/截断规则、箭头按钮、状态标签和底部指标占位。

## 任务范围

TASK-06-01 至 TASK-06-05、TASK-13-02。
