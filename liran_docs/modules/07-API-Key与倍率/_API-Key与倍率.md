# API Key 与倍率

> 2026-07-14 当前证据：今日请求最多自动选择、全零历史回退、手动持久化、失效回退、脱敏摘要和真实倍率优先级已接线并通过自动化、两站只读与 macOS 打包应用页面检查；未显示或保存完整 Key。

上级：[[03-索引]]
下级：Key 摘要、自动选择、手动选择、倍率合并
依赖：[[06-API适配器与能力探测]]、[[08-今日统计与使用记录]]

## 职责

安全管理 Key 摘要并决定默认 Key 与最终倍率。覆盖 RQ-04、RQ-05。

## 规则

- 仅保存 Key ID、名称、脱敏标识、状态、groupId；不保存完整 Key。
- 自动选择按本机今日请求数降序；平局使用上次选择，其次稳定 ID 排序。
- 手动选择永久保存，失效时提示并临时回退，不能静默改写用户设置为 auto。
- 实际倍率优先级：`GET /groups/rates` 的当前用户分组倍率，其次 Key 内嵌 `group.rate_multiplier`，再次 `GET /groups/available` 的分组默认倍率；均缺失时显示“倍率不可用”，不得伪造。

## 状态

loading、auto-selected、manual-selected、manual-invalid-fallback、empty、rate-unsupported。

## 验收

- 多 Key、零请求、平局、跨日、手动持久化、Key 禁用/删除、倍率覆盖和缺失均有单测。

## 任务范围

## 当前实现证据

已实现脱敏 Key 摘要、按今日请求数自动选择、历史回退、手动选择持久化、失效临时回退、三层倍率优先级和悬浮窗/总览接线。获授权站点只读验证确认可获得可信倍率；验证过程不修改远端 Key，文档与日志不得保存完整 Key。

TASK-05-01 至 TASK-05-04、TASK-10-02。
