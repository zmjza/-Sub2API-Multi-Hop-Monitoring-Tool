# 测试、CI、构建与 Gitee 分发

## 2026-07-24 API 密钥与监控联动发布收口

新增叶子文档：[API 密钥与监控联动回归发布](API密钥与监控联动回归发布.md)，覆盖 RQ-27。`1.4.0` 已完成 34 文件/210 项 Vitest、开发与 macOS 打包应用 E2E 6/6、15 张页面证据、DMG/NSIS/asar/架构及 SHA-256 审计。Windows 未执行真机。

`1.4.1` 修复 macOS bundle 签名不完整：electron-builder 使用 `identity: "-"` 完成整包 ad-hoc 签名，DMG 和 `/Applications` 安装副本均通过 `codesign --verify --deep --strict`，LaunchServices 启动和最终安装副本 E2E 6/6。Windows 同步重建为 `1.4.1`，仍仅为交叉构建；macOS 仍未使用 Apple Developer ID 签名且未公证。

> 2026-07-14 当前证据：18 个文件 71 项 Vitest、6 项开发 Electron E2E、4 项 macOS 打包应用 E2E、lint、typecheck、Renderer/Electron 构建、两个获授权站点只读复测和五个页面稳定截图已通过。根目录 macOS ARM64 DMG 与 Windows x64 NSIS 已重新生成并审计；Windows 未真机，macOS 产物未签名、未公证。

上级：[[03-索引]]
下级：自动化测试、macOS 验收、Windows CI、源码分发
依赖：全部实现模块

## 2026-07-21 1.3.9 发布收口

- 版本同步为 1.3.9；构建与 Electron E2E 通过，倍率标题移除回归已覆盖。
- DMG 校验有效，macOS 为 Mach-O ARM64；Windows 为 NSIS PE32，解包主程序为 PE32+ x86-64，内部版本均为 1.3.9。
- `release/` 仅保留 1.3.9 四个分发文件和当前构建元数据；Windows 未执行真机。

## 2026-07-21 1.3.8 发布收口

- 版本同步为 1.3.8；完整 Vitest 28 文件/171 项、生产构建和最终 macOS 打包应用 Electron E2E 6/6 通过。
- macOS 页面检查确认副标题删除，四个官网 SVG 在 1600px/720px 下清晰且布局稳定；13 张证据位于 `real-test-evidence/macos-1.3.8/`。
- DMG 校验有效，macOS 主程序为 Mach-O ARM64；Windows NSIS 为 PE32，解包主程序为 PE32+ x86-64，两端内部版本为 1.3.8。Windows 未执行真机。
- `release/` 最终仅保留 1.3.8 四个分发文件和当前构建元数据；SHA-256 记录于 `CHANGELOG.md` 和 README。

## 2026-07-21 1.3.7 发布收口

- 版本同步为 1.3.7；倍率规则 26/26、完整 Vitest 28 文件/171 项、开发和最终 macOS 打包应用 Electron E2E 6/6 通过。
- macOS 页面检查覆盖 1600px 与 720px，确认四卡固定单行、图标清晰、品牌色和状态基线一致、无可见滚动条、无重叠或第二行；共 13 张证据。
- DMG 校验有效，主程序为 Mach-O ARM64，Info.plist/asar 为 1.3.7；Windows NSIS 为 PE32，解包主程序为 PE32+ x86-64。macOS 未公证，Windows 未做真机。
- `release/` 仅保留 1.3.7 DMG、NSIS、两个 blockmap 和当前构建元数据；四个 SHA-256 记录于 `CHANGELOG.md` 和 README。

## 2026-07-21 1.3.6 发布收口

- 版本同步为 1.3.6；Prettier、ESLint、TypeScript、Vitest 28 文件/168 项、生产构建、开发和 macOS 打包应用 Electron E2E 6/6 通过。
- macOS 页面检查覆盖五卡四列、720px 单列、长标题、五种摘要状态和 footer 单行；11 张证据位于 `real-test-evidence/macos-1.3.6/`。
- DMG 校验有效，主程序为 Mach-O ARM64，Info.plist/asar 为 1.3.6；Windows NSIS 为 PE32，解包主程序为 PE32+ x86-64，两端入口完整。
- `release/` 仅保留 1.3.6 DMG、NSIS、两个 blockmap 和 `builder-debug.yml`；SHA-256 记录于 `CHANGELOG.md` 和 README。Windows 未执行真机。

## 2026-07-21 1.3.5 发布收口

- 版本已同步为 `1.3.5`；`release/` 仅保留当前 DMG、NSIS、两个 blockmap 和 `builder-debug.yml`，解包验证目录已移到项目旁可恢复归档。
- 三个真实站点只读接口重新验证成功；macOS ARM64 打包应用 E2E 6/6、页面样式检查和 DMG `hdiutil verify` 通过。
- macOS 主程序为 Mach-O ARM64，Info.plist 与 asar 版本为 `1.3.5`；Windows NSIS 为 PE32，引导出的主程序为 PE32+ x86-64，两端 asar 入口完整。
- Windows 边界不变：仅交叉构建和结构验证，不宣称 Windows 真机通过。四个 SHA-256 记录于根目录 `CHANGELOG.md`。

## 2026-07-20 1.3.4 发布收口

- 版本同步为 `1.3.4`，生成 macOS ARM64 DMG/blockmap 与 Windows x64 NSIS/blockmap；`release/` 仅保留当前分发文件和 `builder-debug.yml`。
- macOS DMG 通过 `hdiutil verify`，主程序为 Mach-O ARM64；Windows 安装器为 NSIS PE32，解包主程序为 PE32+ x86-64；两端 asar 版本均为 `1.3.4` 且包含 Renderer/main/preload。
- macOS 真机与打包应用 E2E 6/6 通过；Windows 仅交叉构建和结构验证，不宣称 Windows 真机通过。
- 产物 SHA-256 详见根目录 `CHANGELOG.md` 的 1.3.4 表格。

## 职责

建立内部证据、跨平台构建和源码交付。覆盖 RQ-18、RQ-19。

## 测试层次

- Vitest：纯逻辑、仓储、适配器、调度和安全单元测试。
- mock sub2api：认证、能力、错误和多站集成测试。
- Playwright Electron：录入、切站、筛选、加载、失败恢复、无框窗口、主窗/悬浮窗切换、四角停靠、设置持久化和主要页面流程。
- macOS 真机：悬浮窗、托盘、通知、Keychain、开机启动、多显示器和主要页面。
- Windows CI：构建、启动、测试；不得标 Windows 真机通过。
- 视觉验证：固定浅色、减少透明、高对比度和不同窗口尺寸，并检查不存在其他主题入口；`1440 x 1024` 仅作视觉参考，Renderer 必须铺满 BrowserWindow，左侧菜单固定。macOS 检查实际窗口与页面，Windows 只记录自动化/交叉构建证据。
- Stitch 产物验证范围：5 个业务 Screen 的 HTML/截图来源登记、控件规格盘点、实例级尺寸保真、无敏感静态数据、Design System 与项目固定浅色需求冲突检查。
- 本 Stitch 路线的视觉证据采用 HTML 事实、实例级规格、构建、入口、自动化截图和 macOS 打包应用人工检查，不执行另行的设计评审流程。

## 分发

- Gitee 源码包含 README、LICENSE、.gitignore、锁文件和双平台一键脚本。
- 提供 macOS ARM64 DMG 与 Windows x64 NSIS 构建；未签名/未公证事实和本机构建方式写清。
- 发布前生成校验值；是否发布预编译包由后续决定。

## 当前发布规则

- 每次功能优化、Bug 修复或行为调整都必须递增 SemVer，并重新生成 macOS ARM64 DMG 与 Windows x64 NSIS 安装包。
- 安装包文件名必须包含当前版本号，更新说明必须同步记录本次变更和 SHA-256。
- `release/` 只保留最新版本的安装包、blockmap 和当前构建元数据；生成新版本前清理旧产物。
- 指定恢复历史版本时必须检出 Gitee 对应历史提交的完整代码树，再重新验证和打包；只改版本号不构成版本恢复。
- Windows 只记录交叉构建和 CI 证据，不冒充真机验收；macOS 真机状态单独记录。

## 验收

- lint、typecheck、Vitest、Electron E2E、Renderer/Electron build、安全检查和最终 diff 均有当前轮次真实输出。
- macOS 打包应用需逐页检查全部站点、使用记录、渠道状态、站点管理与设置、独立悬浮窗；Windows 不得标记真机通过。

## 任务范围

TASK-12-01 至 TASK-12-11、TASK-13-01 至 TASK-13-05、TASK-14-01 至 TASK-14-04、TASK-15-01 至 TASK-15-04。

## 2026-07-18 外发版合并增量（内部门禁通过；Windows 构建待后续阶段）

本轮已完成 macOS ARM64 内部目录包、macOS 打包 E2E 6/6、Windows x64 NSIS 交叉构建及 PE/asar 结构审计。Windows 安装器为 PE32 NSIS，解包主程序为 PE32+ x86-64；未执行 Windows 真机，也未生成本轮正式发布包。

- 本次功能调整已递增为 `1.1.0`；正式产物和校验值已生成并记录在 `CHANGELOG.md`。
- `release/` 已清理并仅保留当前版本的 macOS ARM64 DMG、Windows x64 NSIS、blockmap 和构建元数据。
- `dist:mac` 必须保持 `--arm64`，不得带入朋友版 `--universal`；`dist:win` 必须保持 `--x64`。
- Windows 只做构建、NSIS/PE 结构、可运行自动化和 CI 证据；macOS 才进入 Codex 真机验收和页面样式检查。
- 产物、签名、公证、真机、链接、敏感扫描和最终 diff 必须在第二次 `/goal` 中按顺序验证，失败项回写后再复测。

对应规划任务：MERGE-01、MERGE-08、MERGE-10。

## 2026-07-19 1.2.1 发布结果

- Prettier、ESLint、TypeScript、Vitest 25 文件/120 项、Electron E2E 6/6 和生产构建通过。
- macOS ARM64 DMG 通过 `hdiutil verify`，打包应用完成真实站点与页面样式验收；产物未签名、未公证。
- Windows x64 NSIS 安装器为 PE32，解包主程序为 PE32+ x86-64，asar 内版本为 `1.2.1`；仅为交叉构建和结构验证。
- `release/` 只保留四个 `1.2.1` 分发文件和当前 `builder-debug.yml`；旧版和解包工作目录移至项目旁可恢复目录，没有删除用户产物。
- SHA-256 以 `CHANGELOG.md` 和 README 当前 `1.2.1` 表格为准。

## 2026-07-19 1.3.0 发布结果

- Prettier、ESLint、TypeScript、Vitest 26 文件/136 项、生产构建和 Electron E2E 6/6 通过。
- 三个授权站点只读倍率接口复测通过；macOS ARM64 打包应用完成倍率换算、跨站对比、Popover 与页面样式真机验收，最终 DMG 内应用 E2E 6/6 通过。
- macOS ARM64 DMG 通过 `hdiutil verify`；Windows x64 NSIS 安装器为 PE32，解包主程序为 PE32+ x86-64，两端 asar 版本均为 `1.3.0` 且入口结构完整。
- Windows 只完成交叉构建与结构验证，未执行 Windows 真机验收。
- `release/` 只保留四个 `1.3.0` 分发文件和当前 `builder-debug.yml`；SHA-256 以 `CHANGELOG.md` 和 README 当前 `1.3.0` 表格为准。

## 2026-07-19 1.3.1 发布结果

- Prettier、ESLint、TypeScript、Vitest 27 文件/146 项、生产构建、开发 Electron E2E 6/6 和 macOS 打包应用 E2E 6/6 通过。
- 三个授权站点只读验证登录、核心数据、使用记录、倍率、渠道列表和渠道详情均成功；敏感凭据仅运行时注入。
- macOS ARM64 DMG 通过 `hdiutil verify`，主程序为 Mach-O ARM64，Info.plist 与 asar 版本均为 `1.3.1`。
- Windows x64 NSIS 安装器为 PE32，解包主程序为 PE32+ x86-64，asar 版本/Renderer/main/preload 入口完整；未执行 Windows 真机。
- `release/` 仅保留四个 `1.3.1` 分发文件和当前构建元数据；旧 `1.3.0` 产物及本轮解包目录移动到项目旁可恢复归档，没有删除。
- SHA-256 以 `CHANGELOG.md` 和 README 的 `1.3.1` 表格为准。

## 2026-07-20 1.3.2 发布结果

- Prettier、ESLint、TypeScript、Vitest 27 文件/147 项、生产构建、开发 Electron E2E 6/6 和 macOS 打包应用 E2E 6/6 通过。
- macOS ARM64 DMG 通过 `hdiutil verify`，主程序为 Mach-O ARM64，Info.plist 与 asar 版本均为 `1.3.2`。
- Windows x64 NSIS 安装器为 PE32，解包主程序为 PE32+ x86-64，asar 版本与 Renderer/main/preload 入口完整；未执行 Windows 真机。
- `release/` 仅保留四个 `1.3.2` 分发文件和当前构建元数据；旧产物和解包目录移动到项目旁可恢复归档，没有删除。
- SHA-256 以 `CHANGELOG.md` 和 README 的 `1.3.2` 表格为准。

## 2026-07-20 1.3.3 发布结果

- Prettier、ESLint、TypeScript、Vitest 27 文件/147 项、生产构建、开发 Electron E2E 6/6 和 macOS 打包应用 E2E 6/6 通过。
- macOS ARM64 DMG 通过 `hdiutil verify`，主程序为 Mach-O ARM64，Info.plist 与 asar 版本均为 `1.3.3`。
- Windows x64 NSIS 安装器为 PE32，解包主程序为 PE32+ x86-64，asar 版本与 Renderer/main/preload 入口完整；未执行 Windows 真机。
- `release/` 仅保留四个 `1.3.3` 分发文件和当前构建元数据；旧产物和解包目录移动到项目旁可恢复归档，没有删除。
- SHA-256 以 `CHANGELOG.md` 和 README 的 `1.3.3` 表格为准。
