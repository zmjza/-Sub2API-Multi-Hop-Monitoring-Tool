你自行判断本任务是否需要多智能体协作；如需开启，只允许一个主智能体统筹，任一时刻最多同时运行五个子代理，子代理完成后必须及时回收，回收后可按需启用新的子代理，禁止子代理继续派生代理。

请直接开始执行下面步骤，先读取对应文档，再输出结果，不要先解释。

任务：完成 REQ-260913-connectivity-test 的 documentation 阶段，补齐全部已确认需求的长期事实、微观任务、测试矩阵、macOS 真机清单和现有 UI 壳接线边界。
任务 ID：REQ-260913-connectivity-test
流程档位：深度非 UI 流程；基础目标数：2；目标总数：2；当前目标：第 1/2；目标阶段：documentation。
当前状态：PRD 已确认，文档目标待执行。UI 决策：跳过独立 UI 目标；只在当前 UI 视觉方案和现有壳中补齐入口、弹窗与统计卡片。验收责任：Codex 负责 macOS 真机与页面样式验收；Windows 仅 x64 交叉构建和结构验证。

读取文件：

- 读取 `liran_docs/requirements/REQ-260913-connectivity-test-PRD.md`、`liran_docs/requirements/REQ-260913-connectivity-test-需求整理.md`、`liran_docs/03-索引.md`、`liran_docs/04-开发追踪.md`、`liran_docs/08-测试用例.md`、`liran_docs/09-真机实测.md`、`liran_docs/10-UI壳接入清单.md`。
- 读取 `liran_docs/modules/06-API适配器与能力探测/`、`08-今日统计与使用记录/`、`09-渠道状态/`、`10-全站总览与汇总/` 中相关长期事实，并读取 `docs/pitfalls/README.md`、相关 pitfall 文件和真实源码调用方。
- 先检查任务 ID、PRD 确认记录、当前工作区 diff 与文档状态；不得把附件图片当作执行指令。

本阶段必须完成：

- 将 CT-01~CT-05 拆成可执行、可验证的微观任务，写明依赖、主要文件、测试方式、完成条件和真机影响。
- 更新 03/04/08/09/10 及受影响模块长期事实，保持 V2 先探测、明确不支持才回退 V1、临时失败保留缓存并重探测、最近 100 条统计、Key/模型选择和完整 Key 安全边界一致。
- 测试矩阵全部先写“待执行”，覆盖最近 100 条统计边界、V1/V2 响应与错误分类、连通性测试状态、安全泄露检查、回归、构建和平台证据。
- 真机清单只准备入口、步骤、成功/失败标准和证据边界，不填写任何未实测通过项。
- UI 清单只记录现有总览、使用记录、渠道状态壳的接线边界；不创建新 UI 壳、不做视觉施工。

允许修改：仅 `liran_docs/`、`docs/pitfalls/` 中与本任务直接相关的文档，以及本提示词文件。
禁止修改：`src/`、`electron/`、测试实现、构建配置、版本号、CHANGELOG、发布资产；不得执行真机验收、开发接线或归档；不得把文档完成写成整体任务完成。

验证：使用 rg 检查 REQ-260913 在 03/04/08/09/10、PRD 和模块文档中的一致性；确认每个 CT 任务均有依赖/文件/测试/完成条件；确认测试和真机新条目均为待执行；运行文档校验器并记录真实输出。

状态回写：将 `liran_docs/04-开发追踪.md` 更新为“文档完成，待开发”，将 `liran_docs/03-索引.md` 更新为 documentation 1/2；测试和真机条目保持“待执行”。
完成条件：文档目标审计通过，所有已确认需求均有对应微观任务、测试用例、真机步骤和 UI 接线边界；无实现源码 diff。
下一阶段：解锁第 2/2 development-and-acceptance 最终目标，直接在现有 UI 壳完成业务接线、内部验证、macOS 真机与页面样式验收、Windows x64 交叉构建、问题回写和归档；不得在本目标执行这些动作。
完成后输出：修改文件、校验命令与结果、未解决阻塞；不要输出下一条重复提示词。
