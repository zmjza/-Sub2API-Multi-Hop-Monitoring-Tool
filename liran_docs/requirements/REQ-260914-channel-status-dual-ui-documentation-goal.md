你自行判断本任务是否需要多智能体协作；如需开启，只允许一个主智能体统筹，任一时刻最多同时运行五个子代理，子代理完成后必须及时回收，回收后可按需启用新的子代理，禁止子代理继续派生代理。

请直接执行 REQ-260914-channel-status-dual-ui 的 documentation 阶段，完成深度非 UI 流程第 1/2 个目标。本任务已合并原 REQ-260915-connectivity-test-models，渠道状态、测试连通性和使用记录统计必须统一规划。

当前阶段：documentation；当前目标：第 1/2 个目标；流程档位：深度非 UI；目标总数：2。

任务 ID：REQ-260914-channel-status-dual-ui

读取文件：正式 PRD v1.1、合并需求整理、任务拆分、03/04/08/09/10、模块 06/09/10、使用记录模块、测试连通性源码和相关 docs/pitfalls。

允许修改：仅 liran_docs/ 与 docs/pitfalls/ 中本任务相关文档。
禁止修改：src、electron、测试实现、构建配置、版本号、CHANGELOG、发布资产。

必须完成：

1. 将 CHS 渠道状态任务与 CTM 测试连通性任务、STAT 使用记录统计任务统一纳入同一任务树和追踪矩阵。
2. 补齐 V1 先探测、V2 回退、V1 飞轮海卡片、V2 mdkj 卡片、全部站点/详情/悬浮窗共享载荷和缓存竞态边界。
3. 补齐按 siteId+keyId 安全请求 /v1/models、Key 切换刷新、首个模型自动选择、模型响应兼容、错误/空态/重试、普通文本流式测试和敏感信息边界。
4. 补齐中转站与 OpenCodex 最近 100 条平均耗时、平均缓存率、四卡片布局和筛选刷新规则。
5. 为 CHS、CTM、STAT 每个微观任务写清依赖、文件、测试方式、完成条件和真机影响；同步 03/04/08/09/10、模块长期事实和 pitfall。
6. 新增测试用例和 macOS 真机条目保持待执行，不提前写成通过；不创建独立 UI 目标。

验证：运行项目文档校验器、git diff --check，并确认 src/ 与 electron/ 没有本目标产生的实现源码 diff。发现不一致先修正文档再结束。

状态回写：将 03/04 更新为 documentation 1/2 已完成、待开发；测试和真机条目保持待执行。

完成条件：合并后的文档、任务拆分、微观任务、追踪矩阵、测试矩阵和真机清单完整一致，校验命令真实通过，且无实现源码修改。

下一阶段：解锁 development-and-acceptance 2/2；下一目标负责 CHS、CTM、STAT 的开发、测试、macOS 真机验收、Windows 交叉构建、版本递增 0.1、提交、双远端推送、Release 和最终归档。
