# REQ-260920 渠道状态、使用记录与缓存率告警 · 任务拆分

任务 ID：REQ-260920-channel-usage-cache-alert  
流程：深度流程，第 1/2 阶段 documentation  
依据：正式 PRD v1.0（已确认）  
状态：任务已拆分，待 development-and-acceptance 2/2 执行  
UI 路线：not-applicable；不单独造壳，沿用当前视觉方案  
验收责任：Codex 真机测试并检查页面样式

## 1. 固定执行拓扑

1. `V2-01 -> V2-02 -> V2-03 -> V2-04`：先取得同口径证据，再修共享归一化、快照和页面语义。
2. `AVG-01 -> AVG-02/03 -> AVG-04`：共享最近有效样本算法先于两类服务接线。
3. `USG-01 -> USG-02 -> USG-03`：查询规则和代次隔离先于页面状态接线。
4. `ALR-01 -> ALR-02 -> ALR-03 -> ALR-04/05`：纯状态机先于持久化、调度与系统通知。
5. `SET-01 -> SET-02/03`：共享设置契约先于设置页和生命周期清理。
6. `V2/AVG/USG/ALR/SET -> UI-01 -> QA-01 -> QA-02 -> QA-03 -> REL-01`。

通用禁止项：不得按站点、域名或分组硬编码；不得新增数据库表或第二套轮询；不得把 Windows 交叉构建写成真机通过；不得记录 API Key、账号、完整 URL、请求正文或 Token 明细；不得在 RED 证据缺失时事后补写“先红后绿”。

## 2. V2 渠道状态（REQ-01；AC-01～03、16）

| ID | 目标与依赖 | 输入与输出 | 允许文件 / 禁止文件 | RED、正常与失败路径 | 完成条件与回写 |
| --- | --- | --- | --- | --- | --- |
| V2-01 | 对同一真实 V2 站点、凭据、分组和 90m 范围做脱敏对照；无依赖 | 输入：官方请求和桌面端请求；输出：timezone、group_by、data_through、coverage、分组 ID/名称、success_rate、cache_rate、TTFT 差异表 | 允许真实测试临时证据、09、pitfalls；禁止完整凭据/响应正文和实现修改前下结论 | RED：复现官方绿色、客户端红色；正常：找到可重复字段/单位/快照差异；失败：证据不足时标记待补，不把时区当根因 | 根因有可复现证据；AC-01；回写 09、documentation audit，确认后再写 pitfalls |
| V2-02 | 统一 V2 比例和非法值归一化；依赖 V2-01 | 输入：0.984、98.4、0、100、缺失、非有限、越界；输出：统一 0..100 的安全值或无样本 | 允许 channel-monitor-v2 及其测试、共享契约；禁止站点特判和 V1 行为改写 | RED：两种比例当前解释不一致；正常：0.984/98.4 都为 98.4%；失败：非法/越界不进入健康判断 | 单元测试覆盖 0..1、1..100、边界与非法值；AC-02；回写 08、06/09 模块、pitfalls（仅确认事实） |
| V2-03 | 统一覆盖和状态语义；依赖 V2-02 | 输入：complete、partial、bootstrap、no-sample、stale、error、unsupported；输出：可区分状态 | 允许 V2 adapter、SiteService、渠道状态模型及测试；禁止把非完整统一渲染为红色 | RED：partial/bootstrap 等被误判异常；正常：各状态独立；失败：鉴权/临时失败保留错误或旧 stale 快照，明确不支持才走 V1 | 状态夹具和 V1 回归通过；AC-02；回写 08、06/09 模块 |
| V2-04 | 摘要、卡片、18 格、颜色和更新时间绑定同一快照并隔离迟到响应；依赖 V2-03 | 输入：快速切站/分组/范围和乱序响应；输出：带身份/代次的原子快照 | 允许 SiteService、loader、渠道组件及测试；禁止混合新旧代字段 | RED：旧响应可覆盖或页面混代；正常：只提交当前选择快照；失败：失败保留 stale 或明确错误，不清空可用缓存 | 竞态测试和页面断言通过；AC-03；回写 08、09、09 模块 |

## 3. 使用记录首次加载（REQ-02；AC-04～06、16）

| ID | 目标与依赖 | 输入与输出 | 允许文件 / 禁止文件 | RED、正常与失败路径 | 完成条件与回写 |
| --- | --- | --- | --- | --- | --- |
| USG-01 | 固化唯一查询构造器；无依赖 | 输入：站点、日期、Key/模型/分组/请求/计费筛选；输出：规范查询；“全部 Key”省略 api_key_id | 允许 usage-query-controller、数据适配及测试；禁止各入口自行拼参数 | RED：全部 Key 仍残留具体 ID；正常：入口参数一致；失败：无站点不发请求 | 参数表测试通过；AC-04/05；回写 08、08 模块 |
| USG-02 | 首次进入和切站立即查，筛选变化 300ms 防抖；依赖 USG-01 | 输入：页面进入、siteId 变化、筛选连点；输出：立即/防抖调度事件 | 允许 App、UsagePage、controller 及测试；禁止靠切换 Key 触发初始化 | RED：首次为空直到操作 Key；正常：进入/切站立即查；失败：无站点显示真实空态 | fake timers 覆盖立即与 300ms；AC-04/05；回写 08、09 |
| USG-03 | 请求代次、离页取消和独立容错；依赖 USG-02 | 输入：乱序列表/统计响应、离页、列表/统计单独失败；输出：当前代列表和统计状态 | 允许 load coordinator、App/UsagePage、测试；禁止统计失败清空列表 | RED：迟到结果覆盖或统计失败清空列表；正常：同范围提交；失败：列表成功/统计失败为 partial，列表失败才页面错误 | 竞态与 allSettled 路径通过；AC-05/06；回写 08、09、tooling pitfalls（若有新增事实） |

## 4. 最近 30 个有效样本（REQ-03；AC-07～09、16）

| ID | 目标与依赖 | 输入与输出 | 允许文件 / 禁止文件 | RED、正常与失败路径 | 完成条件与回写 |
| --- | --- | --- | --- | --- | --- |
| AVG-01 | 修正共享算法为“按时间倒序取最近 30 个有效样本”，两指标独立筛选；无依赖 | 输入：50 条混合记录；输出：平均值和真实 sampleCount | 允许 recent-averages 及测试；禁止先 slice(30) 再过滤有效值 | RED：有效值落在第 31 条后时样本不足；正常：真实 0 有效；失败：非有限/缺失排除 | 覆盖 0/18/29/30/50 和 0 值；AC-07～09；回写 08、08 模块 |
| AVG-02 | 接入 Sub2API firstTokenMs 和缓存公式；依赖 AVG-01 | 输入：使用记录全量范围；输出：平均首字、平均缓存率及各自 N | 允许 SiteService、Sub2API 数据适配及测试；禁止当前页 20 条代替 30 个有效样本 | RED：仍显示平均耗时/近 100 次；正常：缓存公式不重复 cache_creation；失败：零分母/全缺失无效 | 0% 有效、零分母无效；AC-07～09；回写 08、09、08 模块 |
| AVG-03 | 接入 OpenCodex firstOutputMs 和同一统计组件；依赖 AVG-01 | 输入：OpenCodex 历史；输出：平均首字、平均缓存率及 N | 允许 opencodex-data、页面及测试；禁止触发缓存告警 | RED：字段取错或仍为平均耗时；正常：独立有效样本；失败：无样本占位 | OpenCodex 回归通过；AC-07～09；回写 08、09、08 模块 |
| AVG-04 | 保持全范围总请求、Token、消费；依赖 AVG-02/03 | 输入：完整筛选结果与最近样本；输出：全量总计 + 最近统计 | 允许使用记录统计组合层及测试；禁止把所有卡片缩到 30 次 | RED：全量卡片数值改变；正常：只有两张平均卡使用最近 N；失败：统计失败保留列表 | 页面卡片断言通过；AC-09；回写 08、10 |

## 5. 缓存率告警（REQ-04；AC-10～13、16）

| ID | 目标与依赖 | 输入与输出 | 允许文件 / 禁止文件 | RED、正常与失败路径 | 完成条件与回写 |
| --- | --- | --- | --- | --- | --- |
| ALR-01 | 建立最小纯状态机 armed/notifying/alerted；依赖 AVG-01/02 | 输入：样本数、平均值、freshness、既有状态；输出：trigger/hold/rearm | 允许 notifications domain/shared 测试；禁止在 scheduler 内散落阈值分支 | RED：29 条或 80.00% 触发；正常：30 条且 79.99% 触发；失败/陈旧/不足保持状态 | 边界表通过；AC-10/11；回写 08、12 模块 |
| ALR-02 | 持久化事件身份和重启去重；依赖 ALR-01 | 输入：siteId+groupId，或规范化分组名；输出：notification_states 指纹/时间状态 | 允许 database 现有表、notification service 及测试；禁止新增表 | RED：重启重复两连；正常：未恢复事件保持 alerted；失败：单条状态读写失败不破坏其他组 | 新库/旧库兼容和重启测试通过；AC-11/12；回写 08、13 模块 |
| ALR-03 | 发送 0/2 秒两条且可取消；依赖 ALR-01/02 | 输入：新事件；输出：两次相同 title/body 和可取消 timer 集合 | 允许 notification service/main sender 及 fake timer 测试；禁止无限重试和 Renderer 发系统通知 | RED：次数/间隔或取消失败；正常：恰好 2 条；失败/权限关闭安全降级 | fake timers 覆盖 0/2、发送失败；AC-10/13/15；回写 08、12 模块 |
| ALR-04 | 接入后台刷新，站点串行、站内分组并发 <=2；依赖 ALR-02/03 | 输入：Sub2API 站点和分组；输出：逐组最近 30 有效样本判断 | 允许 RefreshScheduler/SiteService/通知服务及测试；禁止新增轮询、监控 OpenCodex | RED：跨组混算或并发超限；正常：单组失败隔离；失败：鉴权/网络不告警不恢复 | 并发计数和多站多组测试通过；AC-12；回写 08、11/12 模块 |
| ALR-05 | 固定通知文案与脱敏；依赖 ALR-03 | 输入：站点显示名、分组显示名；输出：约定标题正文 | 允许通知格式化函数和测试；禁止 API Key、账号、完整 URL、请求或 Token 明细 | RED：敏感 canary 泄漏；正常：标题和正文逐字匹配；失败：缺名使用安全占位或跳过，不泄密 | 内容/敏感扫描通过；AC-13；回写 08、14 模块 |

## 6. 生命周期（REQ-05；AC-14～16）

| ID | 目标与依赖 | 输入与输出 | 允许文件 / 禁止文件 | RED、正常与失败路径 | 完成条件与回写 |
| --- | --- | --- | --- | --- | --- |
| SET-01 | 不新增独立设置或 UI；无依赖 | 输入：用户最新口径；输出：自动运行的后台告警 | 允许沿用现有系统通知能力；禁止新增控件、设置键或新页面 | RED：出现多余设置 UI；正常：无需页面操作即可判断 | 页面无新增设置项；AC-14；回写 10、15 模块 |
| SET-02 | 恢复、删站/分组、退出取消剩余 timer；依赖 ALR-03/04 | 输入：生命周期事件；输出：对应 timer 清理和状态保持/重布防 | 允许 notification service、site lifecycle、main index 及测试；禁止清空无关通知状态 | RED：第 1 条后仍发第 2 条；正常：全部取消；失败：清理异常不阻塞退出 | 四类取消路径通过；AC-15；回写 08、12/13/15 模块 |

## 7. 页面、验证与发布（AC-16）

| ID | 目标与依赖 | 输入与输出 | 允许文件 / 禁止文件 | RED、正常与失败路径 | 完成条件与回写 |
| --- | --- | --- | --- | --- | --- |
| UI-01 | 沿用现有渠道和使用记录视觉完成状态文案与响应式；依赖全部业务任务 | 输入：当前视觉方案；输出：平均首字卡、近 N、状态文案 | 允许现有组件/CSS/测试；禁止独立 UI 壳、缓存率设置控件、系统通知仿制 UI | RED：宽窄窗溢出、红色覆盖所有非完整状态；正常：loading/empty/error/partial/stale/no-valid-sample 可辨；失败：保留可操作重试 | Renderer 测试和截图检查通过；回写 09、10 |
| QA-01 | 执行定向 TDD 与全量静态/测试门禁；依赖 UI-01 | 输入：最终实现；输出：真实命令记录 | 允许责任模块修复、08/04；禁止跳过失败 | 依次执行 format、lint、typecheck、定向/全量 Vitest、audit；失败回责任任务复测 | 全部通过且 diff 审查无无关改动；回写 04、08 |
| QA-02 | 生产构建和 Electron E2E；依赖 QA-01 | 输入：通过的源码；输出：首次进入、切站、卡片、设置和通知模拟证据 | 允许 E2E 和必要修复；禁止以开发态代替打包态 | 正常/空/partial/error/竞态均覆盖；失败回写问题、修复、重跑 | E2E 与 build 通过；回写 08、09 |
| QA-03 | Codex macOS 真机与页面样式检查；依赖 QA-02 | 输入：真实 V2 站点和当前 macOS ARM64 包；输出：脱敏对照、0/2 通知、去重/恢复、宽窄截图 | 允许 real-test-evidence、09 和必要修复；禁止保存凭据或伪造 Windows 真机 | 失败必须回写问题、修复并复测；系统权限关闭单独验证降级 | AC-01、04、07～15 真机项通过；回写 09、pitfalls |
| REL-01 | 3.5.0 版本、CHANGELOG、双平台构建、提交、双远端和统一 Release；依赖 QA-03 | 输入：验收通过提交；输出：DMG/NSIS、blockmap、manifest、哈希、Release | 允许版本/发布文件；禁止旧资产、单平台发布或绕过统一命令 | Windows 只做 x64 交叉构建/结构；任一步失败停止声称发布 | 按 AGENTS 发布协议全部验证；AC-16；回写 03/04/08/09/17/pitfalls |

## 8. REQ/AC 追踪

| REQ | AC | 微观任务 |
| --- | --- | --- |
| REQ-01 | AC-01、AC-02、AC-03、AC-16 | V2-01～04、UI-01、QA-01～03、REL-01 |
| REQ-02 | AC-04、AC-05、AC-06、AC-16 | USG-01～03、QA-01～03、REL-01 |
| REQ-03 | AC-07、AC-08、AC-09、AC-16 | AVG-01～04、UI-01、QA-01～03、REL-01 |
| REQ-04 | AC-10、AC-11、AC-12、AC-13、AC-16 | ALR-01～05、QA-01～03、REL-01 |
| REQ-05 | AC-14、AC-15、AC-16 | SET-01～02、UI-01、QA-01～03、REL-01 |

任何一项没有 RED/Green、失败路径、文档回写和真实验证证据时保持进行中。最后阶段完成后先交给用户检查；未收到“归档”不得创建正式归档。
