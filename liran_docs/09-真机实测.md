# macOS 真机实测清单

## 2026-09-04 2.8.0 OpenCodex 全量历史验收

| 用例     | 操作                                                 | 结果                                                     |
| -------- | ---------------------------------------------------- | -------------------------------------------------------- |
| OCX-2801 | 使用本机 OpenCodex 今日范围调用 request-history 分页 | 返回并完整汇总 4677 条，未截断为 2000/4000；通过         |
| OCX-2802 | 切换提供方/模型/状态和日期范围                       | 查询带服务端筛选条件并重新拉取全部匹配页；通过（自动化） |

说明：真实服务验证使用主进程同等分页逻辑，未输出管理员令牌；完整视觉截图仍受既有 Computer Use ScreenCaptureKit 超时限制。

## 2026-09-04 REQ-260904-usage-navigation-motion（Codex 验收记录）

本轮由 Codex 在 macOS ARM64 真实应用中执行；本节只保留待执行、失败和待复测项，未完成前不得写成通过。

|   # | 模块                            | 前置条件                   | 操作                                        | 期望结果                                                                   | 证据路径                                                            | 失败回写                                    | 状态                   |
| --: | ------------------------------- | -------------------------- | ------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------- | ---------------------- |
| 1–7 | OpenCodex、总览、三类切换、动效 | 2.7.0 ARM64 打包可执行文件 | Playwright 真实 Electron 窗口流程与几何检查 | 主流程通过；总览推荐区不存在；OpenCodex 缓存率列存在；三类切换代码路径通过 | `test-results/`、`release/mac-arm64/`                               | 无新增问题                                  | 通过（自动化真实窗口） |
|   8 | 窄窗/悬浮窗 Computer Use        | 2.7.0 ARM64 打包应用       | 读取桌面窗口并操作关键控件                  | 需补充截图与人工视觉确认                                                   | `real-test-evidence/macos-REQ-260904/08-motion-narrow-floating.png` | ScreenCaptureKit 连续超时，待系统可用后复测 | 阻塞                   |

补充证据：`npm run test:e2e` 为 13 项通过、2 项跳过；使用 `SUB2API_PACKAGED_EXECUTABLE` 直接启动 ARM64 打包可执行文件的主流程 1/1 通过。`npm run dist:mac` 和 `npm run dist:win` 已生成 2.7.0 产物。macOS Computer Use 因 ScreenCaptureKit 超时未完成视觉截图，不能写成完整人工真机通过；Windows 仅为 x64 交叉构建。

验收边界：真实日志中的管理员令牌、Cookie、完整 API Key 和私有数据不得进入截图、日志或文档。

## 2026-08-29 2.6.0 优化验收（用户实测通过）

|   # | 模块           | 操作                                 | 期望结果                                                  | 当前结果        |
| --: | -------------- | ------------------------------------ | --------------------------------------------------------- | --------------- |
|   1 | 全部站点文案   | 打开全部站点和倍率区域               | 不显示“随渠道状态全局同步刷新（10–20 秒）”文案            | ✅ 用户实测通过 |
|   2 | 中转站使用记录 | 查看 Token 与右侧列                  | Token 三行独立，缓存率为独立列且 Badge 颜色正确           | ✅ 用户实测通过 |
|   3 | OpenCodex      | 切换 OpenCodex 请求记录              | Token 三项存在，缓存率不显示                              | ✅ 用户实测通过 |
|   4 | 全站卡片渠道   | 等待 10–20 秒并切换主窗/悬浮窗       | 所有卡片渠道同步更新、显示 `更新于 HH:mm:ss` 且无重复请求 | ✅ 用户实测通过 |
|   5 | Sub2API 切换   | 已打开服务器后点击红框中的其他服务器 | 成功切换；失败保留原页面；登录态不串；不遮挡其他菜单      | ✅ 用户实测通过 |

## 2026-08-29 2.5.2 深度刷新与使用记录验收（用户实测通过）

|   # | 模块           | 操作                                            | 期望结果                                                  | 当前结果        | 证据/备注                            |
| --: | -------------- | ----------------------------------------------- | --------------------------------------------------------- | --------------- | ------------------------------------ |
|   1 | 全局刷新       | 隐藏主窗、最小化并等待两个周期                  | 站点数据随机 30–60 秒继续更新                             | ✅ 用户实测通过 | 需真实打包应用窗口证据               |
|   2 | 渠道状态       | 在总览、渠道页、悬浮窗交替观察 10–20 秒周期     | 三处共享同一 `fetchedAt`，无重复请求                      | ✅ 用户实测通过 | 需真实服务或可观测网络证据           |
|   3 | 失败隔离       | 让单站渠道请求失败                              | 其他站点继续刷新，旧数据标 stale                          | ✅ 用户实测通过 | 不使用生产破坏性操作                 |
|   4 | 使用记录       | 检查中转站与 OpenCodex Token 单元格             | 输入/输出/缓存读取均有图标，0 保留，缓存率 Badge 颜色正确 | ✅ 用户实测通过 | OpenCodex 真实字段语义需服务返回确认 |
|   5 | Radar/常用网站 | 执行 HTTPS 跨页、重定向、后退前进及危险协议尝试 | 安全跳转可用，危险协议/凭据 URL/新窗口仍被阻止            | ✅ 用户实测通过 | 需打包应用页面证据                   |
|   6 | Sub2API 切换   | 登录两个服务器后点击顶部切换栏并重启应用        | 当前服务器高亮、分区隔离、登录态不串联、失败保留原页面    | ✅ 用户实测通过 | 需真实登录态；不复制 Cookie/Token    |

当前状态：`用户验收通过 → 已完成`。用户于 2026 年 8 月 29 日完成目标版本 macOS 真实应用、真实窗口、真实页面和可用服务实测并确认通过；此前 Computer Use 定位超时及旧版安装包证据仅作为排障记录，不覆盖用户对目标版本的实际验收。2.6.0 已发布，发布资产和用户实测共同完成本轮收口。

## 2026-08-19 2.5.0 渠道关联仅手动指定验收清单

本轮把“自动关联作为站点卡片/悬浮窗最终展示来源”改为“仅手动指定”：有手动关联时卡片显示“手动指定”渠道摘要，无手动关联时显示“暂未关联渠道”，悬浮窗同步同一套解析结果。旧 2.4.0 真机行为与本轮口径一致（有手动关联显示“手动指定”渠道、无手动关联显示未关联空态）；2.5.0 空态文案统一为“暂未关联渠道”，已通过 mock 数据 Electron 运行时取证 + 视觉门禁确认，并在 2.5.0 安装包安装后由用户复核确认。

## 实测步骤

| #   | 模块             | 适用性 | 前置条件               | 操作                                           | 期望结果                                                                              | 证据路径                                                                                                                       | 失败回写 | 结果 |
| --- | ---------------- | ------ | ---------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | -------- | ---- |
| 1   | 手动关联展示     | 适用   | 真实设备应用与手动关联 | 读取站点卡片渠道摘要                           | 有手动关联的站点显示“手动指定”与手动渠道名                                            | Computer Use 真机读取（表哥/寒鹤/麻豆传媒），`real-test-evidence/macos-2.5.0/09-channel-status-popover.png`                    | 无       | ✅   |
| 2   | 无关联空态       | 适用   | 存在无手动关联站点     | 读取聪明AI 卡片摘要                            | 无手动关联时显示“当前分组未关联到具体渠道”（2.4.0）/“暂未关联渠道”（2.5.0 mock 取证） | Computer Use 真机读取、`real-test-evidence/macos-2.5.0/01-overview.png`（视觉门禁确认“暂未关联渠道”）                          | 无       | ✅   |
| 3   | 悬浮窗一致       | 适用   | 悬浮窗打开             | 读取悬浮窗渠道卡片                             | 显示手动关联渠道与“近期可用率”，无自动补出、无溢出                                    | `real-test-evidence/macos-2.5.0/05-floating.png`、`27-floating-channel-dialog.png`（视觉门禁确认）                             | 无       | ✅   |
| 4   | 弹层当前关联渠道 | 适用   | 渠道状态弹层打开       | 读取弹层“当前关联渠道”与渠道列表“已关联/关联”  | 弹层展示手动关联渠道，渠道按钮显示“已关联/关联”切换且可操作                           | `real-test-evidence/macos-2.5.0/09-channel-status-popover.png`、`17-channel-manual-association.png`（视觉门禁确认）            | 无       | ✅   |
| 5   | 布局与样式       | 适用   | 宏观/窄窗              | 视觉检查站点卡片摘要与底部操作区、悬浮窗、弹层 | 摘要固定槽位 100px 内无溢出；卡片/悬浮窗/弹层无 P0/P1 重叠溢出                        | `real-test-evidence/macos-2.5.0/01-overview.png`、`10-overview-wide-footer-layout.png`、`11-overview-narrow-footer-layout.png` | 无       | ✅   |
| 6   | 自动化回归       | 适用   | 全量自动化             | 运行全量 Vitest 与 Electron E2E                | Vitest 406 项通过；E2E 10 通过 + 1 预期跳过                                           | `npm run test:e2e`（本机 macOS Electron 运行时）                                                                               | 无       | ✅   |
| 7   | 2.5.0 安装包验收 | 适用   | 2.5.0 DMG 构建并安装   | 安装 2.5.0 并复核“暂未关联渠道”与悬浮窗        | 与 2.5.0 源码口径一致；用户确认安装并复核通过                                         | 发布为 GitHub Release 2.5.0，用户自行安装复核                                                                                  | 无       | ✅   |

## 当前状态

`真机测试通过`：2.5.0 已发布（GitHub Release 2.5.0，tag/提交 12c23c0），用户自行安装并复核通过；内部实现、自动化、Electron 运行时取证、真机界面读取与页面样式检查全部完成。开发追踪与发布记录见 [[04-开发追踪]]。

## 2026-08-14 常用网站模块验收清单

本轮新增“常用网站”模块，支持在线、localhost、本机回环和局域网地址，支持规则编辑、独立持久化会话和内嵌网页。自动化证据：全量 Vitest 389 项通过、类型检查/lint/格式/生产构建通过、完整 Electron E2E 通过（含新增 2 项常用网站 E2E）。页面样式已通过截图检查，无 P0/P1 重叠、溢出或截断。

## 实测步骤

| #   | 模块               | 适用性 | 前置条件             | 操作                                        | 期望结果                                              | 证据路径                                                                                 | 失败回写 | 结果 |
| --- | ------------------ | ------ | -------------------- | ------------------------------------------- | ----------------------------------------------------- | ---------------------------------------------------------------------------------------- | -------- | ---- |
| 1   | 菜单入口           | 适用   | 生产构建完成         | 打开主面板并进入常用网站                    | 侧栏显示“常用网站”，空态和新增入口正常                | test-results/fav-list.png、tests/e2e/favorite-websites.spec.ts                           | 无       | ✅   |
| 2   | 网站新增           | 适用   | 常用网站空列表       | 新增 HTTP 本机地址网站                      | 名称和规范化 URL 保存，卡片数量增加                   | tests/e2e/favorite-websites.spec.ts                                                      | 无       | ✅   |
| 3   | 地址校验           | 适用   | 共享规则单测         | 提交 file/javascript/data/用户名密码/空地址 | 全部拒绝并显示明确错误                                | electron/shared/favorite-websites.test.ts                                                | 无       | ✅   |
| 4   | 网站编辑与删除     | 适用   | 已有网站             | 编辑名称并删除网站                          | 名称更新、删除确认、列表与配置持久化正确              | tests/e2e/favorite-websites.spec.ts                                                      | 无       | ✅   |
| 5   | 规则编辑与类型开关 | 适用   | 已有政策             | 编辑地址支持规则并关闭类型开关              | 规则持久化，受影响网站按规则标记                      | tests/e2e/favorite-websites.spec.ts、test-results/fav-policy.png                         | 无       | ✅   |
| 6   | 自定义允许规则     | 适用   | 政策编辑器           | 新增、编辑、启停和删除规则                  | 规则匹配、去重和保存正确                              | electron/shared/favorite-websites.test.ts、tests/e2e/favorite-websites.spec.ts           | 无       | ✅   |
| 7   | 被禁用网站保留     | 适用   | 规则关闭             | 关闭 HTTP 或局域网规则                      | 网站保留但不可打开，恢复规则后可用                    | electron/shared/favorite-websites.affinity.test.ts                                       | 无       | ✅   |
| 8   | 内嵌网页与工具栏   | 适用   | 本机 HTTP 夹具服务器 | 打开网站并操作关闭/返回/主页/刷新           | 网页在应用内加载，工具栏状态与关闭正常                | tests/e2e/favorite-websites.spec.ts、test-results/fav-embed.png                          | 无       | ✅   |
| 9   | 顶层导航与跨域拦截 | 适用   | 共享导航策略单测     | 验证同源放行、HTTP/HTTPS 切换和跨域拒绝     | 决策函数按当前规则拦截，永久禁止项不可放行            | electron/shared/favorite-websites.test.ts                                                | 无       | ✅   |
| 10  | 独立分区会话       | 适用   | 本机测试站点         | 重启同一 userData                           | 网站配置保留，独立 persist partition 不被其他网站污染 | tests/e2e/favorite-websites.spec.ts、electron/main/services/favorite-websites-manager.ts | 无       | ✅   |
| 11  | 既有功能回归       | 适用   | 全量自动化           | 运行全量 Vitest 与 Electron E2E             | 雷达、Sub2API 服务器和业务链路无回归                  | test-results/.last-run.json                                                              | 无       | ✅   |
| 12  | 页面样式检查       | 适用   | 真实窗口截图         | 检查列表、规则弹窗和内嵌工具栏              | 无 P0/P1 重叠、溢出或截断                             | test-results/fav-list.png、test-results/fav-policy.png、test-results/fav-embed.png       | 无       | ✅   |

## 当前状态

`真机测试通过`

发布证据：版本提交 05cb61a 已推送 GitHub 与 Gitee 当前分支，标签 2.2.0 已推送到两个远端；GitHub stable Release 2.2.0 为非草稿、非预发布，DMG、EXE、两个 blockmap 与 update-manifest.json 五项资产均为 uploaded。DMG SHA-256 5da25e2ef43003c75795d4acaf3faaa17b32c2e2ba39f03c87074e32ad73d183；EXE SHA-256 049eb5a162fcc6a19d16848a9431be6cede78ed18cb92c75faa0d52e5bb870eb。

更新修复发布：2.2.0 因发布中断导致线上 DMG 与 manifest 哈希不一致，客户端下载校验失败；2.2.1 使用同一次构建重新生成全部资产，实测下载 GitHub Release 2.2.1 DMG 的 SHA-256 54fa85db755af6fcd0154edf36459f7c09a02b8d17518a2cba3c0d18427af7c9 与远端 update-manifest.json 完全一致，客户端检查、下载与校验流程通过。

## 2026-08-10 2.1.2 Sub2API 快捷入口内置模板验收清单

- [x] 使用隔离 `SUB2API_TEST_USER_DATA` 启动 2.1.2 macOS ARM64 打包应用，新增 `https://www.api-feng.online/` 服务器；新增弹窗不显示快捷入口。
- [x] 保存后点击编辑立即显示本地模板选择器，无需登录；搜索“账号管理”只显示 `/admin/accounts`，两个分组标题正常。
- [x] 勾选 5 个快捷入口后显示“已选 5/5”，未选项禁用；取消再勾选后保存，卡片按模板顺序显示 5 个标签和对应 Lucide 图标。
- [x] 重启同一 userData 后 5 个快捷入口保留；点击“账号管理”后内嵌网页打开真实站点 `/admin/accounts`。
- [x] 使用真实账号登录成功后进入管理后台；关闭并重启同一 userData 后再次打开快捷入口直接进入 `/admin/accounts`，登录态由独立 `persist:` partition 保持。
- [x] 页面样式由视觉门禁逐张检查：服务器卡片、编辑弹窗（滚动到底部）、5 个快捷入口、重启保留、快捷入口直达和登录后管理后台均无 P0/P1 重叠、溢出或截断；编辑弹窗无双滚动条。
- [x] 只读挂载 2.1.2 DMG 后再次执行同一真实站点用例，1/1 通过，证据目录 `real-test-evidence/macos-2.1.2-dmg/`。
- [x] 旧菜单发现缓存停止读取；删除/清除/origin 变更的按服务器清理由 Storage 单测覆盖。
- [x] Windows x64 仅记录 NSIS/PE 交叉构建和结构证据，不写成 Windows 真机通过。

验收命令：`SUB2API_PACKAGED_EXECUTABLE=... SUB2API_SUB2API_REAL_E2E=1 SUB2API_REAL_EVIDENCE_DIR=real-test-evidence/macos-2.1.2 npm run test:e2e -- --grep "real api-feng"`，结果 1/1 通过；凭据仅从本次验收环境变量读取，不写入仓库、文档、日志或截图。

视觉证据：`real-test-evidence/macos-2.1.2/` 中 `01-server-saved.png`、`02-template-editor.png`、`03-five-shortcuts.png`、`04-restart-preserved.png`、`05-shortcut-open.png`、`06-after-login.png`、`07-after-restart-login.png`、`login-state.json` 和 `login-state-restart.json`；只读 DMG 复测证据位于 `real-test-evidence/macos-2.1.2-dmg/`。分析结果由视觉模型提供。

当前状态：`真机测试通过`

产物哈希：DMG `fa5d84a78e56078ef82120f6027610b5bc8464cae285dfab1295f547cbfa46ee`；DMG blockmap `f550e9983df851dfe5eb649323dc5f1ef01bb15ac468cc3255875867fcc9ecd9`；EXE `61ac35709003a33d603884609263023d42c3cc7bb7ddded5b7839580869b8d3c`；EXE blockmap `88c86346317e203595a4d749c33858e6c2165530c010f956d500dd59d076c5c4`。

发布证据：版本提交 `43f284f` 已推送 GitHub 与 Gitee 当前分支及 `2.1.2` 标签；GitHub stable Release `2.1.2` 为非草稿、非预发布，五项资产均为 uploaded；远端 manifest SHA-256 `6ef0d6a0ad162aa14bd811c1d4dd77c6bf5e4ff3f38859d029b1f3035921576a`。

## 2026-08-09 2.1.0 Sub2API 快捷入口菜单化验收清单

- [x] 使用隔离 `SUB2API_TEST_USER_DATA` 启动 2.1.0 macOS ARM64 打包应用，新增服务器弹窗不显示手填快捷入口字段。
- [x] 打包应用绑定本机真实 HTTPS 夹具服务器，点击“获取菜单”后发现“仪表盘、账号管理、使用记录、API 密钥、渠道状态、我的订阅”六项，登录/外链/退出链接被过滤。
- [x] 勾选 5 个菜单并保存后卡片显示对应快捷标签；重启同一 userData 后菜单缓存与 5 个快捷入口保留。
- [x] api-feng.online 真实站点登录边界：应用内嵌页可加载与导航，但 UI 登录受站点 Turnstile 拦截，API Token 注入会被站点前端鉴权守卫清除；已记录为外部站点交互验证限制，不把未完成的真实菜单列表当作已通过。
- [x] 旧手工快捷入口兼容、菜单缓存清理、清除会话提示和窄窗口样式由自动化 E2E 与页面检查覆盖。
- [x] 页面样式检查覆盖普通桌面与窄窗口：菜单选择器滚动、选中/禁用态、登录过期态、错误态、空态和卡片标签无 P0/P1 重叠或溢出。
- [x] Windows x64 仅记录 NSIS/PE 交叉构建和结构证据，不写成 Windows 真机通过。

当前状态：`真机测试通过（夹具真实 HTTPS 菜单发现；api-feng 完整菜单登录受外部 Turnstile 限制）`

产物哈希：DMG `6f1ada34aad2ffa1f290b6775728f0ad65ba8f7707a963a42f364aabc9250630`；DMG blockmap `788166ebd0ee5878b5d32fa3822c6fdd9115a379fbff154d4eb0e242ddae7b47`；EXE `f7ee5c144f9adce25d57bb6e219b44f0ea8e127d3569ba5675feaa6701276654`；EXE blockmap `f4b03ee32324ce45e7e6c501bf45e4b1f350e5235bef46ae2846aae1eb04593e`。

## 2026-08-09 2.1.1 悬浮窗可用率文案验收结果

- [x] 2.1.1 macOS ARM64 打包应用重新执行本机真实 HTTPS 夹具菜单发现流程，6 项菜单、选择 5 个、保存与重启保留全部通过。
- [x] 悬浮窗主短卡与关联渠道弹框文案已由“近 12 次可用 / 最近 12 次”改为“近期可用率”，百分比计算与 20 格时间线由 Renderer/E2E 回归覆盖。
- [x] DMG `hdiutil verify` 为 `VALID`；Windows x64 仅记录交叉构建。

产物哈希：DMG `cacfcf0bf65d5974ae0ee824f820ced30ceedc85b57e1cf713b641a189ff7939`；EXE `11e583581bbc1e0311a175a3f0408fc475dcecef5ca88e388ec006c68bd11072`；远端 `update-manifest.json` SHA-256 `35fdf06c53abec12775f9d114d5df5d7060212f6e7d39f7d2523b354318dfac1`。

## 2026-08-09 2.0.0 Sub2API 服务器管理与现有问题修复验收结果

- [x] 使用隔离 `SUB2API_TEST_USER_DATA` 启动 2.0.0 macOS ARM64 打包应用，新增 `https://www.api-feng.online/` 服务器并重启同一 userData，服务器卡片与配置持久化保留。
- [x] 点击卡片后服务器网页在当前主窗口 `WebContentsView` 内加载；真实站点无登录表单即可进入 `/home`，登录状态正确显示“已登录”；重启后仍为“已登录”，证明独立 `persist:` partition 会话保持。
- [x] 点击“清除 真机测试服务器 登录状态”后清理分区存储并重置登录状态，卡片与列表恢复正常，无残留视图。
- [x] 菜单映射真机复核：“通知”进入“通知规则设置”，“设置”进入“通用设置”。
- [x] 页面样式由视觉门禁逐张复核：服务器列表、重启后卡片、内嵌网页 toolbar、清除会话、通知规则和通用设置均无 P0/P1；卡片标题、域名、操作按钮无重叠，通知浮层不再遮挡工具栏。
- [x] 本地受控全链路 Electron E2E 覆盖 Sub2API CRUD、重启保留、菜单映射、悬浮窗 20 格、余额不闪总额、favicon、无渠道过滤与时间线；完整 E2E 为 8 项通过、1 项按配置跳过。
- [x] Windows x64 仅记录 NSIS/PE 交叉构建和结构证据，不写成 Windows 真机通过。

证据目录：`real-test-evidence/macos-2.0.0/`，其中 `01-server-added.png`、`02-card-geometry.json`、`03-remote-before.json`、`04-embedded-before.png`、`06-login-state.json`、`09-restart-login-state.json`、`10-restart-logged-in.png`、`11-server-after-clear.png`、`12-notification-rules.png`、`13-general-settings.png` 为本轮真实证据。

Release 资产：DMG `b35b3f2388eee09f0d1f98a76522ebcea2b460a0dfc13d5d8038e8c49bed00ce`；DMG blockmap `e114c63f31058b50bc2e74d6ea6e9a1e8d1e20e8b93f7da4063e8efd00d4e5d8`；EXE `d4316906e662f8971eb78454a54331c6c4037d60993c16a49efcf2514cbb9239`；EXE blockmap `5c7274b5b26b597880560b279fbc361669296b0ad2417abb1b8d353e6dbe96ae`。

当前状态：`真机测试通过`

## 2026-08-09 1.9.3 Radar 动态站点列表验收结果

- [x] 使用隔离 `SUB2API_TEST_USER_DATA` 启动真实 macOS Electron，首次进入 Radar 显示两个默认站点，顶部有“新增雷达站点”，卡片打开/删除按钮为同级且删除不触发打开。
- [x] 新增弹窗在真实应用中打开；无效 `http://example.com` 显示“网址必须是完整的 HTTPS 地址”，重复名称和重复规范化 URL 均显示行内错误。
- [x] 新增长名称站点 `https://example.com/very/long/path?query=radar-long-url-test` 后卡片数量为 3，长名称换行且不撑破卡片；真实 `WebContentsView` 加载 Example Domain 成功。
- [x] 两个公网默认站点 `https://codexradar.com/` 与 `https://deng.codexradar.com/` 均在主窗口 `WebContentsView` 内加载；关闭图标、`Esc`、resize 到 `960x640` 后 bounds 为 `676x560`，返回后恢复动态列表。
- [x] 重启同一隔离 userData 后新增项仍存在；删除弹窗显示名称和完整 URL，取消后保留，确认后消失并保持其余条目。
- [x] 删除全部站点后显示空态和新增入口；SQLite 设置只保留 `radar:entries`，未触碰站点凭据、渠道或业务缓存。
- [x] 页面样式截图由视觉门禁逐张检查：默认列表、新增弹窗、错误态、长名称卡片、删除确认、空态、持久化列表、嵌入网页和窄窗口无重叠/溢出/遮挡；远程站点自身小分辨率省略号不属于应用壳回归。

验收命令：`SUB2API_RADAR_REAL_E2E=1 SUB2API_REAL_EVIDENCE_DIR=real-test-evidence/macos-1.9.3 npm run test:e2e -- --grep "embeds both real Radar sites"`，结果 1/1 通过。

视觉证据：`real-test-evidence/macos-1.9.3/` 中的 `radar-chooser.png`、`radar-add-dialog.png`、`radar-add-invalid-http.png`、`radar-after-add-long.png`、`radar-delete-dialog.png`、`radar-empty.png`、`radar-persisted-after-restart.png`、`radar-added-window.png`、`radar-codex-window.png`、`radar-codex-window-small.png`、`radar-distributed-window.png` 和远程页面截图。分析结果由视觉模型提供。

打包证据：`npm run dist:mac` 与 `npm run dist:win` 均成功；DMG `hdiutil verify` 为 `VALID`。只读挂载 DMG 后，`SUB2API_PACKAGED_EXECUTABLE` 启动的打包应用完整 Electron E2E 为 7 项通过、1 项按配置跳过；同一打包应用的 Radar 真实网络用例再以 1/1 通过。Windows x64 仅记录 NSIS/PE 交叉构建，不代表 Windows 真机通过。

产物哈希：DMG `f4fceec23647542264e038547f804d2be916c9569fa496d835707e2e6d3ded90`；DMG blockmap `f8d2939d923b20bc8691fedf065c0c97c7c40bf1ffe2a143158f31091e67ec41`；EXE `7d22d6a2edfb886592986e3a34567312df7a540cb258a2db1b7f3a07172493ed`；EXE blockmap `4ed882c8d2c24956f23044aaf8887b077fcbc42b3bb563a3c2fd2e14b18a2785`。

当前状态：`真机测试通过`

## 2026-08-07 1.9.2 最近 12 次渠道记录验收结果

- [x] 从本轮 1.9.2 DMG 挂载的包内应用执行完整 Electron E2E，确认版本、入口、arm64、签名结构和 `380×260` 悬浮窗。
- [x] 主渠道卡只显示“近 12 次可用”和状态格，不出现来源、渠道名或状态徽标标题行，也不残留旧高度空白。
- [x] 12 条真实记录全部显示状态颜色，最左为截取范围内最早检查，最右为最新 `checked_at`；刷新新增记录后旧格左移且最旧记录移除。
- [x] 少于 12 条时浅灰空槽只在左侧，真实记录在右侧；空槽“暂无更早记录”与未知灰色可区分，零记录显示“暂无渠道记录”。
- [x] 每个真实格的 tooltip/ARIA 包含本地时间与中文状态；点击精简区域可打开全部关联渠道弹框，弹框显示“最近 12 次”且关闭/焦点行为保持。
- [x] 余额、倍率、Key、今日 Token、今日消费、footer t/s、切站、设置、扩大、刷新和拖动无重叠、溢出或行为回归。
- [x] Windows x64 只记录 NSIS、PE/asar、版本和入口交叉构建，不写成 Windows 真机通过。

验收证据：`real-test-evidence/macos-1.9.2/05-floating.png` 显示 12 个真实状态格且最右为刷新后的最新状态，`27-floating-channel-dialog.png` 显示全部关联渠道的最近 12 次；`packaged-directory-short-history/28-floating-short-history.png` 显示左侧 9 个空槽和右侧 3 条真实记录。顶层完整 E2E 来自只读 DMG 挂载应用（6 项通过、1 项跳过）；短历史补充流程来自同一构建输出的打包目录应用（1 项通过）。

产物证据：DMG `hdiutil verify` 为 `VALID`，挂载应用严格签名、arm64、bundle/asar `1.9.2` 和入口通过；DMG SHA-256 为 `b8e52c97c42f5fc4d229aeec84690cca4057c05755d03c7af092ca513fe167c7`，blockmap 为 `fc579687906bb5348677506bd4bb5e66421e117646bb995c74ba53ad8b50c0d9`。Windows EXE SHA-256 为 `e327240541533466f28cbda8d1642ee4672af74660a9bb653fdfa661502fa8ec`，blockmap 为 `8fe877311aae25a18bd5cfba1ad29066c8106075e1d75282cef54fc215501fa5`；仅为 x64 交叉构建。

已知边界：同一 DMG 首次挂载完整 E2E 已通过；随后为新增短历史证据重复启动挂载路径时，Playwright 在 `electron.launch` 握手阶段超时，而直接执行同一挂载二进制正常、同源打包目录应用测试通过。该环境问题不推翻首次完整挂载态结果，但短历史截图明确记录为打包目录应用证据。

当前状态：`macOS 真机测试与页面样式检查通过；Windows 不属于真机结论。`

发布证据：版本提交 `13dfdf3` 已推送 GitHub 与 Gitee，远端分支和标签 `1.9.2` 均指向 `13dfdf34580b606d7ffbfd5150a4d13f2f774f16`。GitHub Release `1.9.2 稳定版` 为非草稿、非预发布，五项资产全部 uploaded；远端 manifest 为 stable、`testOnly:false`，SHA-256 为 `f0c38b5c44ea301ca8a56c9132892433de945d58c4458af9e671d43a82eba5a2`。

## 2026-08-07 1.9.1 悬浮窗渠道卡片验收结果

- [x] t/s 徽标位于 49px 底栏空白区，Gauge、两位小数和慢/正常/快/暂无分级保留，状态行与扩大/刷新按钮没有挤压或重叠。
- [x] 旧“X 个关联渠道”摘要、`floating-channels` details 和覆盖今日统计的旧面板不存在；当前渠道短卡在主体右侧直接可见。
- [x] 短卡显示手动/自动来源、中文健康状态、最近 1 分钟可用率和 12 个稳定时间线槽；长名称单行省略，无检查点明确显示无数据。
- [x] 自动/手动多渠道、单渠道、无关联、加载/错误/不支持、非法/未来时间边界和正反返回顺序均有回归；全部站点与悬浮窗复用同一稳定主展示渠道选择函数。
- [x] 点击短卡显示全部关联渠道弹框；关闭图标、Escape、遮罩关闭、Tab 焦点循环、关联失效主动关闭和焦点归还通过，内容区滚动且不改变 `380×260` 窗口尺寸。
- [x] 余额、倍率、Key、今日 Token、今日消费、底栏、扩大/刷新、拖动和非激活显示保持；30–60 秒随机刷新、最新用量扫描、手动全刷新、旧响应保护和跨站隔离回归通过。
- [x] macOS ARM64 DMG 挂载应用完整 Electron E2E 为 6 项通过、1 项真实 Radar 网络用例按配置跳过；短卡与弹框视觉均按三张参考图和现有浅色方案人工检查。
- [x] DMG `hdiutil verify` 为 `VALID`，严格签名结构、arm64、bundle `1.9.1`、asar 版本和入口通过；Windows x64 仅完成 NSIS、PE/asar 和版本结构交叉检查。

验收证据：`real-test-evidence/macos-1.9.1/05-floating.png`、`25-usage-speed.png` 与 `27-floating-channel-dialog.png`。DMG SHA-256 为 `295361ce2509ae610ee5ff47f39efbc4111e010a3fc6df8c52e7c5ca7aebde14`，Windows EXE SHA-256 为 `3a2cb17d9c1762d1532027967f5d7e852a204cc5f0874e731210165e02835c21`。当前状态：`macOS 真机测试与页面样式检查通过；Windows 不属于真机结论。`

发布证据：版本提交 `af88fd8` 已推送 GitHub 与 Gitee，标签 `1.9.1` 指向该提交；GitHub Release 为 stable、非草稿、非预发布，五项资产全部 uploaded。远端 `update-manifest.json` 为 `stable`、`testOnly:false`，SHA-256 为 `8d52e9823debb7e01b5776b792be4de2b8fcc6c85fefc019cab8290befc00ac3`。

## 2026-08-07 1.9.0 验收结果

- [x] 使用记录显示耗时和两位小数 t/s，慢/正常/快/不可用徽标正确；产生新记录后悬浮窗近实时更新且数据不串站。
- [x] 全部站点渠道刷新采用每轮 30–60 秒随机调度，页面隐藏暂停、恢复刷新、限流/失败退避和最近 1 分钟稳定语义正确。
- [x] 站点卡片鼠标拖动与键盘排序可用，重启保持；新建追加、删除清理，选择器和悬浮窗顺序一致。
- [x] 充值比例计算器浮层可保存/回滚，并支持焦点、Escape、点击外部关闭。
- [x] 悬浮窗额度随机刷新，Key 渠道摘要和一分钟详情覆盖全部状态；右下手刷同步刷新余额、Key、usage/t/s 和渠道。
- [x] 批量验证卡片、站点图标/名称、脱敏账号、能力摘要和详情弹层正确；单站名称保留，批量 title/favicon 安全回退正确。
- [x] 侧栏“通知”打开通用设置，“设置”打开通知规则；站点管理没有设置残留。
- [x] 新证据目录覆盖主窗口宽屏、窄屏和 `380×260` 悬浮窗，并检查 loading/error/empty/success、弹层、拖动、截断和控件重叠。

验收证据：开发态和 macOS ARM64 打包态 Electron E2E 各 6 项通过、1 项真实 Radar 网络用例按配置跳过；截图位于 `real-test-evidence/macos-1.9.0/`。`21-batch-cards-wide.png`、`22-batch-cards-narrow.png`、`25-usage-speed.png`、`26-batch-card-detail.png` 和 `05-floating.png` 覆盖本轮关键样式。30–60 秒随机边界、隐藏/恢复、退避和 1 分钟边界使用 fake timers 与可注入随机源确定性验证，不以真实等待一分钟替代测试。

产物证据：macOS ARM64 DMG 通过 `hdiutil verify`，应用包通过 codesign 结构校验，Mach-O 为 arm64，包内版本为 `1.9.0` 且主进程/Renderer 入口存在。Windows x64 NSIS、PE32+ 主程序和 asar 版本/入口检查通过，但未执行 Windows 真机安装或交互。

发布证据：版本提交 `af67152` 已推送 GitHub 和 Gitee 当前分支，远端标签 `1.9.0` 指向该提交。GitHub Release `1.9.0` 为 stable、非草稿、非预发布，DMG、EXE、两个 blockmap 和 `update-manifest.json` 五项资产均已上传；远端 manifest SHA-256 为 `7b42a44dd0ac359ba2c0e1bd26fc1fdc8994631c78610a504bf87240f478bb08`。

当前状态：`真机测试通过`。本结论覆盖 macOS ARM64 打包应用、受控本地站点全流程和页面视觉；不包含真实外部账号刷新等待，也不扩展为 Windows 真机结论。

## 当前状态

`真机测试通过`

## 2026-08-05 1.7.12 用户真实 Chrome 验收通过

- 用户确认已在系统 Google Chrome 中完成 Cloudflare Turnstile 人机验证和账号登录，应用随后成功完成站点添加。
- 本条记录只依据用户直接验收反馈，不写入账号、密码、Token、Cookie、CDP 地址或敏感接口响应；未要求用户提供敏感截图。
- macOS/Windows 业务 Renderer 复用同一套 provider 路由和安全边界；Windows 本轮仍只有 x64 交叉构建证据，不扩展为 Windows 真机结论。

当前状态：`用户验收通过：真实 Chrome 人机验证、登录和站点添加已由用户确认成功。`

## 2026-08-05 1.7.12 交互认证浏览器边界复测

- 本轮目标：确认 GeeTest Electron 官方窗口使用原生浏览器标识，不再执行 UA/UA-CH/CDP iframe 注入；确认 Turnstile 仍启动系统 Google Chrome 独立临时 Profile。
- `npm run format:check`、`npm run lint`、`npm run typecheck`、`npm test -- --run`（44 个文件/300 项）、`npm run build`、开发态 Electron E2E（6/6）和 macOS ARM64 打包态 Electron E2E（6/6）均通过。
- 真实 Chrome 烟测使用系统 Google Chrome、独立临时 Profile、随机回环 CDP 和脱敏占位输入，4 秒后返回 `CHROME_AUTH_TIMEOUT`；本次 Profile、Chrome 进程均清理完成，未提交账号或点击 CAPTCHA。
- 页面样式检查覆盖宽/窄窗口、统一通知、安全验证弹窗关闭图标、Escape、按钮布局和渠道状态弹窗；截图位于 `real-test-evidence/macos-1.7.12-final/`，未发现文字溢出、按钮重叠或窄窗口布局破坏。
- macOS ARM64 DMG `hdiutil verify` 为 `VALID`；DMG SHA-256：`d0ceca5777ff258528663d162f539f13d78abeb3bc937488001f95d10732c8d9`；DMG blockmap：`c7e710d47d5248db0c1c0ad471db58e2721d0ec0fa86df61b7fe068e6f03edce`。
- Windows x64 NSIS 交叉构建、PE/asar/版本/入口结构检查通过；EXE SHA-256：`e1647481ad6ef2bf81d1b9b0b9de078703b0ef7cddf0cf124961157da27bc93a`；EXE blockmap：`be3d1dd37415b10332ade094f0a38d45ef8d06ab6d31938e36620d122d75edef`。Windows 不代表真机通过。
- 真实 CAPTCHA、账号登录、核心接口校验和站点最终保存已由用户在后续验收中完成；本节的 Chrome 烟测仍仅代表隔离启动与清理，不替代用户真实登录证据。

当前状态：`用户验收结果见本文件顶部；本节自动化与打包证据通过。`

## 2026-08-05 1.7.11 退出清理、打包态与视觉收口

- `npm run test:e2e` 开发态 6/6、macOS ARM64 打包态 6/6；打包应用从 `release/mac-arm64/看看你还有💰吗？.app` 启动，主窗口、悬浮窗、设置持久化、站点壳和本地受控 API 流程均通过。
- 真实 Chrome 服务烟测使用系统 Google Chrome、独立临时 Profile、127.0.0.1 随机 CDP 和脱敏占位输入，4 秒后按预期返回 `CHROME_AUTH_TIMEOUT`；本次 Profile 已删除，结束后没有带本次前缀的 Chrome 进程。历史临时目录未擅自删除。
- 页面样式检查覆盖宽/窄窗口、长通知堆叠、安全验证弹窗、右上角关闭图标、按钮布局、Escape 关闭行为和渠道状态弹窗；截图位于 `real-test-evidence/macos-1.7.11-final/`，未发现文字溢出、按钮重叠或窄窗口布局破坏。
- macOS ARM64 DMG `hdiutil verify` 为 `VALID`，SHA-256：`d9d36685d4d2337c01b57401b2f9362f3c57ba35585ce198e89c88a31bae2d2d`；DMG blockmap：`d06486ae1b498ea8cebdfeaf8a5fbf259a2e3d0f73294810aabd6131afec9ad7`。
- Windows x64 NSIS 交叉构建、PE/asar/版本/入口结构检查通过；EXE SHA-256：`e09c752059d514c010c325ee399a75c32fc5653dcc0cca2600a7fadeaedfb1b3`；EXE blockmap：`7be7ca9cdb0ac3acf065256072a6509b9334c61c3c369e67a924369cc6d830db`。Windows 不代表真机通过。

当前状态：`受阻：系统 Chrome 启动、CDP 隔离、退出清理和 UI 已验证；真实 Cloudflare CAPTCHA、账号登录、核心接口校验和站点保存仍需用户本人完成。`

证据目录：`real-test-evidence/macos-1.7.11-final/`。

## 2026-08-05 1.7.10 Chrome 登录失败直接重试复核

- Chrome 未安装、启动失败、窗口关闭、同源校验失败、Token 缺失和超时都会保留安全验证弹窗；统一通知说明失败原因，点击“开始登录”即可直接重试，任何失败都不保存站点。
- `ai.maok.shop/api/v1/settings/public` 只读请求返回 HTTP 200 且公开设置声明 Turnstile；`/login` 返回 HTTP 200 并包含 Turnstile 页面标记，未提交账号、未读取 Cookie 或页面存储。
- 当前版本在 macOS 真实 Chrome 临时 Profile 烟测中按 4 秒超时返回 `CHROME_AUTH_TIMEOUT`，Chrome 进程和临时 Profile 均已清理；真实 CAPTCHA、账号登录、核心接口校验和站点保存仍需用户本人完成。
- 打包应用真实点击“开始登录”后确认启动了系统 Google Chrome、随机回环 CDP 和独立临时 Profile；关闭应用后通过进程和目录检查确认 Chrome 与 Profile 均清理。第一次观测脚本错误地只匹配 `/tmp`，修正为匹配系统临时目录后复测通过。
- 1.7.10 macOS ARM64 DMG `hdiutil verify` 为 `VALID`，SHA-256：`6e49114b72fed6ce55fa08e41e17e158e75aa013366da6a389db9d10796c246b`；DMG blockmap：`3044782621163ac16f13aeace1edb04b97e687ae8d4d243eba110021cb1d5bd0`；Windows x64 EXE SHA-256：`1a52f9e20074c79c6b5ff9cc7b3242844b418517390a487496d42068ffe996e9`；EXE blockmap：`79d787b084387f00a12f86576296c79026bd9fab7ec1ef2c17d6e44994d58619`。Windows 不代表真机通过。
- 打包态页面样式检查覆盖宽窗口、窄窗口、安全验证弹窗、右上角关闭图标、长文案换行、统一通知层和渠道状态弹窗，截图位于 `real-test-evidence/macos-1.7.10-final/`；未发现溢出或控件重叠。Windows 仍只记录 x64 交叉构建。

当前状态：`受阻：Chrome 失败态直接重试已实现并验证；真实 Cloudflare CAPTCHA、账号登录、核心接口校验和站点保存仍需用户本人完成。`

证据目录：`real-test-evidence/macos-1.7.10-current/`、`real-test-evidence/macos-1.7.10-packaged-current/`、`real-test-evidence/macos-1.7.10-final/`。

## 2026-08-05 1.7.9 Chrome Turnstile 登录、账号归属与窄窗口复核

- macOS 当前系统 Google Chrome 可被应用发现并以独立临时 Profile 启动；本机 CDP 连接和超时清理烟测通过，临时 Profile 无残留。应用不读取现有 Chrome Profile，不复制 Cookie、完整 localStorage、验证码 Token 或页面内容。
- 受控 Electron 页面已复核固定文案“需要完成安全验证”、Cloudflare Turnstile/Google Chrome 说明、右上关闭图标、`Esc`、暂不添加/开始登录按钮和统一消息通知；将主窗口缩到 720×520 后，弹窗长文案、按钮和关闭图标均在视口内，当前截图：`real-test-evidence/macos-1.7.9-current/20-security-dialog-narrow.png`、`real-test-evidence/macos-1.7.9-packaged-current/20-security-dialog-narrow.png`。
- 渠道状态强制刷新失败保留上次成功数据、选中项和详情，并通过局部重试恢复；不需要重新打开弹窗才能看到状态。
- 重新验证新增 profile 账号归属校验：Chrome 登录账号和站点保存用户名不一致时拒绝覆盖旧 Token；同地址不同用户名仍允许独立保存，同地址同用户名仍拒绝。
- 真实 Cloudflare CAPTCHA、账号登录、profile/Key/分组/倍率/统计核心接口校验和最终站点保存仍需用户本人在官方 Chrome 窗口完成；本轮不把控件显示、CDP 连接或网络恢复写成 CAPTCHA 成功。Windows 仅记录 x64 交叉构建。
- 1.7.9 macOS ARM64 DMG `hdiutil verify` 为 `VALID`，SHA-256：`d3faa8a6d35beeb4afccdf07ccd63d06579fdf13194fa7233349fd7fe95b9255`；DMG blockmap SHA-256：`60ce13bda1867ad895398b19d0adc86031ec93c8194079bce492c965652d308e`；Windows x64 NSIS 交叉构建通过，EXE SHA-256：`c4105b4f16aadebfcc98c3153c80d85a1ab138780bb6722ea139cc6886aed25e`；EXE blockmap SHA-256：`ba59997c10f29f7a65d46986f3fc3192c514f09b8ca142eb1146bdf73c2b77b0`。Windows 不代表真机通过。

当前状态：`受阻：Chrome 启动、CDP 隔离和 UI 已验证；真实 Cloudflare CAPTCHA、账号登录、核心接口校验和站点保存仍需用户本人完成。`

证据目录：`real-test-evidence/macos-1.7.9/`。

## 2026-08-04 1.7.8 Turnstile challenge 网络与退出复核

- 当前机器系统 DNS 对 `brunhild.challenges.cloudflare.com` 返回 `198.18.0.111`，直接 TLS 会失败；1.7.8 启动前使用 Cloudflare HTTPS DNS 公布的 IPv6 边缘提示 `2606:4700::6812:1092`，真实 Electron 官方窗口中的 challenge 请求不再出现 `ERR_CONNECTION_CLOSED`，可返回正常 `204/2xx`。
- 官方登录窗口仍在原站点同源临时 session 中打开，邮箱/密码由异步表单轮询填入，挑战响应为空时不提交；安全验证提示框右上角关闭图标、提示框 `Esc`、官方窗口原生关闭按钮和官方窗口 `Esc` 都只退出验证流程，不保存站点。
- 本轮没有点击或代做 CAPTCHA，没有提交真实账号密码，没有读取或保存 Chrome Cookie、验证码 Token、完整 localStorage/sessionStorage、完整表单值或站点凭据。
- 真实 Turnstile checkbox 的点击、挑战结果、登录、profile/Key/分组/倍率/统计核心校验和站点保存仍需用户本人完成；不能把网络恢复或控件显示写成真实认证成功。

## 当前状态

`受阻：challenge 网络已恢复，真实 CAPTCHA 和后续登录仍需用户本人在官方窗口完成`

证据目录：`real-test-evidence/macos-1.7.8/README.md`。

## 2026-08-04 1.7.7 Turnstile 异步表单填充复核

- 使用最新源码实例打开 `https://ai.maok.shop/login` 官方登录窗口；窗口显示 Cloudflare Turnstile “Verify you are human”，邮箱和密码已由应用填入脱敏值，登录按钮在挑战完成前保持禁用。
- 根因确认：站点 Vue 表单异步挂载，首个 `did-finish-load` 时输入框尚不存在；修复后轮询固定白名单选择器，表单出现后触发 `input`/`change`，不覆盖用户已有内容。
- 点击/代做 CAPTCHA、提交账号密码后的真实登录、profile/Key/分组/倍率/统计核心校验和站点保存本轮未完成；不得把“控件可见+字段已填”标为真实认证成功。
- Escape 关闭官方窗口后，安全验证弹窗退出且站点数不增加；主页面添加阶段恢复为“等待开始”。

## 当前状态

`受阻：Turnstile 控件和凭据填充已修复并复核，真实 CAPTCHA 和后续登录仍需用户本人在官方窗口完成`

证据目录：`real-test-evidence/macos-1.7.7/README.md`。

## 2026-08-04 1.7.6 Turnstile challenge 网络恢复与退出复核

- 使用 1.7.6 macOS ARM64 隔离 userData 启动打包应用，填写非真实诊断数据后，公开设置接口识别为 `Cloudflare Turnstile`，站点页显示统一安全验证弹窗；右上关闭图标、`Esc` 和“暂不添加”均保持站点数为 0。
- 启动官方登录窗口后，应用启动前的 Cloudflare challenge 主机解析规则使 `/login` 页面显示真实 `Verify you are human` checkbox；挑战请求不再立即出现 `ERR_CONNECTION_CLOSED`。本次只检查控件是否加载，不点击或代做 CAPTCHA，不读取 Cookie、Token、localStorage、sessionStorage 或完整表单值。
- 若系统代理路径仍返回可恢复的 challenge 传输错误，临时 session 会自动切换 `direct` 并重载官方登录页；两种路径均失败时不伪造成功，保留官方窗口/提示供用户修复网络后重试。
- 自动化门禁：42 个 Vitest 文件/286 项、`npm run format:check`、`npm run lint`、`npm run typecheck` 已通过；本轮最终 `build`、E2E、DMG/NSIS 和 SHA-256 在产物完成后补记。

## 当前状态

`受阻：Turnstile 控件已能加载，但真实 CAPTCHA、登录、核心接口校验和站点保存仍需用户本人完成`

证据目录：`real-test-evidence/macos-1.7.6/README.md`。

## 2026-08-04 1.7.5 Turnstile 重试与弹窗退出复核

- 代码侧已完成：Turnstile 挑战网络失败、官方窗口加载失败和超时会保留“需要完成安全验证”弹窗；用户可以修复系统代理后直接重试。用户点击右上角关闭图标、点击“暂不添加”或按 `Esc` 时退出流程，站点数量不增加。
- 受控 macOS ARM64 打包 Electron E2E 已验证关闭图标和 `Esc` 两条路径；未点击“开始验证”，没有代做验证码、复制 Cookie/Token 或保存站点。
- 自动化门禁：42 个 Vitest 文件/283 项、`npm run format:check`、`npm run lint`、`npm run typecheck`、`npm run build`、开发态 E2E 6/6、macOS 打包应用受控 E2E 6/6。
- 产物：DMG `3c13ae31b54dce57d4919970eacb01d3944e57c93da6200d829d181379fb76b3`，blockmap `db873b189d6084c68090f1152080d23cf710abb8a994f22d051dbac9f2cc7e9f`；Windows x64 EXE `17ce8051656184cd0eb40379658881dc473675b579bc173a15e69fddf360e2a6`，blockmap `4273c3717092451d83cc6fe9aff8e9d0f5f0ce5a4690e64916485993a64d1706`。Windows 仅交叉构建，不代表真机。
- 真实 `ai.maok.shop` Turnstile 仍受阻：当前机器将挑战域解析到 `198.18.0.111`，TLS 失败；应用不绕过或伪造挑战，状态继续为“受阻”，待用户网络或系统代理恢复后在官方窗口完成。

## 当前状态

`受阻：真实交互挑战尚未完成；1.7.5 客户端重试、关闭和双平台构建已完成`

## 2026-08-04 1.7.4 批量添加门禁与最终产物复核

- 1.7.4 新增批量添加安全门禁：每个 URL 先读取公开 GeeTest/Cloudflare Turnstile 开关；需要交互验证时按单项报告“需要单独完成安全验证”，不尝试密码登录或保存，其他 URL 继续处理。对应回归位于 `electron/main/services/site-service.integration.test.ts`。
- 自动化门禁：`npm run format:check`、`npm run lint`、`npm run typecheck`、`npm test -- --run`（42 个文件/281 项）、开发态 `npm run test:e2e`（6/6）、`npm run build`、`npm run pack`、`npm run dist:mac`、`npm run dist:win` 均通过。
- macOS ARM64 打包应用 E2E 6/6；受控验证弹窗沿用 `real-test-evidence/macos-1.7.3/18-turnstile-dialog-latest.png` 与 `19-turnstile-dialog-narrow-latest.png`，已确认动态显示 `Cloudflare Turnstile`、固定文案、按钮焦点、Escape 取消和窄窗口换行，不把受控证据写成真实挑战成功。
- macOS ARM64 DMG `hdiutil verify` 为 `VALID`，SHA-256 为 `78a7c6e8095153463cf40e530aaab136ef626e091e706628f2fb37c77f57a87c`；Windows x64 NSIS 交叉构建完成，EXE SHA-256 为 `9653364c70a6f13158e3f7bebb524043bc3714215e44b5c2570d00a110502494`，blockmap 与 PE/asar/入口检查通过。Windows 不作为真机验收。
- 真实 `ai.maok.shop` Turnstile 仍未完成：官方窗口可打开，但挑战域连接失败/验证未成功，未提交账号、未复制会话、未保存站点或半成品凭据；真实 GeeTest/Turnstile 认证和依赖真实账号的核心校验继续为“受阻”。
- `python3 scripts/check_real_test_checklist.py liran_docs/09-真机实测.md --require-complete` 应继续返回退出码 `1`，因为当前状态仍为“受阻”；不可为通过挑战而改写清单状态。

## 2026-08-04 1.7.3 交互会话与站点隔离收口

- 本轮新增证据目标：access-only Token、官方窗口同源重定向拦截、重新验证失败回滚和删除站点级残留清理；42 个 Vitest 文件/280 项、Electron E2E 6/6、生产构建和双平台产物结构检查均通过。
- 最新 1.7.3 macOS ARM64 打包应用添加链路复测：站点页显示固定“需要完成安全验证”文案并动态显示 `Cloudflare Turnstile`；点击“开始验证”后官方 `/login` 窗口打开，Turnstile 响应字段为 1、挑战 iframe 为 0，`Sign In` 保持禁用，未保存站点。
- 最新打包应用受控 UI 复核：1152×867 下弹窗 `440×198`，720×640 下弹窗 `440×169`；两种视口均无溢出，焦点落在“开始验证”，Escape 关闭后站点数为 0。截图：`real-test-evidence/macos-1.7.3/18-turnstile-dialog-latest.png`、`19-turnstile-dialog-narrow-latest.png`。
- 受控 macOS 页面检查同时保留 `real-test-evidence/macos-1.7.2/` 历史证据和最新 `real-test-evidence/macos-1.7.3/` 的验证弹窗、窄窗口、焦点和 Escape 证据；不能将受控页面证据写成真实 GeeTest/Turnstile 成功。
- 真实站点完整登录、挑战成功后的 profile/Key/分组/倍率/统计校验仍待用户本人在官方窗口完成；若挑战域继续 `ERR_CONNECTION_CLOSED`，状态保持“受阻”。
- Windows 只做 `dist:win` x64 NSIS 交叉构建和 PE/asar/入口检查，不作为 Windows 真机验收。
- 新增公开只读证据：`real-test-evidence/macos-1.7.3/README.md`。Chrome 页面有 1 个 Turnstile iframe 和 1 个 `cf-turnstile-response` 字段，但挑战域返回 `net::ERR_CONNECTION_CLOSED`；未提交账号、未读取会话状态，真实登录仍未完成。
- 只读网络复核显示目标登录页 HTTP 200，而挑战子域 TLS 无法完成，公开 Cloudflare 边缘地址返回 HTTP 522；本轮未修改系统代理、TUN、DNS 或应用路由。
- 1.7.3 macOS ARM64 打包应用真实窗口已打开官方登录页并显示 Turnstile checkbox；在临时隔离窗口中输入凭据后连续两次按官方控件均返回 `Verification failed`，`Sign In` 保持禁用，未进入核心接口校验，未保存站点或半成品凭据。
- 最新产物摘要：macOS DMG `5838392816557565d03ad99d124408185f16b3ef87c0a3525445aa8e847543d1`，macOS blockmap `f9886a61a160ca5b1abe08c9b6e979784fec9f5ab40f04d6f00c6ff119b5d780`；Windows EXE `9a99ca3051777826ec253de5a65e79e8b4cdb771c81086b49501e3daf1edc189`，Windows blockmap `77d4b7595eae39cbba43807f4bde53dbd25f095511cd987646906570cd6ca195`。
- `python3 scripts/check_real_test_checklist.py liran_docs/09-真机实测.md --require-complete` 返回退出码 `1`，原因是当前状态仍为“受阻”；这是预期的真实挑战门禁结果。

## 2026-08-04 1.7.2 渠道回退与双平台验证记录

- 自动化门禁：`npm run format:check`、`npm run lint`、`npm run typecheck`、`npm test -- --run`（42 个文件/278 项）、`npm run test:e2e`（6/6）、`npm run build`、`npm run pack`、`npm run dist:mac`、`npm run dist:win` 均通过。
- macOS ARM64 打包应用受控页面：官方验证提示动态显示 Cloudflare Turnstile；宽屏与窄屏弹窗均为 `440×198`，均在视口内；默认焦点落在“开始验证”，Escape 关闭后站点数保持 `0`。截图：`real-test-evidence/macos-1.7.2/01-turnstile-dialog-wide.png`、`02-turnstile-dialog-cancelled.png`、`03-turnstile-dialog-narrow.png`。
- 渠道回退：Renderer 定向测试与 Electron E2E 已覆盖强刷失败保留旧渠道/详情、stale 提示和重试；真实打包应用未伪造渠道服务响应，待用户在真实站点完成两轮挑战后补充账号相关证据。
- macOS ARM64 DMG：`hdiutil verify` 为 `VALID`；SHA-256 `c3076c0824f20025fe6047a61208521cbc3d1f7addaddd4054f0269b3ed22fe`；blockmap `592275b50e2e77c6e2498fc9179d7aac71e4a291bc211598eab96f09bb24e798`。
- Windows x64：NSIS、PE/asar/版本 `1.7.2` 和入口结构检查通过；EXE SHA-256 `8075bf5dc76394ecdd1bb2800e3266368ec38ed29ac3cdb05bcc34e2eefb5e91`；blockmap `bf90f3f816934d11fea7b6465b5ef8aa26285b74726772758a4ca0d8e95f379f`。无 Windows 真机，不标记真机通过。
- 正式发布：未执行 `release:publish`、双远端推送或 GitHub Release，因此未生成/验证 `update-manifest.json`。
- 真实 GeeTest/Turnstile challenge 仍需用户本人在官方登录窗口完成；挑战域网络阻断或用户未介入时保持“受阻”，不以自动化代做。
- 清单门禁 `python3 scripts/check_real_test_checklist.py liran_docs/09-真机实测.md --require-complete` 已执行；因真实挑战和用户介入仍未完成，按规则返回退出码 `1`，未把本轮标记为“真机测试通过”。

## 2026-08-03 1.7.1 Turnstile challenge 兼容性复测

本轮只验证客户端兼容性，不代做验证码。隔离 macOS ARM64 打包应用在官方登录窗口加载 `https://ai.maok.shop/login`，检查 challenge iframe、challenge 域请求和登录按钮状态；不得复制 Chrome Cookie、验证码令牌、完整 localStorage 或页面内容。

| 检查项                                                          | 结果                                                                                                                  |
| --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| 新包 User-Agent 去除 Electron 标记                              | ✅；并进一步移除紧邻 Chrome 的应用产品标记，窗口 UA 为纯 Chrome 形态                                                  |
| Turnstile challenge iframe/网络请求                             | 受阻；iframe 为 0，`brunhild.challenges.cloudflare.com` 返回 `ERR_CONNECTION_CLOSED`，确认不再是 Electron UA 标记问题 |
| 用户本人完成官方挑战后的核心接口校验与原子保存                  | 待用户介入，不得自动代做                                                                                              |
| 同地址不同账号、同地址同账号、通知、渠道 stale-while-revalidate | 由既有 1.7.0 自动化与受控打包证据覆盖，待本轮全量复核                                                                 |

1.7.1 复测结论：对话框动态显示 `Cloudflare Turnstile`，官方窗口按安全边界打开；UA 调整后 challenge 仍被外部网络关闭。真实验证码和依赖账号的核心接口保存仍需用户本人在官方窗口完成，当前不能标记为通过。

受控页面样式证据：使用本地公开设置响应启动 1.7.1 macOS 打包应用，弹窗为 `440×198`，两个按钮均为 `88×36`，默认焦点在“开始验证”；Escape 关闭后站点数量保持 0。该检查不提交真实账号，不替代官方 Turnstile 真机挑战。

1.7.1 产物证据：DMG `hdiutil verify` 为 `VALID`；macOS ARM64 DMG SHA-256 为 `61b51459e1d7451e51ce6b7811809d682d73429bf62884469050f04f7d2cbca4`，Windows x64 NSIS SHA-256 为 `dae9236eebf3e8312d8f8d985f20dac6990f36e4bba72d263717c0f8e74a3c9a`。未执行发布命令，因此 `update-manifest.json` 未生成；Windows 仅交叉构建。

## 2026-08-03 1.7.0 真机验收结果与证据边界

验收责任为 Codex：macOS Apple Silicon 负责打包应用功能、窗口、通知、窄窗口和页面样式；Windows 只负责 x64 NSIS 交叉构建、PE/asar/版本/入口结构。真实 GeeTest 或 Turnstile 挑战只能由用户本人在官方登录窗口完成，不能用自动化代做或把未完成步骤标记为成功。

本轮新鲜门禁：Prettier、ESLint、TypeScript、Vitest 41 个文件/271 项、生产构建、开发态 Electron E2E 6/6、macOS ARM64 打包应用 E2E 6/6 均通过。DMG `hdiutil verify` 为 `VALID`；Windows NSIS、PE/asar 和版本入口结构已检查，但没有 Windows 真机证据。页面截图已重新生成到 `real-test-evidence/macos-1.7.0/`，只包含本地受控数据。

| 序号 | 操作与期望                                                                                      | 证据                                                                                                  | 状态 |
| ---: | ----------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ---- |
|    1 | 启动本轮 macOS ARM64 包，进入站点管理并检查普通添加路径                                         | 打包 E2E 6/6；`04-sites-settings.png`                                                                 | ✅   |
|    2 | 受控本地站点触发 GeeTest/Turnstile 检测，检查固定文案、两个按钮、焦点、Escape、窄窗口换行       | Renderer/服务测试；对话框未在本轮打包真机中触发                                                       | 受阻 |
|    3 | 暂不添加、关闭、超时和核心验证失败均不留下站点或覆盖凭据                                        | 服务集成、策略和通知测试                                                                              | 受阻 |
|    4 | 用户本人在官方登录窗口完成需要的验证码，验证成功自动继续添加；窗口使用 520×720 左右原生模态样式 | 官方挑战必须由用户完成                                                                                | 受阻 |
|    5 | 让交互站点会话失效，确认 `auth-required` 与主动重新验证只更新原站点                             | `site-service.integration.test.ts`；打包交互未使用真实账号                                            | 受阻 |
|    6 | 手动选择渠道后观察两个自动轮询周期；失败保留旧摘要和关联，恢复清除警告                          | `03-channels.png`、`09-channel-status-popover.png`、`17-channel-manual-association.png`；受控打包证据 | ✅   |
|    7 | 检查右上统一通知在宽/窄窗口、长文案、减少动画下无重叠且不抢焦点                                 | `04-sites-settings.png`、`10-overview-wide-footer-layout.png`、`11-overview-narrow-footer-layout.png` | ✅   |
|    8 | 扫描 SQLite、日志、临时目录和证据，不含密码、Token、Cookie、验证码结果                          | 修改文件字面量扫描、生产代码 token/cookie 扫描、`git diff --check`；未发现用户敏感值                  | ✅   |
|    9 | Windows x64 交叉构建并检查 NSIS/PE/asar/blockmap/manifest                                       | `release/`、`file`、asar 入口、SHA-256；本轮未生成正式 manifest，非真机                               | 受阻 |

1.7.0 当前结论：代码、自动化回归、可控 macOS 打包页面和双平台结构检查已完成；真实安全挑战以及依赖用户账号的交互打包验收保持“待用户介入”，用户完成挑战后再由 Codex 继续复测。正式发布前还需按项目统一发布命令生成并校验 `update-manifest.json`，不能将当前 `release/` 目录当作已发布版本。

## 当前状态

`受阻`

## 2026-07-29 1.6.0 真机验收结果

macOS Apple Silicon 可见 Electron 应用完成真实验收。真实凭据只在运行时输入，未写入命令、文档、日志、截图或测试夹具；GeeTest 挑战由用户本人完成，Codex 随后继续自动保存、页面、轮询和重启恢复验收。

| 序号 | 操作与期望                                                                             | 证据要求                                 | 状态 |
| ---: | -------------------------------------------------------------------------------------- | ---------------------------------------- | ---- |
|    1 | 从本轮 macOS ARM64 构建启动，进入站点管理，普通站添加路径无回归                        | 版本 v1.6.0、ARM64、可见窗口             | ✅   |
|    2 | 对已启用 GeeTest 的授权站点击“添加并验证”，显示精确文案及两个按钮                      | 可见 Electron 对话框，无账号密码         | ✅   |
|    3 | 暂不添加、Escape、关闭验证窗口和超时均不留下站点/凭据                                  | Escape/关闭真机；取消/超时集成测试       | ✅   |
|    4 | 开始验证后打开 520×720 左右原生模态窗口，用户完成挑战，验证成功后自动继续保存          | 用户完成人机验证；自动添加并读取核心数据 | ✅   |
|    5 | 重启应用后站点可恢复；会话失效时先 refresh，失败后只显示需重新验证                     | 同一隔离 userData 重启恢复；生命周期测试 | ✅   |
|    6 | 检查 loading/success/info/warning/error 通知、长短文案、关闭/操作、计时与键盘          | 可见宽窗/窄窗与组件/E2E，无重叠溢出      | ✅   |
|    7 | 手动选择渠道后观察至少两个自动轮询周期；一次失败时旧数据和关联持续显示，恢复后警告消失 | 两个真实 60 秒周期；失败/恢复由可控测试  | ✅   |
|    8 | 隐藏/恢复窗口，确认隐藏无请求、恢复且缓存 >30 秒立即刷新                               | 调度 fake clock 与 Electron E2E          | ✅   |
|    9 | 扫描日志、SQLite、应用数据和证据，确认无密码、Token、Cookie、验证码结果与原始 IPC 错误 | 仓库/运行数据脱敏扫描及安全契约测试      | ✅   |

Windows 已完成 x64 NSIS 交叉构建、PE/asar/版本/入口和必要更新资产检查；未做 Windows 真机，不将交互、安装或缩放表述为真机通过。macOS 对话框、通知位置、官方登录窗口、真实核心数据、手动关联、两个轮询周期和重启恢复均通过。

发布证据：GitHub Release `1.6.0` 为 stable、非草稿、非预发布，包含 macOS ARM64 DMG、Windows x64 NSIS、两个 blockmap 和 `update-manifest.json`。远程 manifest 中 macOS DMG SHA-256 为 `a913239d462b6db46d77c66e7e0cef77f50a33fbf25cee01661d9e0bbeba7a97`，Windows EXE SHA-256 为 `35cd3ad40bb031084217bb02032c182c5bbc1ed3e93b505ea7d3a413be92f74f`；统一发布命令已清理本地五个临时发布资产。

## 2026-07-27 1.5.2 渠道关联入口验收

| 项目       | 验收重点                                                     | 状态                                                         |
| ---------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 渠道页入口 | 渠道状态页不再显示 Key 分组关联面板和复选框                  | ✅；打包 E2E `03-channels.png`                               |
| 弹层入口   | 全部站点点击“查看渠道状态”后，每个渠道卡片右上角显示关联按钮 | ✅；打包 E2E `09-channel-status-popover.png`                 |
| 多渠道切换 | 逐个关联、取消关联，多个渠道可同时保持“已关联”               | ✅；打包 E2E `17-channel-manual-association.png` 与 IPC 回读 |
| 交互隔离   | 点击关联按钮不改变当前详情渠道；点击卡片主体仍可查看详情     | ✅；打包 E2E 渠道详情断言                                    |
| 失败回滚   | 模拟保存失败时按钮恢复原状态并显示失败消息                   | 未单独验证；内部异常分支已实现                               |
| 窄窗口     | 720px 左右窗口、长渠道名和多渠道列表无重叠或溢出             | ✅；打包 E2E 受影响页面截图，未单独记录精确 720px            |

本轮 macOS ARM64 打包应用证据目录：`/tmp/sub2api-real-evidence-1.5.2/`。Windows 仍仅交叉构建，未执行真机验收。

## 2026-07-25 1.4.5 真机验收计划

| 项目             | 验收重点                                                                    | 状态   |
| ---------------- | --------------------------------------------------------------------------- | ------ |
| API 密钥菜单宽度 | 展开后长分组名称、平台和倍率完整可读，菜单不越出视口                        | 待实测 |
| 版本徽标         | 主界面右上角并列显示“最后更新：时间”和 `v1.4.5`，图标、文字和窗口控制不重叠 | 待实测 |

1.4.5 DMG 挂载应用通过 API 密钥、总览、使用记录、渠道状态和悬浮窗打包 E2E 6/6；页面证据位于 `real-test-evidence/macos-1.4.5/14-api-keys-wide.png`、`15-api-keys-narrow.png`、`10-overview-wide-footer-layout.png` 和 `11-overview-narrow-footer-layout.png`。

## 2026-07-24 1.4.4 真机验收计划

本轮功能已通过内部 Prettier、ESLint、TypeScript、Vitest 35 文件/219 项和 1.4.4 DMG 挂载应用打包 E2E 6/6；以下项目仍需真实交互逐项记录，未执行前标记为“待实测”：

| 项目             | 验收重点                                           | 状态   |
| ---------------- | -------------------------------------------------- | ------ |
| API 密钥分组菜单 | 名称/平台/倍率分栏、键盘操作、外部点击关闭         | 待实测 |
| 站点备注         | API 密钥、使用记录、渠道状态顶部选择器优先显示备注 | 待实测 |
| 总览充值比例     | 宽窄卡片文字完整、无遮挡和重叠                     | 待实测 |
| 无渠道倍率推荐   | 候选参与但显示无渠道状态，不显示稳定               | 待实测 |
| 悬浮窗额度       | 有限额 Key 与无限额 Key 口径正确                   | 待实测 |

1.4.4 构建证据：DMG `37426653edaa32c51605eb749953e334fe0cff510796e05e0ea41a4b316d6a61`，Windows NSIS `f5d527c91c752d3946ef9e9dfc6e48f4224e8313468cf491309ec0b7979cc9da`。DMG 镜像校验、macOS 构建目录 ad-hoc bundle 校验和 Windows PE 结构检查已通过；安装副本与真实站点交互尚未在本轮完成，不能标记为真机通过。

## 2026-07-24 1.4.3 API 密钥页面与悬浮窗统计验收

- 自动化：Prettier、ESLint、TypeScript、Vitest 34 文件/215 项、开发态目标 Electron E2E 1 项通过；完整安装态 E2E 尚未在本轮重新执行。
- 页面：`real-test-evidence/macos-1.4.3/` 的宽窄 API 密钥截图已脱敏；完整 Key 展示、主进程复制、分组选项平台/倍率和悬浮窗当前 Key 统计已在安装副本检查。
- 真实站点：`panel.hanhegufei.online` 完成 Key 分组切换、回读和恢复；`walkai.top` 登录和只读能力可用但管理端点部分不支持；`ai.maok.shop` 因 Turnstile verification failed 阻断。
- 结构：重新构建的 1.4.3 DMG 通过 `hdiutil verify`，`/Applications` 安装副本严格签名通过；Windows 仅完成 x64 NSIS 交叉构建和 PE/asar 结构验证。
- 发布边界：macOS 为 ad-hoc 签名且未公证；完整三站真机通过不成立，maok 保持受阻记录。

## 2026-07-24 1.4.1 macOS 安装启动修复验收

- 根因：`1.4.0` 关闭签名身份发现后只保留 Electron linker-generated 签名，`codesign --verify --deep --strict` 报 bundle 资源未封装，下载后带隔离属性的应用可能被系统判定为损坏或无法打开。
- 修复：`package.json` 的 macOS 构建显式使用 `identity: "-"`，由 electron-builder 对主程序、Helper、Framework 和资源完成整包 ad-hoc 签名；没有对成品 App 手工 `codesign --deep`。
- 结构：`1.4.1` DMG 通过 `hdiutil verify`；镜像内 App 与 `/Applications` 安装副本均通过严格签名校验，标识为 `com.liran.sub2api.monitor`，版本/asar 为 `1.4.1`，主程序为 ARM64。
- 启动：保留用户原有 Application Support 数据，经 LaunchServices 打开安装副本，前台进程、Renderer 和可见主窗口正常；旧 `1.4.0` 安装副本移动到 `/tmp/sub2api-installed-backup-1.4.0-20260724/`，未删除。
- 自动化：Prettier、ESLint、TypeScript、Vitest 34 文件/210 项、开发 E2E 6/6、最终安装副本 E2E 6/6、npm audit 0 漏洞。
- 发布边界：Windows `1.4.1` 仅完成 x64 NSIS 交叉构建和 PE/asar/版本审计；macOS 不是 Apple Developer ID 签名且未公证。

## 2026-07-24 API 密钥与监控联动验收

| 步骤 | 实际操作                                                                | 结果                                          | 证据                                                        |
| ---- | ----------------------------------------------------------------------- | --------------------------------------------- | ----------------------------------------------------------- |
| 1    | 启动 `/Applications/看看你还有💰吗？.app` 1.4.3 安装副本并进入 API 密钥 | 通过                                          | `real-test-evidence/macos-1.4.3/README.md`                  |
| 2    | 在 API 密钥页查看完整 Key、点击复制并读取系统剪贴板                     | 通过；截图前已脱敏                            | `real-test-evidence/macos-1.4.3/api-keys-wide-masked.png`   |
| 3    | 检查分组下拉项的名称、平台和倍率                                        | 通过；实测 Claude/OpenAI 和倍率               | `real-test-evidence/macos-1.4.3/README.md`                  |
| 4    | 将窗口调整到窄尺寸并检查表格承载                                        | 通过；横向滚动正常                            | `real-test-evidence/macos-1.4.3/api-keys-narrow-masked.png` |
| 5    | 切换悬浮窗并查看今日 Token/今日消费                                     | 通过；显示当前 Key 统计                       | `real-test-evidence/macos-1.4.3/README.md`                  |
| 6    | walkai.top 普通用户接口验证                                             | 部分通过；API Key 管理端点不支持              | `real-test-evidence/macos-1.4.3/README.md`                  |
| 7    | panel.hanhegufei.online Key 分组切换、回读、恢复                        | 通过                                          | `real-test-evidence/macos-1.4.3/README.md`                  |
| 8    | ai.maok.shop 登录和 API 验证                                            | 受阻；HTTP 400，Turnstile verification failed | `real-test-evidence/macos-1.4.3/README.md`                  |

本节记录唯一 `/goal` 的新增验收证据。macOS 使用 `1.4.3` ARM64 打包应用；真实站点脚本对两个授权站执行可恢复分组写入，第三站因 Turnstile 仅保留用户已有浏览器会话的只读页面证据。Windows 仅为交叉构建。

| 序号 | 真实操作                              | 期望结果                                       | 平台/证据                                              | 状态       |
| ---: | ------------------------------------- | ---------------------------------------------- | ------------------------------------------------------ | ---------- |
|    1 | 从新版本 DMG 解包并启动主窗口         | 版本、架构、入口均为本轮新版本                 | `release/`、DMG verify、打包 E2E                       | ✅         |
|    2 | 检查左侧导航并进入 API 密钥           | 顺序正确，页面使用全局当前站点                 | `real-test-evidence/macos-1.4.0/14-api-keys-wide.png`  | ✅         |
|    3 | 在真实授权站点分页、搜索、筛选 Key    | 多页完整，所有 Key 仅脱敏                      | `scripts/verify-real-sites.mjs`、打包 E2E              | ✅         |
|    4 | 切换一个获授权 Key 分组并刷新远程网页 | App 与网页 group 一致；原分组恢复              | 两站 `verified-and-restored` 脱敏运行记录              | ✅         |
|    5 | 检查今日/近30天用量的正常与局部失败   | 真实值显示；失败项为待查询                     | adapter/service 测试、打包 E2E                         | ✅         |
|    6 | 依次改变使用记录所有筛选              | 无需点刷新，列表和顶部统计同条件               | `real-test-evidence/macos-1.4.0/02-usage.png`、Vitest  | ✅         |
|    7 | 切换各站手动/自动当前 Key             | 顶部和卡片按当前 Key 重算，无整站兜底          | `real-test-evidence/macos-1.4.0/06-overview-quota.png` | ✅         |
|    8 | 打开渠道页并验证轮询周期              | 默认60秒、旧数据保留、更新时间/倒计时正确      | `channel-polling.test.ts`、打包应用 `03-channels.png`  | ✅         |
|    9 | 最小化、隐藏、切后台、恢复            | 暂停请求；恢复且缓存>30秒立即刷新              | 可见性单测与 Electron E2E                              | ✅         |
|   10 | 触发局部失败/不支持/无关联/歧义       | 文案正确，非健康失败不显示异常                 | ranking/loader 测试、打包 E2E                          | ✅         |
|   11 | 检查渠道页、弹层和关联摘要            | 无折算图标/倍率徽标/相关文案；独立倍率功能仍在 | `03-channels.png`、`09-channel-status-popover.png`     | ✅         |
|   12 | 检查窄窗口 Key 表格与所有受影响页     | 可横滑、无重叠/竖排/跳动、字号可读             | `15-api-keys-narrow.png`                               | ✅         |
|   13 | 扫描打包应用数据、日志、截图和 SQLite | 不含完整 Key、Token、密码                      | 安全 grep、测试夹具与截图检查                          | ✅         |
|   14 | 构建并审计 Windows x64 NSIS           | PE/asar/版本/入口正确，只记交叉构建            | `release/`、PE/asar/file 输出                          | ✅；非真机 |

本轮完成后运行 `python3 scripts/check_real_test_checklist.py liran_docs/09-真机实测.md --require-complete`；第三站 Turnstile 和 Windows 非真机边界不得改写。

上级：[[08-测试用例]]、[[04-开发追踪]]
下级：无
依赖：内部测试通过的 macOS 应用、API 密钥 Codex UI 路线及受影响旧壳接收检查通过、真实授权环境或明确标记的本地 mock 环境

## 2026-07-21 1.3.9 倍率标题精简验收

- 验收责任：Codex；macOS Apple Silicon 真机运行最终 1.3.9 ARM64 打包应用，使用隔离 userData 和本地 sub2api mock。
- 页面：倍率区域不再显示“倍率对比”主标题或旧副标题，刷新周期、刷新按钮和四平台卡片均正常。
- 自动化：完整 Vitest 28 文件/171 项、生产构建、开发定向 E2E 和最终 macOS 打包应用 E2E 通过。
- 发布结构：DMG 通过 `hdiutil verify`，macOS 主程序为 Mach-O ARM64，Info.plist/asar 为 1.3.9；Windows NSIS 为 PE32、解包主程序为 PE32+ x86-64。Windows 未执行真机。

结论：1.3.9 macOS 打包应用页面检查通过；Windows 仅完成 x64 交叉构建和结构验证。

## 2026-07-21 1.3.8 官网 SVG 与标题精简验收

- 验收责任：Codex；当前 macOS Apple Silicon 真机运行最终 1.3.8 ARM64 打包应用，使用隔离 userData 和本地 sub2api mock。
- 自动化：完整 Vitest 28 文件/171 项、生产构建和最终 macOS 打包应用 Electron E2E 6/6 通过。
- 页面：倍率区域只显示“倍率对比”主标题，不再显示“按充值比例折算后，比较各平台最低分组”。
- 图标：OpenAI、Claude、Gemini、Grok 均使用官网下载的本地 SVG；1600px 与 720px 下边缘清晰，无模糊、裁切、加载失败、重叠或布局偏移。
- 证据：`real-test-evidence/macos-1.3.8/01-overview.png` 至 `13-rate-comparison-stitch-narrow.png`，重点为 `12-rate-comparison-stitch-wide.png` 和 `13-rate-comparison-stitch-narrow.png`。
- 发布结构：DMG 通过 `hdiutil verify`，macOS 主程序为 Mach-O ARM64，Info.plist/asar 为 1.3.8；Windows NSIS 为 PE32、解包主程序为 PE32+ x86-64，两端 asar 为 1.3.8。

结论：1.3.8 macOS 打包应用真机和页面样式检查通过。Windows 仅完成 x64 交叉构建与结构验证，未执行 Windows 真机安装、启动或交互验收。

## 2026-07-21 1.3.7 倍率对比 Stitch 页面验收

- 验收责任：Codex；当前 macOS Apple Silicon 真机运行最终 1.3.7 ARM64 打包应用，应用仅 adhoc 签名、未公证，使用隔离临时 userData 和本地 sub2api mock，不污染正式站点。
- 自动化：倍率规则 26/26、完整 Vitest 28 文件/171 项、开发 Electron E2E 6/6、最终 macOS 打包应用 Electron E2E 6/6 通过。
- 宽屏：1600px 下 OpenAI、Claude、Gemini、Grok 四卡按固定顺序完整显示，圆角、间距、品牌图标、状态与推荐区基线一致，无重叠和图片加载失败。
- 窄屏：720px 下四卡仍保持单行，第二张卡进入可横向滑动区域；无可见滚动条、没有第二行、页面本身无异常横向撑破。
- 业务：Antigravity 统一进入 Gemini 候选池；现有渠道状态、五分钟稳定门槛、真实接口接线、缓存和刷新行为由集成 E2E 回归覆盖。
- 证据：`real-test-evidence/macos-1.3.7/01-overview.png` 至 `13-rate-comparison-stitch-narrow.png`；倍率宽/窄重点证据为 `12-rate-comparison-stitch-wide.png` 与 `13-rate-comparison-stitch-narrow.png`。
- 发布结构：DMG 通过 `hdiutil verify`，macOS 主程序为 Mach-O ARM64，Info.plist/asar 为 1.3.7；Windows NSIS 为 PE32、解包主程序为 PE32+ x86-64，两端入口和内部版本完整。

结论：1.3.7 macOS 最终打包应用真机及页面样式检查通过。Windows 仅完成 x64 NSIS 交叉构建和结构验证，未执行 Windows 真机安装、启动或交互验收。

## 2026-07-21 1.3.6 站点卡片 footer 与页面样式验收

- 验收责任：Codex；当前 macOS Apple Silicon 真机运行最终未签名、未公证的 1.3.6 ARM64 打包应用，使用隔离临时 userData 和本地 sub2api mock，不污染正式站点。
- 自动化：Prettier、ESLint、TypeScript、Vitest 28 文件/168 项、生产构建、开发 Electron E2E 6/6 和最终打包应用 Electron E2E 6/6 通过。
- 几何：五张站点卡片在 1600px 窗口形成四列首行，720px 最小窗口为单列；每卡三个 footer 控件同一行，边界、scrollWidth/scrollHeight、同排摘要/footer 顶边和跨排底部偏移均满足门槛。
- 状态：加载、成功、当前分组无匹配、渠道列表错误和详情错误摘要外框等高；长站点名、长备注、额度、充值比例待设置和 `1:10` 均未破坏布局。
- 页面样式：宽屏长站点名安全省略并保留 title，刷新徽标不竖排；窄屏充值比例值完整可读，两个查看按钮文字完整，当前浅色、圆润和绿色/蓝色按钮方案保持一致，无重叠、第二行或横向溢出。
- 证据：`real-test-evidence/macos-1.3.6/01-overview.png` 至 `11-overview-narrow-footer-layout.png`，重点为 `10-overview-wide-footer-layout.png` 和 `11-overview-narrow-footer-layout.png`。
- 发布结构：DMG `hdiutil verify` 有效；macOS 主程序为 Mach-O ARM64，Info.plist/asar 为 1.3.6；Windows NSIS 为 PE32、解包主程序为 PE32+ x86-64，两端 Renderer/main/preload 入口完整。

结论：1.3.6 macOS 打包应用真机和页面样式检查通过。Windows 只完成 x64 交叉构建及结构验证，未执行 Windows 真机安装、启动或交互验收。

## 2026-07-21 1.3.5 严格渠道门槛与单推荐验收

- 验收责任：Codex；环境为当前 macOS Apple Silicon 真机、未签名且未公证的 `1.3.5` ARM64 打包应用，测试使用隔离 userData。Windows 只做 x64 交叉构建和结构证据，不冒充真机。
- 真实站点：三个授权站点重新完成只读登录、profile、Key、今日统计、usage/筛选、倍率、渠道列表和渠道详情验证，倍率分组数为 17/8/25；凭据仅运行时注入，文档和截图不包含账号、密码、Token、Cookie、完整 Key 或完整敏感响应。
- 严格关系：实测聚合继续保持保守结果，未唯一匹配和歧义分组不进入推荐；无渠道状态不使用中性分兜底。三个站点的五分钟合格数会随实时渠道记录变化，页面只展示当次核验中满足全部 hard gate 的候选。
- 业务结果：当前生效 Key 渠道摘要位于站点 footer 上方；渠道页和弹窗按渠道自身分组显示折算倍率；五分钟全 normal 硬门槛、实际平台识别、min-max 价格分、60/40 综合分和每平台单推荐均由单测及本地完整链路覆盖。
- 异步结果：渠道列表/详情共享缓存、single-flight、并发 4 和 revision；Key/站点切换清除旧摘要。修复悬浮窗自动切站继承旧站点 `refreshing` 后，打包应用中刷新按钮恢复可用，点击后正确禁用并在请求结束后恢复。
- 页面样式：OpenAI、Claude、Gemini、Grok 固定为浅绿、浅橙、浅蓝、黑灰首四列且保持单行；站点卡片同排等高，充值比例、查看倍率和查看渠道状态固定底部；摘要、徽标、长文本、渠道时间线和弹窗内部滚动没有重叠或遮挡。
- 自动化：ESLint、TypeScript、Vitest 28 文件/168 项、生产构建、开发目标主链路和 macOS 打包应用 Electron E2E 6/6 通过。页面证据为 `real-test-evidence/macos-1.3.5/01-overview.png` 至 `09-channel-status-popover.png`。
- 发布结构：DMG 通过 `hdiutil verify`；macOS 主程序为 Mach-O ARM64，Info.plist 与 asar 为 `1.3.5`；Windows NSIS 为 PE32，解包主程序为 PE32+ x86-64，两端 asar 入口完整。`release/` 仅保留当前四个分发文件和构建元数据，SHA-256 见 `CHANGELOG.md`。

结论：`1.3.5` macOS 打包应用真机测试和页面样式检查通过。Windows 未执行安装、启动或交互真机验收，仅交叉构建与结构验证通过。

## 2026-07-20 1.3.4 倍率卡片内联渠道状态与视觉验收

- 环境：macOS 26.5，Apple Silicon；开发 Electron 与未签名 `1.3.4` ARM64 打包应用，隔离 userData。
- 自动化：Prettier、ESLint、TypeScript、Vitest 28 文件/154 项；开发 Electron E2E 6/6；`1.3.4` 打包应用 E2E 6/6。
- 页面样式：默认窗口检查 OpenAI/Claude/Gemini/Grok 固定一行及浅绿/浅橙/浅蓝/黑灰主题；窄窗口检查不换行、横向滚动、加载动画、错误重试和长文本截断。
- 交互：倍率卡片无需点击即可显示匹配渠道状态、7 天可用率和时间线；无渠道状态显示标签；点击站点级入口才加载完整渠道弹窗，弹窗 UI 与渠道状态壳一致。
- 页面证据：`real-test-evidence/macos-1.3.4/01-inline-channel-overview.png`、`02-inline-channel-retry.png`、`03-channel-status-popover.png`、`04-narrow-window.png`、`06-horizontal-scroll-grok.png`。
- 发布结构：DMG `hdiutil verify` 通过；macOS Mach-O ARM64；Windows NSIS/PE32、主程序 PE32+ x86-64；两端 asar `1.3.4` 且入口完整。Windows 不作真机结论。

结论：`1.3.4` macOS 真机页面、交互和打包应用 E2E 通过；Windows 仅交叉构建与结构验证。

## 2026-07-20 1.3.3 键盘入口与最终发布验收

- 环境：macOS 26.5，Apple Silicon，未签名 `1.3.3` ARM64 打包应用；临时 userData，本地集成站点不污染正式数据。
- 键盘：倍率平台横向列表具有 `tabindex=0`、可访问名称和清晰焦点轮廓，E2E 已验证焦点入口存在；触控和鼠标横向滚动保持不变。
- 自动化：Vitest 27 文件/147 项；开发 Electron E2E 6/6；macOS 打包应用 E2E 6/6。
- 真实站点：三个授权站点最终只读复验登录、核心数据、使用记录、筛选、倍率、渠道列表和详情全部成功；倍率分组数为 18/8/22，未写入账号、密码、Token 或完整响应。
- 页面证据：`real-test-evidence/macos-1.3.3/08-rate-comparison.png`、`09-channel-status-popover.png`、`05-floating.png`。
- 发布结构：DMG 校验有效；macOS 主程序为 Mach-O ARM64；Windows NSIS 为 PE32、解包主程序为 PE32+ x86-64；两端 asar 版本为 `1.3.3` 且包含 Renderer/main/preload。
- 结论：`1.3.3` macOS 真机测试通过。Windows 仅完成交叉构建和结构验证，未执行 Windows 真机。

## 2026-07-20 1.3.2 稳定性边界修复验收

- 环境：macOS 26.5，Apple Silicon，未签名 `1.3.2` ARM64 打包应用；临时 userData，本地集成站点不污染正式数据。
- 修复：最近五分钟时间线含 unknown，或新鲜时间点与当前 degraded/failed 状态矛盾时，不再误判“稳定”。
- 自动化：Vitest 27 文件/147 项；开发 Electron E2E 6/6；macOS 打包应用 E2E 6/6。
- 页面证据：`real-test-evidence/macos-1.3.2/08-rate-comparison.png`、`09-channel-status-popover.png`、`05-floating.png`。
- 发布结构：DMG 校验有效；macOS 主程序为 Mach-O ARM64；Windows NSIS 为 PE32、解包主程序为 PE32+ x86-64；两端 asar 版本为 `1.3.2` 且包含 Renderer/main/preload。
- 真实站点：三个授权站点的登录、核心数据、使用记录、筛选、倍率、渠道列表和详情只读验证已在本目标中通过；本次修复未改变网络接口、协议或适配器。
- 结论：`1.3.2` macOS 真机测试通过。Windows 仅完成交叉构建和结构验证，未执行 Windows 真机。

## 2026-07-19 1.3.1 倍率、渠道快捷查看与悬浮窗验收

- 环境：macOS 26.5，Apple Silicon，未签名 `1.3.1` ARM64 打包应用；临时 userData，本地集成站点不污染正式数据。
- 真实站点：三个授权站点仅执行只读验证；登录、核心数据、使用记录、筛选、倍率、渠道列表和渠道详情均成功。未记录账号、密码、Token、Cookie、完整 Key 或完整敏感响应。
- 自动化：打包应用完整 Electron E2E 6/6；开发 E2E 6/6；Vitest 27 文件/146 项。
- 页面证据：`real-test-evidence/macos-1.3.1/08-rate-comparison.png`、`09-channel-status-popover.png`、`05-floating.png`。

| ID  | 验收项                 | 操作与证据                                                | 结果                                                                     | 状态 |
| --- | ---------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------ | ---- |
| R31 | 四平台固定顺序与主题   | 打包应用加载五平台倍率，检查 DOM 顺序、颜色和截图         | OpenAI/Claude/Gemini/Grok 固定为浅绿/浅橙/浅蓝/黑灰，第五平台追加        | ✅   |
| R32 | 单行横向滚动           | 读取五张卡几何位置与容器 scrollWidth                      | 五张卡 top 一致，`scrollWidth > clientWidth`，没有第二行                 | ✅   |
| R33 | 评分与候选完整性       | 检查综合/价格/稳定分、状态标签和候选排行                  | 60/40 评分显示正确；全部候选保留；无状态明确标为“无渠道状态”             | ✅   |
| R34 | 渠道弹窗按需加载       | 点击前记录请求数，点击后观察 loading、列表和详情          | 未点击不请求；点击后加载整站 7 个渠道卡及首个详情                        | ✅   |
| R35 | 渠道弹窗缓存与关闭     | Escape 关闭后重开，比较列表/详情请求计数                  | 页面会话缓存命中，请求数不增加；外部点击和 Escape 均可关闭               | ✅   |
| R36 | 悬浮窗最新使用站点跟随 | 本地两站最近使用时间先后变化，观察打包悬浮窗标题          | 悬浮窗自动从前一站切到 `localhost`；只改本地显示，不改变主窗口持久选站   | ✅   |
| R37 | 页面样式与视口         | 视觉检查三张证据图                                        | 卡片圆润、文字可读、弹窗内部独立滚动，无重叠、溢出或布局跳动             | ✅   |
| R38 | 三站只读接口           | 运行时凭据注入 `verify:real`                              | 三站登录、核心/使用/倍率/渠道列表/详情全部成功；倍率分组数为 18/8/22     | ✅   |
| R39 | macOS/Windows 发布结构 | `hdiutil verify`、`file`、Info.plist、asar 入口和版本检查 | macOS ARM64 与 `1.3.1` 有效；Windows x64 仅结构通过，未冒充 Windows 真机 | ✅   |
| R40 | 发布目录与校验         | 检查 `release/` 和 SHA-256                                | 仅保留当前四个分发文件及构建元数据；旧产物和解包目录移到项目旁可恢复归档 | ✅   |

结论：`1.3.1` macOS 真机测试通过。Windows 未执行真机安装或启动，状态仅为交叉构建与结构验证通过。

## 2026-07-19 1.3.0 倍率比较验收

- 环境：macOS 26.5，Apple Silicon，未签名 `1.3.0` ARM64 打包应用；临时 userData，不污染正式数据。最终 DMG 只读挂载后，直接运行镜像内应用完成 Electron E2E 6/6。
- 三站只读 API：登录、profile、Key、usage、渠道和倍率列表均成功；倍率分组数量为 18/8/21，平台按站点动态覆盖 OpenAI、Claude、Gemini、Grok。
- 业务：为三站设置 `1:1 / 1:1 / 1:10`，跨重启保持；跨站区显示 4 个平台，未设置比例规则由纯逻辑和 E2E 覆盖。
- Popover：maok 显示 2 个平台和 8 个分组，最低价保留平局，搜索、平台筛选、刷新、原始倍率、折算倍率和滚动正常。
- 样式：1152x867 Renderer 无横向溢出，Popover 完全位于视口内，3 张站点卡片均有比例控件与“查看倍率”，文字和控件没有重叠。
- 脱敏证据：`real-test-evidence/macos-1.3.0/01-rate-comparison-overview.png`、`02-maok-rate-popover.png`、`03-site-card-actions.png`；余额和个人用量已模糊，不含账号、密码、Token、Cookie 或完整 Key。
- 发布产物：macOS ARM64 DMG 经 `hdiutil verify` 通过；Windows x64 NSIS 安装器为 PE32，解包主程序为 PE32+ x86-64，两端 asar 均为 `1.3.0` 且包含 Renderer/main/preload。
- Windows：x64 交叉构建和结构验证通过，不标记真机通过。

## 当前状态

`真机测试通过`

以上状态包含历史 1.0.0 适用项和 2026-07-18 1.2.0 优化增量的本轮适用验收。新增验收使用独立证据目录；Windows 仅记录交叉构建，不属于真机结论。

2026-07-15 追加修复：此前打包应用证据只证明失焦后 visible，未证明非激活显示不会被台前调度重新归类。本轮将主窗最小化和托盘显示统一改为 `showInactive()`；新 macOS 打包应用 E2E 已通过；在开启 Stage Manager 的 macOS 上切换 Chrome、切换 Space 并切换全屏前台应用后，悬浮窗仍保持原坐标、`380×260` 尺寸且 `AXMinimized=false`，前台应用可覆盖悬浮窗，相关真机项已复测通过。Windows 只记录 x64 NSIS 交叉构建与结构证据，不属于真机结论。

验收责任：Codex。
目标平台：当前 macOS 真机。
Windows：仅 CI 验证，不在本清单中冒充真机。

## 前置门禁

- 开发追踪相关任务达到 `内部测试通过`。
- lint、typecheck、单元、集成、Electron E2E 和 macOS build 有真实通过输出。
- Stitch 页面已完成 HTML 实例级接收检查；本路线不执行 Product Design Design QA。
- 测试构建不含真实密码、Token、数据库或用户缓存。
- 两个真实站点的凭据已由用户在当前目标上下文提供，仅允许运行时读取；不得写入本文、命令历史、Git、截图或日志。

## 实测步骤

|   # | 模块               | 适用性                                       | 前置条件                                | 操作                                              | 期望结果                                          | 证据路径                                                                                               | 失败回写                           | 结果   |
| --: | ------------------ | -------------------------------------------- | --------------------------------------- | ------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------- | ------ |
|   1 | 启动               | 适用                                         | 干净隔离 userData、macOS 打包应用       | 启动打包后的真实应用                              | 自动打开首次录入且无崩溃                          | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `04-开发追踪.md`、模块01           | ✅     |
|   2 | 错误录入           | 适用；破坏性错误只用本地 mock                | 本地受控 mock 可用                      | 分别提交无效地址、超时和错误密码场景              | 错误分类明确且不保存正式站点                      | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `08-测试用例.md`、模块03           | ✅     |
|   3 | 真实站点录入       | 适用                                         | 两站运行时凭据可用                      | 通过真实界面测试并保存两个站点                    | 核心能力成功后保存，渠道缺失仅局部降级            | `real-test-evidence/macos-2026-07-14/02-overview.png`                                                  | `07-API文档.md`、模块03/06         | ✅     |
|   4 | Keychain           | 适用                                         | 已保存真实站点                          | 退出并重启应用后刷新                              | 使用真实 safeStorage 恢复会话，界面和日志无明文   | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `06-数据字典.md`、模块05/13        | ✅     |
|   5 | 悬浮窗切站         | 适用                                         | 已保存两个站点                          | 左右循环切站并观察后台刷新                        | 先显示对应缓存，随后同步最新状态                  | `real-test-evidence/macos-2026-07-14/06-floating.png`                                                  | 悬浮窗 UI 壳清单、模块02           | ✅     |
|   6 | 窗口位置           | 适用                                         | 主显示器可用                            | 移动和缩放主窗口，退出后重启                      | 完整 bounds 恢复且窗口仍在工作区                  | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `04-开发追踪.md`、模块02           | ✅     |
|   7 | 多显示器           | 不适用：当前 Mac 仅连接一个显示器            | 第二显示器不可用                        | 保留自动化可见位置回退验证                        | 硬件条件如实记录，自动回退逻辑通过                | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `09-真机实测.md`、模块02           | 不适用 |
|   8 | 焦点与尺寸         | 适用                                         | 打开可输入文本的本机应用                | 显示悬浮窗并切换长标题和错误态                    | 悬浮窗不抢焦点，固定尺寸且无溢出                  | `real-test-evidence/macos-2026-07-14/06-floating.png`                                                  | 悬浮窗 UI 壳清单、模块02           | ✅     |
|   9 | 动态标题           | 适用                                         | 两个真实站点和本地未知态可用            | 切换高余额、低余额和查询中状态                    | 三条确认文案与实时余额分支一致                    | `real-test-evidence/macos-2026-07-14/06-floating.png`                                                  | `01-需求文档.md`、模块02           | ✅     |
|  10 | 默认 Key           | 适用                                         | 至少一个站点存在有效 Key                | 切换自动/手动模式并重启                           | 自动规则正确，手动选择持久化且不展示完整 Key      | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `08-测试用例.md`、模块07           | ✅     |
|  11 | Key 失效           | 适用；仅使用本地 mock，不修改真实站点 Key    | 本地 mock 提供失效 Key                  | 选择手动 Key 后模拟禁用并刷新                     | 提示失效并临时回退自动，保留手动偏好              | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `08-测试用例.md`、模块07           | ✅     |
|  12 | 使用记录           | 适用                                         | 真实站点存在使用记录                    | 验证筛选、分页、排序、重置、列设置和 CSV          | 参数与数据正确，CSV 脱敏、防公式注入且权限 0600   | `real-test-evidence/macos-2026-07-14/03-usage.png`                                                     | 使用记录 UI 壳清单、模块08         | ✅     |
|  13 | 渠道状态           | 适用                                         | 真实站点渠道能力已探测                  | 打开列表和详情并切换站点                          | 支持项显示真实字段，缺失字段显示待查询            | `real-test-evidence/macos-2026-07-14/04-channels.png`                                                  | 渠道 UI 壳清单、模块09             | ✅     |
|  14 | 全站总览           | 适用                                         | 两个真实站点已刷新                      | 核对汇总、排序、当前站和缓存状态                  | 汇总只包含有效缓存，状态和覆盖数正确              | `real-test-evidence/macos-2026-07-14/02-overview.png`                                                  | 总览 UI 壳清单、模块10             | ✅     |
|  15 | 后台刷新           | 适用                                         | 两个真实站点已保存                      | 保持应用运行并观察调度状态                        | 当前站优先、无同站重复、失败相互隔离              | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `04-开发追踪.md`、模块11           | ✅     |
|  16 | 托盘关闭           | 适用                                         | 主面板和悬浮窗已显示                    | 关闭主面板后从托盘恢复                            | 主进程和刷新继续，托盘可恢复主面板                | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `09-真机实测.md`、模块01/02        | ✅     |
|  17 | 托盘退出           | 适用                                         | 应用正在运行                            | 验证生产托盘退出回调并在收尾停止进程              | 调度停止、进程退出且状态落盘                      | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `09-真机实测.md`、模块01/02        | ✅     |
|  18 | 通知发送           | 适用；使用本地受控状态                       | 系统通知服务可用                        | 用本地受控状态触发低余额和失败/恢复               | 通知内容脱敏、冷却和恢复语义正确                  | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `08-测试用例.md`、模块12           | ✅     |
|  19 | 通知拒绝           | 不适用：不改变用户当前全局通知权限           | 不安全修改系统权限                      | 使用本地 permission-denied 分支验证               | 明确降级且不循环请求权限                          | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `09-真机实测.md`、模块12           | 不适用 |
|  20 | 开机启动           | 不适用：当前产物为未签名且未安装的内部目录包 | 不满足登录项服务注册条件                | 点击开关并检查系统登录项与恢复状态                | 保持关闭且测试结束无残留登录项                    | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `08-测试用例.md`、模块15           | 不适用 |
|  21 | 删除站点           | 适用；只删除隔离 userData 内的测试站点       | 测试站点已保存                          | 二次确认删除并重启                                | 站点、缓存、安全凭据和关联数据均清理              | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `06-数据字典.md`、模块13/15        | ✅     |
|  22 | Stitch 接收检查    | 适用；不执行 Design QA                       | Stitch HTML 和五个正式入口可读          | 核对结构、尺寸、状态入口及正式运行静态数据边界    | 五个业务界面来源一致，正式态无预览控制器          | `real-test-evidence/macos-2026-07-14/README.md#稳定截图`                                               | `10-UI壳接入清单.md`、模块16       | ✅     |
|  23 | 日志与存储审计     | 适用                                         | 完成真实站点流程                        | 扫描日志、SQLite、凭据文件、CSV 和截图            | 无密码、Token、Cookie或完整 API Key 泄漏          | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `06-数据字典.md`、模块14           | ✅     |
|  24 | 完整回归与清单校验 | 适用                                         | 以上项目已处理                          | 运行全部门禁和严格清单校验                        | 自动化、构建、打包、链接、diff 和清单全部通过     | `real-test-evidence/macos-2026-07-14/README.md#e01-e24`                                                | `04-开发追踪.md`、`08-测试用例.md` | ✅     |
|  25 | Sub2API CRUD       | 适用                                         | 2.0.0 macOS 打包应用、隔离 userData     | 新增、重启保留、编辑快捷入口、删除确认            | 服务器持久化，快捷入口最多 5 个，删除清理分区存储 | `real-test-evidence/macos-2.0.0/01-server-added.png`、`02-card-geometry.json`                          | `04-开发追踪.md`、模块16           | ✅     |
|  26 | 内嵌登录持久化     | 适用                                         | 真实站点 `https://www.api-feng.online/` | 打开服务器网页、重启后再次打开                    | 登录状态为“已登录”，重启后仍保持                  | `real-test-evidence/macos-2.0.0/06-login-state.json`、`09-restart-login-state.json`                    | `07-API文档.md`、模块16            | ✅     |
|  27 | 清除会话           | 适用                                         | 已登录服务器                            | 关闭内嵌、点击清除登录状态                        | 登录状态重置，分区存储清理，视图正常关闭          | `real-test-evidence/macos-2.0.0/11-server-after-clear.png`                                             | `06-数据字典.md`、模块16           | ✅     |
|  28 | 菜单映射           | 适用                                         | 2.0.0 打包应用                          | 点击“通知”和“设置”                                | 通知进入通知规则，设置进入通用设置                | `real-test-evidence/macos-2.0.0/12-notification-rules.png`、`13-general-settings.png`                  | `08-测试用例.md`、模块15           | ✅     |
|  29 | 页面样式           | 适用                                         | 上述真机截图                            | 视觉门禁逐张检查布局、字体、颜色、控件和重叠      | 无 P0/P1，卡片标题/域名单行，通知不遮挡工具栏     | `real-test-evidence/macos-2.0.0/01-server-added.png`、`10-restart-logged-in.png`                       | `10-UI壳接入清单.md`、模块16       | ✅     |
|  30 | 悬浮窗/推荐/时间线 | 适用                                         | 本地受控 E2E                            | 20 格时间线、12 次百分比、余额、过滤和时间线      | 20 格左补空、余额不闪总额、异常不误报             | `tests/e2e/electron-smoke.spec.ts`、`src/renderer/shells/floating/FloatingWindow.test.ts`              | `08-测试用例.md`、模块10/11        | ✅     |
|  31 | favicon            | 适用                                         | 本地受控站点与元数据测试                | 单站/批量/交互添加后抓取，失败回退                | icon 持久化、任务卡片回填、失败不阻塞添加         | `electron/main/services/site-metadata.test.ts`、`electron/main/services/site-service-metadata.test.ts` | `06-数据字典.md`、模块03           | ✅     |
|  32 | Windows x64        | 适用；非真机                                 | macOS 交叉构建                          | 构建 NSIS，检查 PE/架构/版本                      | 安装器 NSIS PE32，主程序 PE32+ x86-64，不写真机   | `release/Sub2API-Multi-Hub-Monitor-2.0.0-win-x64.exe`、`builder-debug.yml`                             | 模块17                             | ✅     |
|  33 | 全量门禁           | 适用                                         | 上述项目已处理                          | 运行 format/lint/typecheck/Vitest/build/E2E/audit | 全部通过，DMG verify VALID，双平台产物哈希正确    | `real-test-evidence/macos-2.0.0/`、`release/`、`CHANGELOG.md`                                          | `04-开发追踪.md`、`08-测试用例.md` | ✅     |

## 真实站点专属项

- 登录、余额、倍率、今日统计、使用记录和渠道监控需至少一个真实二开站验证。
- 凭据只能通过应用录入或安全临时渠道提供，不写入本文、终端历史、Git 或截图。
- 两个真实站点的运行时凭据已由用户在当前目标上下文提供。本文件只记录能力结论和脱敏证据，不记录账号、密码、Token、Cookie或完整响应。

## 本轮深度优化增量清单

|   # | 范围           | 必须操作与期望                                                                                  | 证据                                        | 结果                         |
| --: | -------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------- | ---------------------------- |
| N01 | 新版启动与外壳 | 从本轮 ARM64 打包应用启动隔离实例；无黑色原生标题栏，Renderer 铺满窗口                          | 打包 E2E；`01-overview.png`                 | ✅                           |
| N02 | 主窗生命周期   | UI 最小化按悬浮设置切换悬浮窗或托盘；UI 关闭结束进程；扩大恢复主窗                              | 打包生命周期 E2E                            | ✅                           |
| N03 | 主窗缩放       | 默认约工作区 60% x 90%，居中；内容响应式铺满，无大空白，侧栏固定                                | 几何断言；打包 bounds E2E                   | ✅                           |
| N04 | 全部站点       | 无悬浮窗预览/假加载；真实状态转换、更新时间、K/M、倍率和刷新动画正确                            | `01-overview.png`；两站只读                 | ✅                           |
| N05 | 使用记录       | 真实站点/模型/分组/Key 下拉；20 条分页；本地时间；思考等级；无地区；CSV 原始 Token              | `02-usage.png`；本地完整流 E2E              | ✅                           |
| N06 | 渠道状态       | 切站/切渠道无旧数据冒充；超过 6 个可独立滚动；状态徽标与 PING 布局稳定                          | `03-channels.png`；E2E 几何断言             | ✅                           |
| N07 | 站点验证       | 单站显示阶段；批量显示当前/总数且部分失败不中断；结束汇总原因不泄密                             | `04-sites-settings.png`；Vitest/E2E         | ✅                           |
| N08 | 通知与设置     | 开关、冷却、系统支持、刷新、悬浮开关/四角、过期阈值可用并跨重启保持                             | 打包跨重启 E2E；`04-sites-settings.png`     | ✅                           |
| N09 | 悬浮窗         | 固定尺寸；四角均可达并持久化；设置/扩大/状态行不重叠；扩大恢复主窗                              | `05-floating.png`；打包四角 E2E             | ✅                           |
| N10 | 桌面层级       | 悬浮窗失焦和切换应用后保持显示，但不覆盖浏览器等正常前台窗口；台前调度跨 Space 可见且不覆盖全屏 | 原生标志、打包应用状态读取、Chrome 人工操作 | ✅；Windows 仅自动化策略覆盖 |
| N11 | 两站只读       | 重新登录后只读核对 profile、Key/倍率、统计、usage 模型/记录、渠道能力；不记录敏感值             | 脱敏命令结果；服务层验证                    | ✅                           |
| N12 | 样式总检       | 五界面和悬浮窗无重叠、异常截断、大片空白、布局跳动、错位或缩放失稳                              | 本轮证据目录 5 张稳定截图                   | ✅                           |
| N13 | 产物           | 根目录 DMG 为本轮构建且可启动；Windows NSIS 只记录交叉构建/结构检查                             | DMG verify；NSIS/PE 结构；SHA-256           | ✅；Windows 未真机           |

本轮稳定截图位于 `real-test-evidence/macos-2026-07-14-deep-optimization/`。`release/` 当前产物及 SHA-256：

- macOS ARM64 DMG：`Sub2API-Multi-Hub-Monitor-1.0.0-mac-arm64.dmg`，`540b12a17b7fc2b060196c0cae8024f01b84bd7786e61d4642c903a475a0c9d2`；本轮从 Gitee `0dd640c` 的完整 1.0.0 代码树重新生成并通过 `hdiutil verify`。既有生产 E2E 与 Stage Manager/Space/全屏人工复测结论对应同一代码基线。
- Windows x64 NSIS：`Sub2API-Multi-Hub-Monitor-1.0.0-win-x64.exe`，`7e4d24c033e9f6c0549d2cb8b3a0e9b3a06ec31cc9c6044fd26e2c9a80ce2afa`；本轮从同一代码基线交叉构建，安装器为 NSIS PE32，解包主程序为 PE32+ x86-64。
- 发布边界：macOS 产物未签名、未公证；Windows 未真机启动或安装。

## 完成门禁

标记 `真机测试通过` 前必须：

1. 每一适用步骤有真实证据且结果为严格 `✅`。
2. 不适用项有原因，不得用“不适用”掩盖缺失能力。
3. 运行 `python3 scripts/check_real_test_checklist.py liran_docs/09-真机实测.md --require-complete`。
4. 结构校验通过后仍需人工核对证据真实性。
5. 问题已回写、修复并复测，开发追踪同步。

## 2026-07-18 外发版合并验收增量（本轮已验收）

本节只规划 1.1.0 功能合并后的新增验收，不引用上一轮 1.0.0 证据。验收责任为 Codex：macOS 执行真机和页面样式检查；Windows 执行 x64 交叉构建、NSIS/PE 结构和自动化证据，不能填写 Windows 真机通过。

|   # | 模块             | 适用性       | 前置条件                                | 操作                                                        | 期望结果                                                         | 证据                                                                                                                                          | 失败回写                | 结果 |
| --: | ---------------- | ------------ | --------------------------------------- | ----------------------------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ---- |
|  25 | Radar 导航       | 适用         | macOS ARM64 内部目录构建                | 从正式主导航打开 Radar 并验证刷新、成功/空态/错误分支       | 页面可达，站点主链路不受影响                                     | `real-test-evidence/macos-1.1.0/playwright/radar.png`、`test-results/radar.png`                                                               | 模块16、RQ-20、MERGE-03 | ✅   |
|  26 | Radar 网络边界   | 适用         | E2E 可控网络条件                        | 验证两个固定 HTTPS origin、非白名单导航、网络错误和嵌入返回 | 只允许两个精确 origin，错误可恢复，不接站点敏感数据              | `tests/e2e/electron-smoke.spec.ts`、`electron/shared/radar.ts`、`src/renderer/shells/radar/RadarPage.test.ts`                                 | 模块14、MERGE-03        | ✅   |
|  27 | 使用记录增强     | 适用         | 两个真实站点只读复测和打包 E2E          | 核对推理等级、缓存创建 Token、耗时、筛选、分页和 CSV        | 字段与响应一致，缺失可解释，CSV 保持脱敏和权限规则               | `real-test-evidence/macos-1.1.0/playwright/02-usage.png`、`scripts/verify-real-sites.mjs`、`tests/e2e/electron-smoke.spec.ts`                 | 模块08、MERGE-04        | ✅   |
|  28 | 渠道关系与切站   | 适用         | 两个真实站点均返回渠道能力              | 切站、切渠道、刷新并观察关系排序和详情                      | 不出现旧站/旧渠道串数据，多候选不强行绑定                        | `real-test-evidence/macos-1.1.0/playwright/03-channels.png`、`scripts/verify-real-sites.mjs`、`src/renderer/channel-load-coordinator.test.ts` | 模块09、MERGE-05/06     | ✅   |
|  29 | 悬浮窗透明度     | 适用         | macOS ARM64 内部目录构建和 E2E          | 验证 35%、84%、100%、跨重启、失焦和窗口层级                 | 原生透明度同步、固定 380×260、非激活显示和前台覆盖保持           | `real-test-evidence/macos-1.1.0/playwright/05-floating.png`、`electron/main/floating-opacity.test.ts`、`tests/e2e/electron-smoke.spec.ts`     | 模块02、MERGE-07        | ✅   |
|  30 | 页面样式总检     | 适用         | Radar、usage、channels、floating 已可达 | 逐张检查桌面窗口、窄视口、长文本和各状态                    | 无重叠、大空白、错位、跳动、截断或控件规格回归                   | `real-test-evidence/macos-1.1.0/playwright/01-overview.png`、`02-usage.png`、`03-channels.png`、`05-floating.png`、`test-results/radar.png`   | UI 壳清单、MERGE-09     | ✅   |
|  31 | Windows x64 证据 | 适用；非真机 | macOS 上可用 electron-builder 交叉链    | 构建 NSIS，检查 PE/架构、asar、preload 和启动入口           | 安装器为 NSIS PE32，主程序为 PE32+ x86-64；不写 Windows 真机通过 | `release/Sub2API-Multi-Hub-Monitor-1.1.0-win-x64.exe`、`builder-debug.yml`、`file`/`asar list` 输出                                           | 模块17、MERGE-08/10     | ✅   |
|  32 | 发布门禁         | 适用         | macOS/Windows 构建及 macOS 验收完成     | 递增至 1.1.0，清理 release/，生成双平台产物并校验 SHA-256   | release/ 仅保留当前版本产物，更新说明与证据一致                  | `CHANGELOG.md`、`release/`、`liran_docs/04-开发追踪.md` MERGE-10、`hdiutil verify`、`shasum -a 256`                                           | 模块17、MERGE-10        | ✅   |

## 2026-07-18 1.2.0 优化验收增量

本轮验收责任为 Codex：macOS 真机和页面样式；Windows 只做 x64 交叉构建与结构验证。真实站点只读验证使用运行时凭据，不在本文记录凭据或固定敏感响应。

| 编号 | 项目          | 操作                                        | 期望                          | 证据                                                                                     | 结果 |
| ---: | ------------- | ------------------------------------------- | ----------------------------- | ---------------------------------------------------------------------------------------- | ---- |
|   33 | usage 首字    | 验证 9999/10000/19999/20000ms、缺失值和 CSV | 颜色和空值规则准确            | `UsagePage.test.ts`、`packaged-release/02-usage.png`、`tests/e2e/electron-smoke.spec.ts` | ✅   |
|   34 | 站点卡片/备注 | 本地受控站点切换、卡片布局、双击备注        | 无溢出，备注持久化且不泄密    | `packaged-release/01-overview.png`、`packaged-release/06-overview-quota.png`、打包 E2E   | ✅   |
|   35 | Key quota     | 自动/手动 Key、quota=0、有效额度、超额      | 余额回退和剩余额度正确        | quota 单测、适配器/服务测试、`packaged-release/06-overview-quota.png`、三站脱敏只读验证  | ✅   |
|   36 | 批量进度      | 执行成功、部分失败、异常                    | Loading、进度、汇总和退出完整 | `SitesPage.test.ts`、`packaged-release/07-batch-progress.png`、打包 E2E                  | ✅   |
|   37 | 悬浮窗刷新    | 点击刷新、重复点击、成功/失败缓存           | 当前站点刷新且不重叠          | `packaged-release/05-floating.png`、`tests/e2e/electron-smoke.spec.ts`                   | ✅   |
|   38 | Windows x64   | 构建 NSIS，检查 PE/asar/preload（非真机）   | 仅记录交叉构建，不冒充真机    | `release/`、`builder-debug.yml`、PE/asar 输出                                            | ✅   |

## 2026-07-19 1.2.1 macOS 真机与页面样式验收

验收载体：未签名 macOS ARM64 目录包。真实站点仅执行既有凭据下的读取和刷新，不记录凭据、完整 Key 或敏感响应。

| 项目              | 真实操作与结果                                                     | 证据                                                        | 结果   |
| ----------------- | ------------------------------------------------------------------ | ----------------------------------------------------------- | ------ |
| 全站刷新          | 点击“刷新站点”，四张卡片同时显示刷新中并禁用按钮，完成后逐卡更新   | `real-test-evidence/macos-1.2.1/02-overview-refreshing.png` | ✅     |
| Key/额度跨站      | 多站手动 Key 下拉和 quota 卡片同时可见，切站描边不改变其他卡片模式 | `real-test-evidence/macos-1.2.1/01-overview.png`            | ✅     |
| 使用记录          | 真实记录显示输入/输出/缓存读取 Token、斜杠时间及首字绿/黄/红       | `real-test-evidence/macos-1.2.1/03-usage.png`               | ✅     |
| 最终包筛选回归    | 新构建目录包恢复真实站点，Key/模型/分组下拉均可用，记录表完成刷新  | `real-test-evidence/macos-1.2.1/06-usage-final-package.png` | ✅     |
| 悬浮窗标题和按钮  | 顶部显示当前站点备注；返回与刷新按钮紧邻位于右下角，均可点击       | `real-test-evidence/macos-1.2.1/04-floating.png`            | ✅     |
| 悬浮窗拖动        | 从标题非交互区域拖动后窗口连续移动，内容和按钮仍可用               | `real-test-evidence/macos-1.2.1/05-floating-dragged.png`    | ✅     |
| 固定尺寸/窗口层级 | 保持 `380 x 260`、`alwaysOnTop=false`，返回主页面后主窗恢复        | Electron E2E 与 Computer Use 真机步骤                       | ✅     |
| 页面样式          | Overview、Usage、Floating 无重叠、控件溢出、异常换行或大片空白     | 上述六张截图                                                | ✅     |
| macOS 产物        | DMG 通过 `hdiutil verify`；未签名、未公证                          | `release/Sub2API-Multi-Hub-Monitor-1.2.1-mac-arm64.dmg`     | ✅     |
| Windows x64       | 仅交叉构建和 PE/asar/版本结构验证，未在 Windows 安装或启动         | `release/Sub2API-Multi-Hub-Monitor-1.2.1-win-x64.exe`       | 非真机 |

限制：当前 Mac 为单显示器，物理拔插未实测；负坐标、显示器消失和工作区越界由纯函数测试覆盖。未签名目录包不把开机启动登记作为通过项。

## 2026-07-14 已修复问题

- 托盘 SVG Data URL 在 Electron 43 解码为空，已改为内嵌 PNG Data URL；原始 36×36，生产缩放 18×18 并启用 macOS template，3 项托盘测试通过。
- CSV 初次真机导出权限为 0644，已在写入和写后两层强制为 0600；Electron E2E 先失败后通过，最终打包应用导出复测为 0600。
- `electron/main/adapters/sub2api-adapter.ts` 格式门禁失败，已仅用 Prettier 格式化，无逻辑变更。
- 新确认坑已回写 `docs/pitfalls/electron-build.md` 与 `docs/pitfalls/tooling.md`。

## UPD-28 在线更新真机验收（macOS 已通过，Windows 待真机）

验收责任：Codex 负责真实更新链路和页面样式检查。用户确认内部开发与真机验收合并为同一条 `/goal`。当前状态为 `GitHub 双平台资产已发布，macOS 真实更新通过，Windows 真机待验证`。

### 当前已取得证据（2026-07-25）

- 版本已递增至 `1.4.6`，生成 macOS ARM64 DMG、Windows x64 NSIS、blockmap 和 `release/update-manifest.json`。
- Vitest 37 文件/227 项、TypeScript、ESLint、生产构建和 Electron E2E 6/6 通过。
- 历史 Gitee Release `760145` 已发布；该批次用于旧更新源验证。当前 1.4.8 安装包改由 GitHub Release 提供，远端包含 `update-manifest.json`、macOS ARM64 DMG、Windows x64 EXE 及对应 blockmap。
- macOS 真实在线检查识别到 `1.4.6`，远程 DMG 下载 100% 完成并通过 SHA-256 校验；最终 DMG 约 97 MB。
- macOS 使用旧 `1.4.5` DMG 启动对象替换为最终 `1.4.6` DMG，替换后版本为 `1.4.6` 且 App 可启动；页面样式截图已保存。
- 当前机器无 Windows 真机；Windows 仅完成 NSIS、PE、asar、blockmap 和远端资产检查。

### 验收批次

建议使用至少两个递增 patch 测试包（例如 `1.4.6` -> `1.4.7` -> `1.4.8`），每个包的 GitHub Release 说明必须写明“真机更新测试专用、无业务变化”。测试包只用于更新链路，不得覆盖功能版本结论。

### macOS ARM64

1. 使用旧版本启动，确认版本徽标、站点配置、SQLite 数据和安全存储存在。
2. 通过启动检查、版本徽标和设置页分别触发检查，确认 single-flight、无更新和网络错误状态。
3. 发布测试 patch 后确认显示版本号、更新说明、跳过和稍后提醒。
4. 下载 DMG，确认进度、校验、文件来源和错误状态；确认当前应用仍可运行。
5. 自动打开 DMG；记录 ad-hoc、未公证条件下是否需要用户手动替换 App，禁止写成静默自动更新。
6. 替换后重新启动，确认 SQLite、站点、设置和 safeStorage 数据保留。
7. 保存截图、DMG/URL/SHA-256、系统版本、应用版本和实际操作日志；敏感信息脱敏。

### Windows x64

1. 先用交叉构建检查 NSIS、blockmap、内部版本和资源结构；此步骤不等同真机通过。
2. 在真实 Windows 环境用旧版本检查测试 patch，确认更新说明、下载、自动安装和自动重启。
3. 验证跳过版本、稍后提醒、安装失败回退、数据保留和再次检查。
4. 记录 Windows 版本、架构、安装包版本、Gitee 资源、日志和截图；没有 Windows 环境时保持“待验证”。

### 页面样式检查

Codex 需检查版本徽标点击态、更新弹窗、更新说明长文本、下载进度、错误提示、跳过/稍后按钮、自动重启提示，以及 1600px/720px 窗口下无重叠、无跳动、文字不溢出和焦点可见。样式证据只针对真实项目页面，不用旧截图冒充。

### 完成门禁

Windows 真机安装/重启/回退仍待验证；macOS、GitHub 资产、页面样式和本机 DMG 替换已有真实证据。UPD-28 按“可用平台完成、Windows 真机边界明确”收口，严禁把 Windows 交叉构建写成真机通过。

## 2026-07-25 1.4.8 更新反馈与版本徽标验收

- 1.4.8 GitHub Release 已发布并保留为历史版本；当前远端 `latest` 为修正版 `1.4.9`，两者均包含 macOS ARM64 DMG、Windows x64 NSIS、两个 blockmap 和 `update-manifest.json`，manifest 的稳定通道、版本、说明和 SHA-256 已核对。
- 重新生成的 macOS ARM64 DMG 使用 ad-hoc 签名、未公证条件；打包应用 Electron E2E 6/6 通过，覆盖启动、窗口生命周期、设置持久化、页面接线和窄窗口布局。
- 使用隔离 userData 启动打包应用，点击右上角 `v1.4.8` 版本徽标后实际收到“当前已是最新版本”通知；证据：`real-test-evidence/macos-1.4.8/17-update-latest-toast.png`。
- 既有页面证据 `01` 至 `16` 复测生成，检查中 toast 在窄窗口下不遮挡 API 密钥表格；设置页更新源文案已修正为“检查 GitHub 稳定版更新”。
- 本轮回归：Vitest 38 文件/231 项、格式检查、ESLint、TypeScript、生产构建、开发 Electron E2E 6/6、macOS 打包应用 Electron E2E 6/6 均通过。
- 本轮没有伪造“发现新版本”或自动安装结果：当前打包应用已是远端最新版本；Windows 自动安装/自动重启仍只有交叉构建证据，必须在真实 Windows 环境补测。

## 2026-07-25 1.4.8 更新反馈验收边界

- 验收责任：Codex 负责真实更新链路和页面样式检查。
- 待验证入口：1.4.8 macOS ARM64 打包应用主窗口右上角版本徽标，以及启动后的自动检查。
- 必查结果：检查中 toast、当前已是最新 toast、检查失败 toast、发现新版本 modal、真实 releaseNotes、跳过/稍后提醒、下载进度和窄窗口无重叠。
- 版本边界：1.4.6 及更早仍使用旧 Gitee 更新逻辑的客户端需先安装 GitHub 更新源版本；不能把旧客户端直接显示 GitHub 1.4.8 视为通过。
- 当前状态：1.4.8 双平台 Release 已发布并归档，1.4.9 修正版已成为 latest；macOS 版本徽标和页面样式证据已完成，Windows 自动安装/重启仍只保留交叉构建证据。

## 2026-07-25 1.4.9 在线更新提醒修复

- 修复“稍后提醒”时间立即过期的问题，`UpdateService.remindLater()` 现在把提醒时间设置为当前时间后 24 小时，并由单元测试验证未来时间边界。
- 1.4.9 是 1.4.8 的修正版，发布流程必须重新生成 macOS ARM64 DMG、Windows x64 NSIS、blockmap 和 manifest；1.4.8 Release 资产保持历史可追溯，不覆盖旧资产。
- Windows 仍未进行真机安装、自动重启或回退验证；macOS 真机证据继续使用 `real-test-evidence/macos-1.4.8/`，本次修复为主进程提醒持久化逻辑，不改变页面布局。
- 1.4.9 实际 GitHub DMG 已下载并通过 `hdiutil verify`，挂载后的 Release 应用 Electron E2E 6/6 通过；证据见 `real-test-evidence/macos-1.4.9-remote/`。

## 2026-07-25 1.5.0 在线更新组件视觉与入口验收

- 1.5.0 目标：验证右上角版本徽标和设置页检查按钮的统一交互，以及发现新版本后的自制更新卡片。
- 受控 available manifest 验收已完成：宽窗口证据为 `real-test-evidence/update-ui-draft/03-available-wide.png`，720×800 窄窗口证据为 `real-test-evidence/update-ui-draft/04-available-narrow.png`；版本对比、真实更新说明、真机测试专用提示和单列按钮均可见且无重叠。
- 1.4.9 远程 latest 状态下，版本徽标和设置页检查按钮均收到“当前已是最新版本”反馈；证据为 `real-test-evidence/update-ui-draft/01-badge-latest-wide.png`、`02-settings-latest-wide.png`。
- 发布后必须使用真实 1.5.0 macOS ARM64 DMG 执行 `hdiutil verify`、挂载启动、版本徽标检查、设置页检查、宽窄窗口截图和焦点/关闭行为复测；不得把受控 available manifest 截图描述成远程 Release 下载或安装结果。
- 已完成真实 Release DMG 验收：远端文件下载后 SHA-256 与 manifest 一致，`hdiutil verify` 报告 `VALID`；挂载后的真实 1.5.0 App 完整 Electron E2E 为 6/6，版本徽标和设置页 latest 证据已更新到 `real-test-evidence/macos-1.5.0-remote/`。
- Windows x64 仍只执行交叉构建、PE/asar/资产和 manifest 审计，不写成 Windows 真机安装或自动重启通过。

## 2026-08-05 1.8.1 Radar WebContentsView 真机验收

验收范围：Codex 自行完成真实 macOS Electron 流程和页面样式检查；本轮只验证当前源码应用，不把 Windows 交叉构建写成真机结果。

- 版本目标：`1.8.1`，雷达入口从本地数据页面调整为两个固定网页卡片：`Codex 雷达` 与 `分布式雷达 Codex 站`。
- 自动化基础：`npm run format:check`、`npm run lint`、`npm run typecheck`、`npm test`（45 个文件/307 项）、`npm run build` 和默认 Electron E2E（6/6）通过。
- 真实流程命令：`SUB2API_RADAR_REAL_E2E=1 SUB2API_REAL_EVIDENCE_DIR=real-test-evidence/macos-1.8.1-radar-final-rerun npm run test:e2e -- --grep "embeds both real Radar sites"`，结果为 1/1 通过。
- 真实行为：两个公网地址均在当前主 `BrowserWindow` 的 `WebContentsView` 中加载；没有创建新 `BrowserWindow` 或调用系统浏览器；视图 bounds 从 `x=284,y=80` 开始，窗口缩放到 `960x640` 内容尺寸后更新为 `676x560`。
- 返回行为：右上角应用按钮关闭后恢复两个卡片；对分布式雷达视图发送真实 Electron `Escape` 输入后恢复卡片；测试结束时主应用正常关闭。
- 视觉证据：`real-test-evidence/macos-1.8.1-radar-final-rerun/` 中的 `radar-chooser.png`、两个大窗口整窗截图和 `radar-codex-window-small.png` 检查卡片、toolbar、关闭图标、远程页面边界及不同尺寸无重叠；`radar-codex.png`、`radar-distributed.png` 为远程 `WebContentsView.capturePage()` 的实际站点截图，确认两个目标网页非空且可滚动。
- 双平台产物：`npm run dist:mac` 与 `npm run dist:win` 均成功；DMG `hdiutil verify` 为 `VALID`，macOS 应用为 Mach-O ARM64，Windows 安装器为 PE32+ x86-64，Windows 仅为交叉构建证据。
- 产物 SHA-256：DMG `483a46931dd9b0c358060e36ccfa0314aaa0043b9ed5bb9215f2c4b1961eb5f6`；DMG blockmap `30a6a8c9661822f95ea1fd41eda33749784f90f82afef8cda3d4e7da8a73257c`；EXE `dc636da23268ba3ac63eeb422ddf7174cdafeb1e27ccfcdee45b71caa54dabc7`；EXE blockmap `d86c3e10770ded07561db41085fb2f94fbf2bcbcbf2b3d82604fd9cc00e9ce8f`；本地产物 `update-manifest.json` `e123a2b8635ba851f404b62bedd857e6c76c039f95f5404f5bcd464c736c695d`。manifest 中两平台安装包 SHA 已与实际文件复核一致。
- GitHub Release `1.8.1` 已验证为 stable、非草稿、非预发布，五个目标资产全部存在；正式远端 `update-manifest.json` SHA-256：`3f9bb1d6ae22af3df83ef926f639d8154fc9a2169e3814eaa1c92e442ce715ad`。GitHub 与 Gitee 当前分支均指向提交 `7d245415b6bf8eeb8cea97faa8c47665ed463421`。
- 当前限制：远程站点内容由站方控制，Codex 只保证固定 origin、嵌入容器尺寸和应用返回控制；Windows 只执行项目定义的 x64 交叉构建和产物审计，待 Windows 真机时再补充安装/重启证据。
- 证据目录：`real-test-evidence/macos-1.8.1-radar-final-rerun/`。未记录账号、密码、Token、Cookie、CDP 地址或完整敏感响应。

# 2026-08-29 2.5.2 真机验收记录

状态：`待实测`。

已完成准备：`npm run pack` 成功生成 macOS ARM64 内部目录包；Electron E2E 运行中 15 项有 11 项通过、2 项跳过，窗口几何用例单独复跑通过。

当前待验证：macOS ARM64 打包应用中主窗口隐藏/最小化后渠道仍按 10–20 秒刷新；总览、渠道页、悬浮窗 `fetchedAt` 一致；OpenCodex 真实 localhost:10100 数据的三项 Token 与缓存率；Radar/常用网站导航；Sub2API 多服务器切换、登录态隔离、窄窗口滚动和无重叠布局。综合本地站点用例仍受后台自动刷新与既有测试响应序列竞争影响，尚未记为通过。

本轮没有伪造真机截图、真实站点结果或发布资产结果。

# 2.6.0 新增真机检查项

- 待在真实 macOS 打包应用中确认：全部站点每张卡片和悬浮窗关联渠道区域均显示 更新于 HH:mm:ss，渠道 10–20 秒批次成功后时间同步变化；无数据与 stale 场景不出现错误时间。
- Sub2API 服务器遮挡修复：自动化已验证打开服务器后切换到服务器管理 Shell，点击其他菜单会关闭原生网页视图；修复后重新启动打包应用截图时 Computer Use 返回 timeoutReached，真实截图复核暂记受阻。
