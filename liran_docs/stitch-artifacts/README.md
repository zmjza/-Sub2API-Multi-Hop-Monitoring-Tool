# Stitch 产物登记

## 倍率对比增量项目（2026-07-21）

- Project Title：`空间视觉优化方案`
- Project ID：`15431967796154605995`
- Screen Title：`中文标签版倍率对比卡片组`
- Screen ID：`227212f94b9b427e887875644935ab9e`
- 目录：`15431967796154605995/01-rate-comparison/`
- HTML：`screen.html`，第一视觉事实来源。
- 截图：`screenshot.png`，实际下载为 `2560×790` 的头部裁切，仅用于标题和控制区校对。
- HTML 引用的 OpenAI、Claude、Gemini、Grok PNG 已下载到 `src/renderer/assets/rate-platforms/`，运行时不再访问远程资源；Antigravity 使用 Gemini 图标。

该增量只承接“全部站点”的倍率对比带，不替换下方站点卡片或其他 Screen。原始 HTML 不修改；真实业务继续由现有 Renderer 数据流提供。

项目：`Sub2API 桌面监控台`
Project ID：`10340103531009759971`
设备类型：Desktop
设计系统资产：`assets/eecbe145dfb04e67b467b5dc295d7fee`
主题：`LIGHT`，字体：Inter，侧栏：216px，工具栏：56px。

## 事实来源优先级

1. 本目录下载的 Stitch HTML。
2. Stitch 项目返回的设计系统、代码和规范。
3. 本目录下载的截图。

## Screen 登记

| 目录                | Screen ID                          | Stitch 标题                       | 尺寸      | HTML   | 截图   |
| ------------------- | ---------------------------------- | --------------------------------- | --------- | ------ | ------ |
| `01-overview`       | `1c782820120b464b91b8c59135fd07e3` | 全部站点（全宽布局 & 深度汉化版） | 2560×2048 | 已下载 | 已下载 |
| `02-usage`          | `5184f4f26e7d48968b505e2b72e1ae14` | 使用记录（完整功能恢复版）        | 2560×3388 | 已下载 | 已下载 |
| `04-channels`       | `9ee4845a9bdc44ed8d9dec8d224bfd59` | 渠道状态（布局整理 & 深度美化版） | 2560×2048 | 已下载 | 已下载 |
| `05-floating`       | `29f04763029b4f6db724b9d31427f34a` | 悬浮窗（字号优化 & 滚动标题版）   | 2560×2048 | 已下载 | 已下载 |
| `06-sites-settings` | `65b1d63815f14859b56b47667d620d06` | 站点管理与设置（标注修复版）      | 2560×2048 | 已下载 | 已下载 |

Design System 不是独立业务页面，不建立单独 UI 壳；它作为全局 token 和控件规格依据登记。

## 已观察到的产品事实

- 全站导航包含：全部站点、使用记录、渠道状态、站点管理、通知、设置；账号模板功能已取消，不作为主导航或内部 Tab。
- 全部站点原始 HTML 包含总余额、今日消耗 Token、今日消耗、站点状态计数、站点表格和悬浮窗规格实例；当前产品把悬浮窗作为独立 Renderer，不在总览页面保留预览。
- 使用记录包含站点切换、总请求数、总 Token、总消费、标准费用、平均耗时、筛选、刷新/重置、列设置、CSV 和密集记录表；原始 HTML 中的地区操作不属于当前业务范围，不进入真实实现。
- 渠道状态包含渠道列表、平台/模型、正常或降级状态、对话延迟、端点 Ping、7 日可用性、自动刷新倒计时和状态时间线。
- 悬浮窗包含多站实例、左右切站、动态标题、余额、今日 Token、今日消费、正常/延迟较高/连接中状态和滚动标题。
- 站点管理与设置包含站点录入、批量验证任务、连通性测试/获取额度/验证模型步骤、通知规则和通用设置；不包含账号模板功能。
- 设计系统中的设计说明仍包含旧的 Dark Mode 文本，但当前项目需求已明确只保留固定浅色；承接时以项目 `liran_docs` 的固定浅色需求覆盖该旧说明，不生成深色 UI。

## 项目确认覆盖项（2026-07-14）

Stitch 原始 HTML 保持不修改，以下属于用户确认的正式产品覆盖规则，由真实 Renderer 实现并在 UI 壳清单记录：

- 全部站点不展示原型中的悬浮窗预览，独立悬浮窗仍保留为第五个业务界面。
- 使用记录不展示原型中的地区列或地区批量操作；新增真实站点/模型/分组/Key 下拉、思考等级和每页 20 条规则。
- `1440 x 1024` 是视觉核对参考，不是固定缩放画布；正式 Renderer 铺满可调整大小的 BrowserWindow。
- 主窗和悬浮窗使用无框外壳；窗口生命周期、四角停靠、非置顶桌面常驻和真实状态由项目需求覆盖原型静态行为。
- 原型静态文案和样例数据不得冒充真实 loading、success、stale、error、倍率或更新时间。

## 下载说明

HTML 和截图由 Stitch MCP 返回的 hosted URL 使用 `curl -fL` 下载。下载时间、URL 和文件名以当前工作区文件为准；不把任何账号、Token 或真实站点凭据写入产物。

## 当前使用边界

- Stitch HTML 是第一视觉事实来源，代码/CSS/规范是第二来源，截图只用于辅助校对。
- 当前仅保留固定浅色，支持减少透明效果和高对比度降级；原 Design System 中旧主题说明不生成产品入口。
- 这些文件是只读原始产物，本轮文档校准不修改 HTML、CSS 或截图。
- 当前已完成实例级承接、业务接线、自动化验证和 macOS 打包应用逐页检查；Windows 仅完成交叉构建与产物结构检查，未真机。
