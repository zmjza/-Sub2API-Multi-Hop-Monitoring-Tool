import type { SiteDraft } from './types';
// Static UI preview data only. Credential values are intentionally absent.
export const siteDrafts: SiteDraft[] = [
  {
    name: '示例站点',
    url: 'https://example.invalid',
    validation: '待测试',
  },
];
